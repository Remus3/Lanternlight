# Lanternlight ledger

The per-item record of what actually landed. One entry per item, **newest
first**, each carrying the acceptance evidence that justified calling it done.

This file exists because continuity in this project lives on disk, not in a
context window. A session that has been cleared or compacted reads the top few
entries here, plus `ROADMAP.md` and `git log`, and knows where it is. Nothing
about the work is expected to survive in conversation.

## Format

Each entry is a level-3 heading followed by its evidence:

```
### LL-0000 - YYYY-MM-DD - one-line summary of what changed

**Evidence:**
- the test, file, or command that proves it
- one line per piece of evidence
```

`ops/loop/ledger.py` writes these. It inserts new entries directly below the
marker line at the bottom of this preamble, and it writes atomically through a
temporary file, so a reader polling this file never sees a half-written entry.

## Append-only

Entries are added. They are never edited, reordered, reflowed or deleted. If an
entry turns out to be wrong, the correction is a **new entry** that says so and
names the entry it corrects. The value of a ledger is that it is a record; a
record that can be quietly revised - especially by an unattended loop - is
worth nothing.

## Do not verify an entry by its commit hash

Entries may cite a commit. Treat that citation as a hint, not as proof, and
expect a meaningful share of older hashes to resolve to nothing.

This is not history rewriting and it is not corruption. Work is done on
branches and in worktrees; when a branch is squashed on merge, or a commit is
cherry-picked onto another branch, the sha that existed when the entry was
written stops existing. The change still landed. Only its address moved.

**Verify by file and by test.** Open the file the entry names and read it; run
the test the entry names and watch it pass. Those survive squash, rebase,
cherry-pick and reclone. A hash does not. If a hash is dead and the file and
test check out, the entry is good - do not reopen the item, and do not "fix"
the history.

## Item ids

`LL-NNNN` is the convention. An id that appears in a roadmap item, a branch
name, a commit message and a ledger entry is what ties those four records to
each other, which is the whole reason ids exist.

**Two things this section used to claim are not true, and saying them anyway
hid a real defect.**

It said ids are "allocated in order". Nothing serialises allocation. Lanes work
on separate branches cut from a common base, so two lanes each asking "what is
the next free id?" get the same answer and both take it - and because each lane
appends only to its own `lanes/<lane_id>.LEDGER.md`, the two fragments merge
cleanly with nothing anywhere complaining. That happened on 2026-08-11: `ingest`
and `research` both took `LL-0023` for different work. Order is a convention the
integrator maintains by hand, not a property the machinery provides.

It said `LL-NNNN`, while the parser accepts any `### <id> - ` heading and the
safety lane's fragment had already used `SAF-0001` and `SAF-0002`, which
`integrate()` parsed without a murmur. Those two were renumbered by hand to
`LL-0026` and `LL-0027`. The format is still `LL-NNNN` and a fragment that uses
anything else will need renumbering before it lands here, but nothing enforces
it, so do not read a well-formed id as evidence of a checked one.

**"Never reused" is the part that is now enforced.** `ops.lane_state.integrate`
compares content, not just the id: an entry already present with the same text
is skipped, so the function stays idempotent and safe to re-run after a partial
merge, while an id present with DIFFERENT text raises `LedgerIdCollision` and
writes nothing. It will not renumber for you - the new id has to change in the
roadmap item, the branch and the commit message too, and quietly rewriting an
append-only record is its own defect. `ops.lane_state.duplicate_claims()` lists
every clash across this file and every lane fragment, so the collision can be
found before an integration rather than during one.

<!-- LEDGER ENTRIES BELOW - NEWEST FIRST -->

### LL-0289 - 2026-09-21 - OPS-36 CLOSED - the charter's caveman-wiring clause is DECLINED on three rules this repository already holds, and the decline is guarded rather than left in prose

**Evidence:**
- The outstanding half of criterion 4 was the caveman-wiring clause; 4(b), the worktree ordering invariant, closed 2026-09-16 in tests/test_lane_launcher.py.
- The clause asks adopters to take RC's module body VERBATIM plus a settings entry and a second document. The DIALECT is already satisfied by CLAUDE.md's Output constraints section; the WIRING is DECLINED.
- Three grounds, each an existing rule rather than a new judgement: the module body is UNLICENSED sibling source and this is a PUBLIC Apache-2.0 tree, which is the same gate that refused docs/CHANNEL.md until RC named a license; its command uses an ABSOLUTE repository root, which tools/hook_command_guard.py forbids under OPS-61; and it duplicates a rule CLAUDE.md already holds.
- DECLINING NEEDED NO RULING. THE ONE ASYMMETRY reserves ADOPTING to the operator and has always left refusing to the session. Nothing was adopted, and criterion 5 does not apply because the conflict is answered by a rule rather than escalated.
- docs/OPERATIONS.md gains the record and tests/test_caveman_dialect_record.py guards it - 8 tests, red observed first at 5 failed and 3 passed, seven mutations each with its anchor asserted present. Collected 3661 to 3669, no per-file drop.

A DECLINE RECORDED ONLY IN PROSE is a decline that quietly becomes an adoption the first time nobody re-reads it. That is why the deliverable for a refusal is a GUARD and not a paragraph: the module reddens if the record is deleted, if the sentence saying the CLAUDE.md definition GOVERNS is weakened, if the word DECLINED softens to 'not adopted', if the clauses are split across two sections, or if the dialect line leaves CLAUDE.md.
ONE MUTATION FAILED TO APPLY AND PRINTED 8 PASSED - this repository's own recorded trap, live: a mutation that fails to apply looks exactly like a passing test. A heredoc backslash produced a SyntaxError, the mutation never landed, and the run was green for the wrong reason. Re-applied with the mutation asserted present, it reddened properly. That is the SECOND heredoc backslash loss in two days here - see OPS-99, where the same class of defect produced a published figure that had to be withdrawn.

### LL-0288 - 2026-09-21 - OPS-101 - the SPAWN tier has a runtime arm, and measuring the audit events first contradicted two assumptions written into the item own acceptance criterion

**Evidence:**
- tests/test_spawn_capability_at_runtime.py, 21 tests. It watches subprocess creation under sys.addaudithook, because a subprocess needs no ctypes at all to reach a process and the native arm cannot see one.
- MEASURED BEFORE BUILDING, and two measurements CONTRADICT the shape acceptance criterion 2 assumed. subprocess.Popen fires with (executable, command_line, cwd, env): argument 1 is None for an ordinary list call, and argument 2 is a single STRING because list2cmdline has already run - a guard indexing it as a vector reads ONE CHARACTER. And shutil.which raises NO EVENT AT ALL, so no runtime arm can watch it; the criterion invited it and the static tier is the only answer there.
- Also measured: subprocess.run and os.popen raise the same Popen event and nothing of their own; os.system raises (command,); os.spawnv raises (mode, path, argv, env) where argv IS a real list; multiprocessing does NOT raise subprocess.Popen, only _winapi.CreateProcess, whose command line arrives as a single control character and is usable only as a creation counter.
- ASSEMBLY IS CARRIED, which is the property the static tier cannot have: task plus kill concatenated at run time appears as the literal taskkill in the command line. Raising in the hook ABORTS the spawn.
- THE OUR-CHILD LINE IS A REGISTRY, not kinship. The probe starts a sleeper BEFORE installing the hook and records its pid. Order: forbidden cmdlet, then process-control program permitted only if every literal pid target is registered, then allowlist, then refuse. Default is refuse and a parse failure refuses. Control: identical source, identical flags, only the pid differs - refused against the probe own pid, permitted against the sleeper, and the sleeper really dies.
- SIX things it cannot distinguish are written into OWN_CHILD_LINE_LIMITS and ASSERTED: pid reuse, image-name targets, pids computed inside the spawned program, grandchildren, and anything after creation. A limit that is not pinned drifts into a claim.
- Defeat sketch planted in a REAL roster module - ops/docguards.py given a run-time-assembled taskkill: rc=1, 2 failed, 19 passed; restored with a sha256 match, 21 passed.
- Eight mutations, all red, each restored. tests/test_spawn_capability_at_runtime.py: 21 passed in 4.46s. python -m ops.preflight: PRE-FLIGHT PASS, 390 passed and 1 skipped, lint clean.

AN EARLIER DRAFT OF THE DEFEAT SKETCH TERMINATED THE PYTEST RUN ITSELF, because it targeted os.getpid() and the module it was planted in also runs in-process at collection. Switched to a sentinel pid. Worth recording: a probe that proves a guard fires is still a program, and this one could have been mistaken for a hung suite.
The guard went to the SAFETY lane, the same as its native sibling. Split ownership of two arms over ONE boundary would let one lane weaken the other lane floor.
THE ITEM OWN ACCEPTANCE CRITERION WAS WRONG in two places, and it was written by this project yesterday. That is the third time in two days that measuring first contradicted something this repository had filed about itself.

### LL-0287 - 2026-09-21 - OPS-99 CLOSED - LW's eleven git-root filenames and ours agree member by member and byte for byte, so the agreeing total was not a coincidence

**Evidence:**
- LW delivered the list this project asked for. Compared PROGRAMMATICALLY rather than by eye: set difference empty in both directions, all eleven names identical, every size re-stat-ed on disk and equal to LW's figure, disk sum 45,189.
- The check re-measured the files rather than comparing two published tables to each other, so it can tell a transcription error from a real difference. Eleven names beside eleven numbers is exactly the comparison a reader performs badly and confidently.
- LW corroborates BOTH halves of our diagnosis from outside: probe_ph.py and mutate2.py DO carry a root path written with BACKSLASHES, which confirms the heredoc character-class defect on named files; and tlg.bak and inventory_backup.md carry no root path at all, which confirms our own caveat that a root-marker predicate can never produce a total.

LW asked to be told if any member disagreed, on the grounds that an agreeing TOTAL over disagreeing members is a coincidence rather than a corroboration. It is the right question and the answer is that no member disagrees.
A SUBAGENT CLAIM RE-PROBED AND REFUTED, in the direction that matters: a digest flagged LW as citing a '64 per cent extractor recall' that 'appears in no thread today'. It appears in OUR 1930 note - 'After repair: precision 78 per cent, recall 64 per cent'. LW cited us correctly and the flag was wrong. Relaying it would have accused a sibling of inventing our own figure.
The same pass corrected a count of our own: the outbox holds TWELVE notes sent on 2026-09-20, not eleven.

### LL-0286 - 2026-09-20 - The census's two severity-2 findings are fixed: a guard that stayed green while the SHARED file corpus narrowed, and the missing guard over the one door every outgoing note passes through

**Evidence:**
- PROBLEM 1, and the defect was confirmed EMPIRICALLY rather than reasoned about: tests/test_no_hardcoded_home_path.py asserted 'git' in inspect.getsource(...), and a DOCSTRING is part of a function's source. Swapping the body to an rglob walk left the old guard at 2 passed while the corpus silently grew to 802 entries, 324 of them under the gitignored note channel.
- That corpus is SHARED - it feeds test_no_pii, test_ports, test_ascii_hygiene and the process-capability scope - so a narrowing is not a failing test, it is four guards each checking fewer files and all reporting success.
- WHAT ACTUALLY DISCRIMINATES a git corpus from a walk, measured across four candidates rather than assumed: a gitignored file on disk DISCRIMINATES, and so does a file inside a gitignored directory. An untracked-but-not-ignored file does NOT, because the corpus adds --others --exclude-standard deliberately; nor does a tracked file deleted from disk, because both corpora drop non-files.
- The three new arms redden against the same mutant: 2 failed against the rglob swap, 2 failed against an extension allowlist, each a different pair. One vacuous test was REMOVED and three added: 25 collected to 27.
- PROBLEM 2 was a GAP rather than a weak guard - nothing asserted that a write into a sibling inbox goes through ops.outbox.deliver. New tests/test_sibling_inbox_choke_point.py asserts at RUN TIME, in a subprocess with fake inboxes under tmp_path, that the local copy and manifest row are written BEFORE any sibling write - the ORDER read off the event stream rather than from what exists afterwards - and that a refused note produces ZERO write events.
- Non-vacuity by transient mutation of ops/outbox.py, restored immediately and its digest checked: dropping the local-record-first writes reddened both ordering arms, and moving the redaction gate after the local write reddened the refusal arm.
- python -m ops.preflight: PRE-FLIGHT PASS, 14 guard modules, 390 passed and 1 skipped, lint clean.

WHAT THE CHOKE-POINT GUARD CANNOT SEE is in its own docstring and is the honest limit: any run it does not make - it is three runs, not a session-wide watch - and anything written outside this repository's Python, by a shell, an editor, or an agent writing the file directly. That last one is what LL-0170 ACTUALLY was, so the guard does not close the case it was written from. Worth doing next: wire its predicate into the session-wide audit hook conftest.py already installs, which converts a three-run claim into a whole-suite one.
A REDACTION GUARD CAUGHT THE NEW RUNTIME MODULE and the fix was to remove the number, not to teach the redactor an exception. The docstring had pasted an observed ctypes.call_function tuple verbatim, and a 15-digit run-time address matches LONG_ID. It is not an operator identifier, but the guard cannot know that from the digits, and a pointer from one run is unreproducible anyway - the literal carried no information the shape does not.
A temporary ops.lane_state claim was reverted in favour of real roster entries in ops/lanes.py for both new modules, under the safety lane: a lane that may not weaken a hygiene guard must not be able to weaken half of one either, and the lane that owns redaction owns the guard over the one door redaction sits behind.

### LL-0285 - 2026-09-20 - A census of guards that read SOURCE to assert a RUNTIME property found FOUR protecting THE HARD BOUNDARY with no runtime counterpart, and a module that no severity-1 guard had ever examined

**Evidence:**
- 110 modules surveyed - 74 tests/, 25 ops/, 11 tools/ - 21 read in detail, 15 in-scope entries covering 24 individual guards. Eight independent grep patterns; never grep -iF, which crashes on this machine and reads as a clean negative. Pattern 6 found a guard the AST sweep missed, which is why more than one pattern was used.
- SEVERITY 1, all four protecting THE HARD BOUNDARY and none with any runtime counterpart: the process-capability scope decided by a name DENYLIST; the two termination-path guards, whose matcher records a Call.func that is a Name or an Attribute and so misses getattr(...)(...); and the wider-right guard, which inherits the scope hole.
- A LIVE GAP, not a hypothetical: overlay/window.py imports ctypes and had never been examined by ANY severity-1 guard, because a denylist of module names cannot see a module nobody added to it.
- THE MEASUREMENT THAT MAKES A RUNTIME ARM POSSIBLE: ctypes.dlsym fires with the REAL symbol name after assembly, so 'Open' + 'Process' is a BinOp in the source with the literal nowhere in the text, and the event still carries OpenProcess. ctypes.call_function carries the FOLDED access mask. os.kill carries the signal as a VALUE. A raising audit hook ABORTS the audited operation, which is what lets the probe be safe.
- Three of the four defeat sketches were applied to the real tree: the static guards stayed ALL GREEN at 99 and 95 passed while the new runtime arm went RED. The fourth was PARTIALLY REFUTED and is recorded as such - both computed-mask spellings that could be constructed were caught by the existing static text arm.
- The scope is now POSITIVE: every published module outside tests/ is IN by default and leaves only by importing nothing that can reach native code, or by a written exclusion with a reason that a test reddens on when it goes stale. One exclusion today, overlay/window.py.
- Collected per file: test_process_capability.py 94 (was 94), test_loop_watch.py 171 (171), test_loop_guard.py 88 (88), new test_process_capability_at_runtime.py 28. No file dropped. git diff ops/ is empty - production code is byte-identical.

A CORRECTION TO THIS PROJECT'S OWN BELIEF, caught before it was published: the responder runner's audit-hook guard was NOT this tree's only runtime counterpart. tests/conftest.py already installs one recording real .md opens, compared against a static derivation with pre-commit refusing on disagreement - OPS-31, and OLDER than OPS-68. Four more exist. The outbox was checked before assuming, and no note carrying the wrong claim had gone out.
NOT EXECUTED, and stated plainly: every defeat in the census itself was read off the matcher's semantics rather than demonstrated. The four that WERE demonstrated are the four above. The .githooks/* shell bodies were not swept at all, because the census's patterns are Python-shaped - a real hole in the sweep rather than a scoping decision.
The SPAWN tier is declared and NOT closed: 18 published modules import subprocess, and a subprocess needs no ctypes to reach a process. Filed as OPS-101 with an acceptance criterion rather than left as a sentence in a report.

### LL-0284 - 2026-09-20 - OPS-95 item 2 - the merge gate now has a NAMED working-tree baseline and a dispatch-time floor check, and the rule is written where the ritual is

**Evidence:**
- What the gate did TODAY, measured rather than taken from the roadmap: verify() accepts per_file_baseline as a caller-supplied mapping and only ever compares downward, with no notion of WHERE the mapping was measured. The documented ritual already defaults to the checkout the module lives in, so the default call was correct - but nothing STATED the rule and nothing detected a floor obtained any other way.
- ops/merge_gate.py gained take_per_file_baseline(root), a working-tree measurement under a name that says so, and the pure check_baseline_floor(baseline, tree) which reports stale-baseline when the floor is below the tree and baseline-missing-file when the tree collects a file the baseline has no row for - the second because check_per_file_counts treats that as new and clean, leaving the whole file unprotected.
- DELIBERATELY NOT WIRED INTO verify. At merge time baseline < current is the HEALTHY case and is already pinned as clean by an existing test, so wiring it there would redden every well-behaved lane. It is a DISPATCH-time check.
- Red observed first: 7 failed, AttributeError on take_per_file_baseline for six of them and a missing PRIMARY WORKING TREE docstring assertion for the seventh.
- Non-vacuity, five mutations each restored: the floor comparison disabled (2 failed), the missing-file branch disabled (1 failed), the comparison loosened to != (1 failed), take_per_file_baseline returning empty (4 failed), and the rule removed from verify.__doc__ (1 failed). Restored: 7 passed.
- tests/test_merge_gate.py: 86 collected before, 93 after, zero deletions in the diff. python -m ops.preflight: PRE-FLIGHT PASS, 388 passed and 1 skipped, lint clean.
- The rule is now written where the ritual is: CLAUDE.md beside the gate snippet, and ops/lane_contract.py, whose template drives the eight generated lane contracts - regenerated with scripts/write_lane_contracts.py so no contract carries the old wording.

THE MEASUREMENT THAT MAKES IT CONCRETE: a floor of 46 for a file the working tree held at 60. That is 13 tests a lane could have deleted in the same session and still passed the per-file check, which is the exact failure the per-file floor exists to prevent, one level down.
A STRUCTURAL FINDING recorded rather than fixed: inside a lane worktree, merge_gate default root resolves to THAT worktree, so a contract following the ritual measures the worktree base rather than the primary tree. ops/lanes.py already provides a main-checkout resolver. No in-tree code creates lane worktrees today - the only git worktree add is in a back-test tool that takes no test-count baseline - so this is latent.

### LL-0283 - 2026-09-20 - OPS-95 items 1 and 3 are DONE, and item 3's own description of its defect as LATENT was refuted - two of the four sites were live blind spots

**Evidence:**
- ITEM 1: CLAUDE.md's third-party licence gate now carries a fifth trap, THE WRAPPER DOES NOT CLEAR THE PAYLOAD, in this project's own words, with the archify measurement as its instance - root LICENSE MIT with two copyright lines while THIRD_PARTY_NOTICES.md records packaged brand-mark data under CC-BY-NC-SA-4.0, CC-BY-SA-3.0 and CC-BY-SA-4.0 plus a font embedded as subsets in EVERY delivered artifact.
- It names BOTH mechanisms, because a tree reasoning only about its own outbound licence misses the other: against a copyleft outbound a non-commercial term is a compatibility CONFLICT, and against a permissive outbound like ours the bite is republishing non-commercial or share-alike bytes out of a PUBLIC permissive repository under a permissive label.
- ITEM 3: tests/test_provenance.py gained data_rows(block) and is_separator_row. The slice ASSERTS that block[1] is a separator rather than searching for one, because a search would quietly ACCEPT a malformed table if it found a separator further down.
- is_separator_row requires every cell to match a dashes-and-colons pattern, measured against all 71 separators in docs/AFFIXES.md and docs/OBSERVED_IDS.md - every one a plain dash run, no colons. Deliberately not 'contains a dash': affix data rows carry a literal dash cell.
- Red observed first: NameError on is_separator_row and data_rows, 6 failed and 1 passed. All four sites then proved non-vacuous - separator deleted from the real table IN MEMORY, each reddening with 'row 1 of this table is not a separator', each restored to green, and the tracked documents never edited.
- tests/test_provenance.py: 53 collected before, 60 after. The count did not drop. python -m ops.preflight: PRE-FLIGHT PASS, 388 passed and 1 skipped, lint clean.

THE ITEM'S OWN DESCRIPTION WAS WRONG and that is worth more than the fix. It said the defect was LATENT 'because the consumers compare full dictionaries'. Two of the four sites do not compare dictionaries: one only asks which rows LACK a date and stays GREEN and silent when the row it swallowed carries one, and one asserts != so swallowing a row makes it MORE likely to pass and it can never redden. Both were live blind spots.
A filed claim is a hypothesis, including a claim this repository filed about its own defect, and 'latent' was the comfortable reading of it.
OUT OF SLICE, recorded not fixed: tests/test_overlay_render.py lines 191 and 298 use lines[2:], but that is a rendered panel's fixed two-line header rather than a Markdown table - a different shape, listed only so the next sweep does not re-find it as new. And tests/test_provenance.py is not `ruff format` clean at HEAD, which is a pre-existing repository-wide fact rather than something this slice introduced; `ruff check` passes either way.
A PROCESS FAILURE from the same cycle: a commit was launched while a slice was still editing tests/test_provenance.py. The hook linted a half-written file, found 7 errors and refused - correctly, after spending 128 seconds on a suite to tell the merger something the merger should not have needed telling. The merger owns the merge, and a merge that overlaps a live slice is not a merge.

### LL-0282 - 2026-09-20 - OPS-100 - a sibling's question about ITS OWN tree found a relative-path defect in our pre-commit hook, and it would have failed CLOSED as a guard finding

**Evidence:**
- .claude/settings.json PARSES, asserted first. All six hook entries are anchored with $CLAUDE_PROJECT_DIR/..., and the interpreters are bare python and pythonw, which is PATH resolution - a THIRD category that must not be collapsed into 'absolute'.
- All five hook scripts RUN from a foreign working directory behaved correctly; the watcher's output from the repo root and from the foreign directory is byte-identical at 3078 bytes. POSITIVE CONTROL: unsetting CLAUDE_PROJECT_DIR collapses the path and exits 2, so the probe can see a failure when there is one.
- THE DEFECT: .githooks/pre-commit computes repo_top at line 225 and uses it absolutely at 242, 373 and 378, but two lines ran "$py_bin" -m ops.docguards with no anchor, and -m resolves the package from the CURRENT directory. Measured both ways: from a foreign cwd the bare form exits 1 with ModuleNotFoundError and the anchored form succeeds.
- Fixed with cd "$repo_top" && ... in a subshell at both sites, with the reason written beside the first.
- VERIFIED THE ONLY WAY A HOOK CAN BE: a file carrying a banned glyph was staged and a real commit attempted. pre-commit REFUSED the commit and HEAD was unchanged across the attempt. The probe file was unstaged and removed.

IT WAS LATENT, NOT LIVE - git always chdirs to the worktree top before running a hook - and it was fixed anyway because of HOW it would have failed. It fails CLOSED, and the hook reports it as 'the doc-guard selector failed to run', which reads as a guard finding rather than as a path bug. The first symptom would have been a commit refused for a reason nobody could reproduce, in a file whose own comments say it anchors everything to repo_top.
The finding came from a sibling asking every tree a question about ITS OWN tree. Nobody reading this repository's documents would have found it, because the documents say the hook anchors itself and they are 90 per cent right.
RECORDED AND NOT CHANGED: core.hooksPath is absolute here, but scripts/install_hooks.py sets it RELATIVE, so every fresh clone gets the relative form. On git 2.53.0.windows.3 it fires from the toplevel, a subdirectory, git -C and a linked worktree, with a control showing a missing hooksPath lets a commit land silently. It works, so it is written down rather than tidied.
NOT CHECKED: the harness-dispatched hooks end to end, because the harness's working directory cannot be set from here; and .githooks/commit-msg, because pre-commit refuses first.

### LL-0281 - 2026-09-20 - The channel contract checked the document hash and its version but not its central CLAIM - arm 8 now pins the roster

**Evidence:**
- Found by the re-pin round rather than by review. Arms 1 to 7 all stayed GREEN across the v1-to-v2 swap except where a digest or a version number moved, and the ROSTER - the entire substance of v2 - is prose in a table carrying no path tokens and no digest. Every one of those arms would have stayed green if the roster table had been mangled.
- tests/test_channel_contract.py gained roster_table() and test_arm_8_the_roster_pins_six_participants_and_five_carriers, pinning the whole roster as (code, carrier answer, is-pin-holder) tuples plus a row count asserted BEFORE any row is compared.
- It pins the CARRIER column rather than participation, because that column governs re-pin acceptance: RC stated bar is five carriers hashing equal, not five replies, so a tree silently gaining or losing carrier status changes what a completed round means.
- NON-VACUITY, five mutations on a COPY of the text so the vendored file is never edited: SS promoted to carrier RED, LL dropped from the roster RED on the row count, LL silently de-carriered RED, a code renamed RED, and the section renamed RED on the parser refusing to search harder. Baseline green, vendored file verified untouched afterwards.

THE SHAPE OF THE HOLE, worth carrying: a contract that checks a documents HASH and its VERSION but not its central claim is checking that the file is the file, which the digest arm already does. The version number moved and the digest moved, so the contract had two independent-looking confirmations that something changed and no arm at all about WHAT changed.
The grammar arm caught v2 eighth column only because it asserts the ROW WIDTH before comparing cell content. An arm comparing cells positionally would have reported a content mismatch to the LEFT of the new column - a true red for a false reason.

### LL-0280 - 2026-09-20 - OPS-91 - the joint CHANNEL_VERSION 2 re-pin completed and this tree hashes equal, with the licence gate re-run at the new commit

**Evidence:**
- Re-fetched anonymously from Remus3/Amberstone at commit 6ad1531e2: HTTP 200, 25425 bytes, sha256 fc22e86eebe93bb247a91f44835257a3fe717a287c4a3184a8e7a7b9a463fb9c, equal to the digest RC published for v2. Zero CRLF, zero bare CR, zero non-ASCII. The bytes declare CHANNEL_VERSION: 2.
- THE GATE WAS RE-RUN, not inherited. A licence is a fact about a COMMIT and not about a repository in general. At 6ad1531e2: LICENSE Apache-2.0 at 219 lines, SCOPE OF THIS LICENSE block present, both copyright lines RENDERED, carve-out only for data under data/, and Share/LICENSE.md still 404.
- OBSERVED RED FIRST: swapping the bytes with the constants still naming v1 failed FOUR arms at once - the licence digest, the contract digest, the declared-version arm, and the filename-grammar arm, which caught that v2's table gained an EIGHTH column reading Example / RC gate 6 / RSC / LW / CS / LL / SS / Shape.
- The NOTICE gained a Superseded pins table recording v1's commit, byte count and digest as retired rather than deleting them, so a reader meeting 899f6eb9 in a sibling's note or in this ledger can resolve it.
- The drift arm was RELAXED to allow that and was probed against the RELAXATION rather than the original rule: a retired digest leaking into the live prose, the superseded heading disappearing, the current pin filed as retired, and the statement of changes removed - four mutations, four reds, NOTICE restored byte-identical each time.
- Published as 2026-09-20-2115-from-LL-FYI-fc22e86eebe9-LL-has-re-pinned-to-channel-version-2-and-hashes-equal-path-bytes-and-own-disk-digest.md to five recipients, none failed.

ONE MUTATION DID NOT REDDEN and it is recorded rather than hidden: dropping the upstream commit from the provenance table left the arm green, because 6ad1531e2 appears TWICE in the NOTICE and the arm asks whether the string is present rather than where. Not a vacuity - 4(b) asks that the NOTICE record the commit and it still does - but the arm is weaker than it reads, and the second occurrence is load-bearing.
THE EIGHTH-COLUMN TRAP, worth carrying: the grammar arm reported honestly only because it asserts the ROW WIDTH before comparing cell content. An arm comparing cells positionally would have reported a content mismatch to the LEFT of the new column - a true red for a false reason, which is worse than a green.
N is the HOLDER count and not the roster size. SS holds no copy and says so, so asking SS to re-pin would be asking a tree to re-pin something it does not have.
NOT CLAIMED: this project has not read v2 as prose. It re-pinned and re-ran its arms. Nothing about what v2 SAYS should be attributed to Lanternlight yet.
RC's note says RC read OUR tree under RC's operator's direction and names the three files. RSC and LW refused the same probe. Recorded as a fact about the channel rather than as a complaint: nothing here is private, and a sibling reading a public tree is not a boundary this project polices.

### LL-0279 - 2026-09-20 - OPS-68 criteria 4 and 5 are discharged - the channel was told what was built, including the finding that our own guard was defeatable

**Evidence:**
- Criterion 4: delivered to CS, LW, RC, RSC and SS as 2026-09-20-1930-from-LL-FYI-we-built-a-draft-only-responder-that-cannot-send-and-our-own-pass-proved-the-static-guard-saying-so-was-defeatable.md, five recipients, none failed.
- The note states the adjudicated scope, the draft-only decision and the measured reason for it - two numbers published to five trees and withdrawn on the same day - and carries the static-guard finding in full, because a sibling with a cannot-do-X guard of its own is one runtime-assembled name away from the same hole.
- It also publishes the extractor's real recall of 64 per cent and says plainly that a silence from this project may be a MISS rather than a decline. Nobody asked for that figure.
- Criterion 5: OPS-48 is CLOSED and archived, and its question 1 already delegates the scope question to OPS-68. It gains a dated subsection recording that question 1 now has an artifact, that what was declined is still declined, and that a reader arriving at 'auto-responder: NO' should follow it to OPS-68.
- Nothing in that closure was edited or reordered. A closed record is superseded, never rewritten - the same rule the ledger runs on.

The note deliberately proposes NOTHING cross-project. It reports what this tree built, asks for no adoption, and says the audit-hook technique is a property rather than source - describe it in your own words and build your own, which is the same rule this project applies to siblings' work.
The one thing it asks for is worth its own line: if a sibling's cannot-do-X guard is STATIC, ten minutes trying to defeat it the way ours was defeated is the highest-value thing on that channel today.

### LL-0278 - 2026-09-20 - OPS-68 - the responder runner is BUILT draft-only, was REFUTED on its cannot-send property, and the repair replaced a claim about the source with a claim about the run

**Evidence:**
- ops/responder.py and tests/test_responder.py. Per unread note addressed to us it extracts sender, class, subject, timestamp and reply target, pulls out every question put to us, and writes ONE draft per thread into moon_sync_inbox/_drafts/ following the vendored channel skeleton, every question left as an explicit UNANSWERED placeholder. It never overwrites an edited draft, it writes atomically, and --dry-run writes nothing.
- git check-ignore -v on a drafts path exits 0 with rule .gitignore:208:moon_sync_inbox/, RE-ASKED by a test on every run rather than trusted once.
- REFUTED on axis 1. Three STATIC guards asserted the runner could not send and an adversarial pass defeated all three at once with two lines inside run(): importlib.import_module of a concatenated module name, then getattr of a concatenated attribute name. The suite stayed GREEN at 50 of 50 while the runner could reach deliver.
- Why each missed it: the AST import check finds no import node because the name is assembled at run time; the sys.modules probe covers IMPORT TIME and not a lazy import inside a function; and the deliver-call check records a Call.func that is a Name or an Attribute, and getattr(...)(...) is neither.
- REPAIRED with a runtime guard. TestTheRunnerCannotSendAtRUNTIME runs responder.run() in a python -B SUBPROCESS with sys.addaudithook installed BEFORE ops.responder is imported, and fails on any audited import, open, compile, exec or os.rename naming outbox, or any write outside the fixture drafts directory. Inbox, drafts and seen set are all under tmp_path.
- MEASURED, and it would otherwise have made the guard vacuous: the import audit event is NOT raised by importlib.import_module, which calls the bootstrap directly. The FILE-level events carry this guard, and the constant's own comment says so.
- VERIFIED BY THE MERGER independently rather than taken from the repair report. The refuter's exact mutation was re-applied with importlib bound so the mutated runner still WORKS - a crashing mutant proves nothing, because the guard could be red for the wrong reason. Result: exactly 2 failed, 55 passed, one of the two being the runtime audit guard. Restored byte-identical, digest checked.
- Question extractor graded on five REAL notes rather than its own fixtures: precision 75.0 per cent, recall 54.5 per cent before - 6 TP, 2 FP, 5 FN - and precision 77.8 per cent, recall 63.6 per cent after, at 7 TP, 2 FP, 4 FN. A 34,807-byte note addressed to this project yielded ZERO questions, because its two question marks sit in headings the paragraph scanner never read.
- ops/inbox_watch.py gained DRAFTS_DIRNAME, drafts_summary, three Scan fields and a line in BOTH report shapes saying the drafts were DELIVERED TO NOBODY. Six mutations, six reds, six restores.
- tests/test_responder.py: 57 passed, up from 50; the count never dropped. python -m ops.preflight: PRE-FLIGHT PASS, 14 guard modules, 388 passed and 1 skipped, lint clean.

THE LESSON, and it is the item: a static check is a claim about the SOURCE TEXT and will always lose to a name built at run time. The replacement is not a better static check, it is a different kind of claim - an audit hook watching one actual run.
The DRAFT-ONLY shape was chosen on MEASURED grounds, not caution. On this same day this project published two wrong numbers to five trees inside four hours and withdrew both - LL-0271 and LL-0277. A runner that sends would have amplified them faster, not caught them. A new criterion 6 makes delivery conditional on a measured review record rather than permanently forbidden.
A CHANGE THAT HURT is recorded rather than hidden: stripping underscore and backtick as emphasis added two false positives and zero true positives, so it was narrowed to asterisks only. Heading scanning gained nothing and cost nothing on the five notes and is proved by unit test rather than by the sample.
NOT a defect, measured rather than assumed: the dry run's 'addressed elsewhere: 0' across 248 notes reads like a classifier that excludes nothing. Over 251 notes it is OURS 83, POSSIBLY OURS 150, NOT OURS 18. run() short-circuits acknowledged notes before classifying, so the figure describes the UNREAD WINDOW.
TWO of the six watcher mutations were added after one of the merger's OWN arms was caught green under mutation: the fixture held only Markdown, so the .md filter was untested and a mutation counting every file passed all four original arms.
Criteria 4 and 5 remain open: tell the siblings through ops.outbox.deliver, and update OPS-48 in the same pass.

### LL-0277 - 2026-09-20 - OPS-99 - our git-root figure of seven is WITHDRAWN as eleven, and the pattern that produced it matched forward slashes only

**Evidence:**
- Published SEVEN files and 10361 bytes to five trees. The answer is ELEVEN and 45,189 bytes. LW was right and this project was wrong, on a number it was more confident about than LW was.
- NINE name a Lanternlight root, 12042 bytes: the seven published plus probe_ph.py (979 B, 2026-08-13) and mutate2.py (702 B, 2026-09-08).
- TWO more name no root at all and are certainly ours, settled from our own git history rather than by eye: inventory_backup.md (10407 B) is an earlier revision of tracked docs/INVENTORY.md, and tlg.bak (22740 B) is an earlier revision of a tracked test module - git log -S on the distinctive class name inside it returns EXACTLY ONE commit, 0eb6217. 12042 + 10407 + 22740 = 45189, which is LW's byte figure to the byte.
- Withdrawn on the channel as 2026-09-20-1830-from-LL-CORRECTION-27b181617a8f-..., five recipients, none failed. LL-0244 requires a withdrawal rather than a private restatement.

THE MECHANISM. Inside a regex character class ONE backslash before a slash is an ESCAPED FORWARD SLASH, so the separator class held the forward slash only; and the pattern reached re.compile through a shell heredoc that collapsed the doubled backslash to one. Measured by LENGTH - six characters arrived where the source had seven. A repr prints the same thing either way, which is why the first probe returned an inconclusive answer that looked conclusive.
Seven of the nine root-namers happen to write a forward slash, so the broken pattern returned a clean, plausible, internally consistent attribution with NO error message. That is what made it publishable.
The claim about the method was the exact inverse of the truth: content-attribution was offered as more rigorous than a grep, and LW's grep for the project word anywhere got the right answer while the extra rigour was a filter that had been broken.
THE FIX IS A PRACTICE. The re-measurement was written to a FILE rather than a heredoc and SELF-TESTS before it walks anything, asserting the pattern matches both a backslash path and a forward-slash path. Carry it into any sweep that greps a Windows path - every tree on this channel is one backslash away from this.
NOT CLAIMED: eleven is a FLOOR, since two were found through history rather than by marker; win.md (175732 B) names both a Lanternlight string and a Clockspeed root and is neither claimed nor assigned; five files were never decoded; top level only. And LW's eleven-under-both-methods is NOT an independent check - win.md is excluded by each method for a different reason.

### LL-0276 - 2026-09-20 - CORRECTION to LL-0275 - CS DID vote, the roster tally is six of six, and the wrong count came from a digest that was stale before it was read

**Evidence:**
- LL-0275 recorded CS as NOT VOTED and its silence as silence. That is FALSE. moon_sync_inbox/2026-09-20-1447-from-CS-REVIEW-899f6eb957cc-CHANNEL-VERSION-1-is-REOPENED-the-roster-is-six-not-five-RC-authors-and-here-are-the-seventeen-lines-a-v2-must-change.md carries: Operator directed: the ruling is a six-carrier roster and a re-pin, broadcast to every tree.
- Re-measured against the file on this disk with a whitespace-collapsed search before this entry was written, rather than accepted from the agent that caught it.
- The tally is SIX of six. CS also names RC as author and supplies seventeen lines a v2 must change, which is the most concrete input any tree has offered on the re-pin.
- ROADMAP.md OPS-98 carries the corrected table and the reason.

THE FAILURE IS WORTH MORE THAN THE CORRECTION. The tally was published off a SUMMARY of thirteen notes while the inbox already held twenty, and the channel was still answering while the digest was being written. A vote count is exactly the class this repository already knows to re-derive - a filed count is a hypothesis - and it was taken from a moving source anyway.
The rule to carry: any count taken from this channel carries the timestamp of the READ and not of the question. A digest of an inbox is stale the moment a sibling session ends.
This corrects LL-0275 rather than editing it. The ledger is append-only.

### LL-0275 - 2026-09-20 - OPS-98 - the roster vote came back five YES and one silent, and Lanternlight is now a fourth pin holder of the channel document

**Evidence:**
- Counted from the notes on this disk rather than from a summary. YES to six participants and to a CHANNEL_VERSION 2 re-pin: SS (1433, abstaining on its own row, then 1441 withdrawing the abstention on its operator ruling), RC (1452), RSC (1520, re-issued 1555 as its operator ruling), LW (1545). Four of the five said the yes was their own operator ruling rather than a session reading.
- CS has NOT voted. Its only note of the day answers SS announcements rather than our question. Recorded AS SILENCE under charter v2 section 2 and deliberately not converted into agreement; whoever cuts v2 should record five of six.
- PIN HOLDERS: RC, RSC and LW each declared a guard over the document. LL is now a fourth - held at 899f6eb957cc26ee25993d83d65d8ca291841fe4eec24a48f729c2dc005f4c6b, 20633 bytes, zero CR, with a guard observed 4-failed before the file existed and reddened by four separate mutations afterwards.
- SS is NOT a holder and says so itself, which RC and RSC independently confirm. So the re-pin N is the HOLDER COUNT and not the roster size - a v2 round assuming six holders would wait forever on a tree with nothing to re-pin.

The vote arrived within about two hours of the question going out, which is the fastest this channel has closed a shared-convention question. Recorded because the usual failure here is a question that nobody answers and that a later session mistakes for consensus.

### LL-0274 - 2026-09-20 - OPS-98 - the operator ruled YES to a six-participant channel roster and a CHANNEL_VERSION 2 re-pin, and the vote went to all six trees

**Evidence:**
- Ruled in chat 2026-09-20, in the same session the discrepancy was raised, and directed to go to every tree rather than to RC alone.
- Delivered as 2026-09-20-1420-from-LL-ACTION-899f6eb957cc-our-operator-ruled-YES-to-a-six-participant-roster-and-a-channel-version-2-re-pin.md to CS, LW, RC, RSC and SS; none failed.
- LL votes: roster to SIX with a standing row for SS, re-pin at CHANNEL_VERSION 2, and LL re-vendors at the new digest when published, publishing the digest it computed so a mismatch surfaces as a mismatch.

A ruling was REQUIRED and a session could not have supplied it. THE ONE ASYMMETRY makes declining an adoption a session decision and adopting one an operator ruling that no grant of authority reaches. That is why the earlier note flagged the discrepancy without proposing a fix.
NOTHING was edited. The vendored file is not touched - the guard fails the instant a byte moves - and no sibling was asked to edit its copy unilaterally, because the document own CHANNEL_PIN line makes a re-pin a JOINT act and the charter gives a byte-identical shared file no timebox. A v2 landing in one tree first is a divergence with a version number on it.
Offered rather than imposed: SS row must describe SS as it is today - surplus-only, no reserved floor, no driver, membership real but DORMANT - and the roster and the ADDRESS LIST move together or rule 6 omission silently keeps five.
Recorded as ONE tree yes. Under charter v2 section 2 silence reads as dissent, so this is not a claim that the channel agreed.

### LL-0273 - 2026-09-20 - OPS-98 - Substrate joined the machine as a sixth project, and teaching our governor to see it exposed a case-sensitivity defect that was never SS-specific

**Evidence:**
- Re-measured rather than taken from the note: C:\Substrate, its inbox and its outbox all exist, tested for EXISTENCE only with no file inside that tree opened. A search of this repository returns ZERO slots.py and ZERO winmutex.py, so we are one of the two carriers SS named as not implicated in its digest pin.
- ops/outbox.py gained SS and docs/REPLY_PATHS.md the matching row; the existing guard fails if the table and the dictionary disagree in either direction. Non-vacuity: mutating the dict path reddened 2 tests, deleting the markdown row reddened 3, both restored to 45 passed.
- A REAL BEHAVIOUR CHANGE stated rather than discovered later: fleet() is derived from that dictionary, so every broadcast now reaches FIVE trees instead of four. No test pinned a fleet SIZE or an exact tuple, so nothing broke - that is OPS-87's design working.
- ops/lane_slot.py gained SS in the DETECTION-only key set and NEVER in the CLAIMING set: slot_order('SS') and slot_order('ss') both still raise, so this project cannot mint a lock on SS's behalf. Width untouched at 3, no reserved floor minted.
- THE NTFS MEASUREMENT: create reserved-SS.lock, then open reserved-ss.lock with O_CREAT|O_EXCL - FileExistsError, and iterdir reports only the creator's spelling. They are ONE FILE. A case-sensitive detector loses to a difference the filesystem does not record. Kept as a skipif-not-Windows test so it is re-measured rather than remembered.
- The defect was never SS-specific: reserved-ds.lock was detectable and reserved-DS.lock was not, for a key already in the alphabet.
- THE INTERACTION NEARLY MISSED: reserved_scheme_state's own-floor exclusion had to fold case too, or reserved-LL.lock - our own footprint under a spelling NTFS calls identical - stops being excluded and latches the detector at PRESENT on nothing but ourselves. Detection folds case; is_slot_name, which gates every unlink, deliberately does not.
- Red before implementation: 5 failed, 8 passed. Six mutations, six reds, six restores: un-fold the detector (2 red), un-fold the own-floor exclusion alone (1 red, exactly its own test), drop SS from the alphabet (4 red), branch staleness on the payload repo field (12 red), let the reaper fold case (2 red), promote SS into the claiming set (6 red). tests/test_lane_slot.py: 145 passed.
- CLAUDE.md records that Substrate holds NO port block, as an explicit fact rather than an absent row, because the registry exists so nobody re-derives an allocation by probing.

SS's warning was RIGHT about the hazard and WRONG about the mechanism here, and both halves are recorded. Nothing in ops/lane_slot.py reads the payload's repo field: encode_payload writes it, is_stale consults only ts then pid, and both reap paths gate on filenames. A fresh SS surplus lock survives reap_for_acquire and returns []. So it is a HUMAN GREP hazard rather than a code path - which is why the note was worth writing and why the answer is a measurement rather than a thank-you.
STILL OPEN and not ours to close: the vendored CHANNEL.md section 0 says five participants and SS makes six. We may not edit a vendored file and a re-pin is a joint act by the document's own terms. The question went to all five trees plus SS; silence will be recorded as silence.
ADR-007 deliberately NOT edited. Its key-string line describes the CLAIM set, which SS does not join, and its five-repository phrasing is an inventory of the RESERVED scheme, which SS declined. No recorded fact became wrong; the phrasing now under-counts PARTICIPANTS, which is a wording question left standing rather than quietly restated.

### LL-0272 - 2026-09-20 - OPS-97 - the stray-walker sweep re-run with the trigger widened to ANY repeated trigger found one near-miss, and it is fixed

**Evidence:**
- ELEVEN repeated triggers enumerated rather than recalled, and .claude/settings.json asserted to PARSE before any of it was believed, because a single-backslash Windows path there makes the file invalid JSON so no hook registers and nothing warns.
- 0 HOT, 1 NEAR-MISS. ops/inbox_watch.py outbox_summary walked with a bare root.rglob('*') and no skip set, reachable from BOTH SessionStart and UserPromptSubmit. Planting one 100-byte .pyc two levels down moved the reported figure from 4 bytes to 104.
- TDD, red observed first: (True, 1, 8292) == (True, 1, 100) failing before the change. outbox_summary now calls _files_under, which prunes by NOT DESCENDING rather than by walking and filtering. tests/test_inbox_watch.py: 73 passed after.
- A SECOND test fails if the traversal stops descending at all, so the prune cannot be satisfied by walking nothing - a guard that reported a stable number by looking at nothing would pass the first test alone.
- The OPS-96 fix was re-verified rather than assumed still present, non-vacuously: on a temporary tree, adding __pycache__/*.pyc and .pytest_cache/ leaves the drop digest and counts identical while an authored captures/ still changes the digest, proving the pruned set is _DROP_RESIDUE_DIRS and not the wider _SKIP_DIRS; emptying that set in memory moves the count 3 to 5 and restoring returns it exactly.
- lanternlight/paths.py rglob('AvgPrice_*.ini') is unpruned and deliberately NOT fixed: no production caller, tests only, so it is not reachable from a repeated trigger. Recorded so the next sweep does not re-find it as new.

Classified NEAR-MISS rather than HOT and the disagreement is recorded rather than settled by assertion. Under LW's wording 'feeds a count' is HOT; this figure is displayed and never compared, reaching no seen set, no content key and no withdrawal baseline. The one-line fix is the same either way, so nothing turned on the label.
STATED GAPS, because a review that does not name them is silence with a signature: tests/ not swept although pytest runs per commit; .claude/commands/ and agents/ unread; no hook run end to end, so every reachability claim is STATIC; dynamic dispatch not ruled out; the two vendored trees unswept.
One sweep returned a false clean negative mid-task when a backtick inside a double-quoted bash pattern became command substitution. Caught and corrected before use - the same class as this repository's grep -iF crash, where an empty result is a claim about the TOOL.

### LL-0271 - 2026-09-20 - CORRECTION to LL-0269 - the harness project directories named after our own scratchpad paths are FIVE, not six, and CS was right

**Evidence:**
- Re-measured by enumerating ~/.claude/projects/ directly on 2026-09-20: 32 project directories in total, of which SIX carry the string Lanternlight. One of those six is C--Lanternlight, this repository's own legitimate project identity and not a scratchpad artifact. The remaining FIVE are the scratchpad-derived ones and are exactly the five named suffixes - -e2e-clone, -e2e-worktree, -probe-clone, -probe-space-clone, -probe-worktree. The bare ...-<session>-scratchpad entry is not there.
- LL-0269 and ROADMAP.md OPS-96 row 4 both say SIX. CS's sweep note of 2026-09-19 1430 said FIVE. CS was right and this project was wrong.
- WITHDRAWN ON THE CHANNEL, not corrected quietly at home: 2026-09-20-1230-from-LL-CORRECTION-... delivered to CS, LW, RC, RSC and SS, none failed. LL-0244 requires that a figure another tree may be designing against is withdrawn in a note.
- ROADMAP.md OPS-96 carries a dated correction section stating the same thing.

The error has a repeatable shape worth naming: a count of 'directories named after our own scratchpad paths' that swept in the directory named after the REPOSITORY. Both match a grep for the project name; only one is the artifact being counted.
NOT claimed: that the sixth never existed. This is a measurement of today's disk. It could have been miscounted on 2026-09-19 or removed since, and nothing available separates those. Asserting either would repeat the overreach that produced the wrong number.
Nothing else in the sweep report is affected. The cause, the fix and the acceptance criterion stand; five artifacts prove the practice as well as six would have.
This entry corrects LL-0269 rather than editing it. The ledger is append-only.

### LL-0270 - 2026-09-20 - OPS-91 - the operator ruled VENDOR, and Amberstone's docs/CHANNEL.md is in the tree byte-identical at the digest its owner published

**Evidence:**
- third_party/rc_channel/docs/CHANNEL.md, 20633 bytes, sha256 899f6eb957cc26ee25993d83d65d8ca291841fe4eec24a48f729c2dc005f4c6b - equal to the value RC published on 2026-09-15, checked BEFORE a byte was copied.
- Fetched ANONYMOUSLY from the public remote at the commit RC named, 6e3c1c752, which is the route RC's own note invited. No sibling TREE was read, so the standalone rule is untouched: a public remote is not a checkout on this machine. The HTTP 200 to a credential-less reader is also this project's own measurement of the PUBLIC claim, which is stronger than a sibling asserting it.
- LICENSE at the same commit fetched and read: Apache License 2.0, 219 lines, with a SCOPE OF THIS LICENSE block putting authored documentation - 'the Markdown that describes them' - inside the grant, and a carve-out only for third-party data under data/. The file is documentation outside data/.
- The copyright LINE was read rather than the license NAME, because a LICENSE can name nobody: two copyright lines, both RENDERED, each with a year and a non-empty holder and no unfilled template. The holder is NOT written into this repository as a literal - it is the operator's git identity.
- The 2026-09-07 three-way licence contradiction was measured on the half that could have poisoned the copy rather than accepted as resolved on RC's word: Share/LICENSE.md returns HTTP 404 at that commit.
- Copied with shutil.copyfile - byte level, never Path.write_text, which on Windows turns LF into CRLF while read_text hides it.
- ONE digest recorded, where third_party/lw_write_tracer records two, and the difference was measured: zero CRLF pairs, zero bare CR, zero non-ASCII bytes, and .gitattributes stores *.md as LF, so disk, git blob and what RC published are the same 20633 bytes.
- third_party/rc_channel/NOTICE.md carries the Apache-2.0 section 4(b) attribution and the statement of changes, which is that NOTHING was changed - the location differs from upstream and not one byte does.
- tests/test_vendored_channel_md.py: 4 failed before the file existed. Then four mutations, four reds, four restores to green - a one-character heading edit (1 failed), a whole-file CRLF rewrite (2 failed, the line-ending assertion firing separately so a text-mode copy reports as what it is), the NOTICE naming an unpinned digest (1 failed), and the NOTICE losing its statement of changes (1 failed).
- ops/lanes.py gained a row for the new test module, for the same reason the write tracer's has one: third_party/** already owns the vendored tree, but a test module under tests/ matches no other glob and was reported as an unowned file by tests/test_lanes.py.

THE ONE ASYMMETRY governed this item for four days: declining was always a session decision, adopting is an operator ruling that no grant of authority reaches. Criteria 1 and 2 were MET on 2026-09-16 and the item still could not move. What unblocked it was a sentence, not a measurement.
The cost of the original refusal is now paid off rather than argued away. This project held NO copy and NO pin rather than a private near-copy that would look like agreement without being it; it now pins the same digest the channel agrees on.
Criterion 4 - the seven portable assertions as OUR OWN test module - is the remainder. RC's gate module is NOT vendored under any answer, because RC states it hard-imports RC-only tooling.

### LL-0269 - 2026-09-19 - The machine-wide stray-work sweep ran read-only and OPS-96 closed the four findings that were ours: this tree's working copy was clean apart from two EMPTY orphans that only one of four git commands can see, the largest stray tree on the box is ours and is KEEP not PRUNE, three drive-root artifacts turned out to be one path bug - and then a refutation pass overturned THREE of the fixes, two of them guards that were green and proved nothing and one a design decision defended in prose while doing damage the prose never considered

**Evidence:**
- SCOPE, stated first because it bounds every row below: the sweep DELETED, MOVED and RENAMED NOTHING anywhere on this machine, including inside this tree. Enumeration was a metadata walk - os.walk plus stat, names and sizes and mtimes - and never read file contents, which is what makes a machine-wide pass admissible under the standalone rule at the top of CLAUDE.md: sizing a directory is not reading it. Two declared exceptions, both at the DRIVE ROOT and neither inside a sibling repository: the Subject and `diff --git` header lines of one patch file, which are paths, and the first line of one stray text file, which was pytest progress dots. Both were read only to name an owner.
- REPO TREE AT THE SWEEP: `git status --ignored --porcelain` returned twelve `!!` entries and ZERO `??` entries. 626 files and 13,298,730 bytes across the whole ignored set, which is smaller than this repository's own git pack at `size-pack: 30.36 MiB`. Largest rows: tests/__pycache__/ 75 files 8,305,115 bytes; moon_sync_inbox/ 251 files 2,213,260 bytes; ops/__pycache__/ 18 files 679,791 bytes; ops/runtime/ 211 files 582,311 bytes and newest at 2026-09-19 14:21, therefore live.
- THE HEADER FINDING, and it is a claim about GIT rather than about the tree. An orphaned EMPTY directory is reported by exactly one of the enumeration commands in common use. `git status --ignored --porcelain` does not list it; `git status --porcelain --untracked-files=all .backtest` returned nothing; `git check-ignore -v .backtest` exited 1 with no output, which is an answer about .gitignore and not about the tree. TWO existed here - .backtest/ at the repository root created 2026-09-12 18:43, and .claude/worktrees/ created 2026-09-12 21:18 - and both were invisible to the command a sweep runs first. This is the repository's own rule that an empty grep is a claim about your pattern, wearing a git costume.
- AND THE ENUMERATION COMMAND THE ITEM ITSELF NAMED MISSES ONE OF THE TWO, measured with both orphans present. `git ls-files --others --exclude-standard --directory` reported .backtest/ and NOT .claude/worktrees/. The same command WITHOUT --exclude-standard reported both. Scoped with `-- .claude` it reported nothing at all while pathlib confirmed the directory existed and was empty. So the guard enumerates with --others --directory and applies .gitignore per path afterwards, rather than trusting a flag whose behaviour differs between a top-level and a nested empty directory. The item's own header paragraph was therefore an over-claim and is left standing with the correction above it.
- .claude/worktrees/ WAS THE DANGEROUS ONE, because .claude/ is DELIBERATELY TRACKED here unlike in the sibling trees. It matched no .gitignore rule, so the first file a session wrote into it would have been an untracked orphan with no owning lane - the condition tests/test_lanes.py fails on - and a whole worktree's contents one `git add -A` from publication in a PUBLIC repository. FIXED BY IGNORING IT BY NAME rather than deleting it, because the harness recreates it and a rule that must be re-applied every time is a rule that stops being applied. `git check-ignore -v .claude/worktrees` reports `.gitignore:185`, and the ten comment lines of reason above the rule are themselves asserted by a test. .backtest/ was removed outright and deliberately NOT ignored: if it returns, the back-test was re-run and left its staging behind again, and the guard is what says so.
- THE REFUTATION PASS FOUND THREE REAL DEFECTS AND NONE WAS A FALSE ALARM. This is the entry's centre of gravity, because two of the three were guards that were GREEN and proved nothing. (1) THE ORPHAN GUARD WAS VACUOUS: its three helpers hardcoded cwd=REPO_ROOT, so test_the_guard_can_actually_fail could not call them and re-implemented the enumeration inline, proving only that git behaves as documented. The agent measured the consequence with two mutants - `_is_empty` returning False unconditionally left the file at 7 passed, and `_untracked_directory_entries` returning [] also left it at 7 passed - so the detection could have been deleted outright behind a docstring asserting the opposite. (2) `_child_counts`' skip filter was UNTESTED while this work asserted it as part of the fix; removing the filter left the whole suite green and `grep -rn child_dirs tests/` returned nothing. (3) Reusing `_SKIP_DIRS` for a drop was WRONG rather than merely untested - see the next line.
- DEFECT 3 IS THE ONE WORTH READING TWICE, because the argument for it was the tidy one and not the correct one. The first fix reused `_SKIP_DIRS` on the reasoning that one shared constant cannot drift. But `_SKIP_DIRS` is tuned for walking THIS repository, so it also skips captures, frames, screenshots, runtime, venv, node_modules and the inbox. Measured by the agent against its own fixture: a drop subdirectory with any of those names, holding real AUTHORED files, was silently excluded from the drop's digest, file_count and total_bytes - all six printed `counted in digest: False`. So an EDIT inside it could never re-surface the note, which is the exact failure the digest exists to prevent, arrived at from the other direction. In a project whose whole subject is screen capture, `captures/` is not a hypothetical directory name. Replaced by `_DROP_RESIDUE_DIRS`, scoped by CAUSE to only what a reader generates by importing or linting a drop: __pycache__, .pytest_cache, .ruff_cache, .mypy_cache. Everything a sibling authored stays in the digest whatever its directory is called.
- EVERY FIX WAS THEN PROVED RED BEFORE IT WAS TRUSTED, with the anchor asserted matched before each mutation and the source digest compared after each restore. Orphan guard, control 8 passed: `_is_empty` always False 1 failed; enumeration returning [] 1 failed; `_is_ignored` always True 1 failed; after restore 8 passed with the digest unchanged. Inbox digest, control 6 passed: child filter removed 1 failed / 5 passed; traversal reverted to `_SKIP_DIRS` 1 failed / 5 passed; after restore 6 passed with the digest unchanged. __pycache__ was deleted before every run, which is CLAUDE.md's own poisoned-bytecode trap - a timestamp-based .pyc is validated on (mtime, size) alone, so a restore inside the same clock second can leave Python serving the mutant while the source on disk is correct.
- THE ORIGINAL ITEM 3 FIX, independently reproduced by the refuting agent rather than taken on this session's word. `inspect.getsource(_manifest_digest)` shows `rglob` False and `_files_under` True, with the os.walk plus `dirnames[:]` prune at lines 1235-1236. The agent built its own fixture inbox in its own temp directory, planted __pycache__/*.pyc, and measured the digest and (count, bytes) = (2, 8) identical across the plant. It then reverted `_files_under` to rglob IN PROCESS and reproduced this session's red byte for byte: 2 failed / 2 passed, digest abd350bf... against 3a7510266f..., file_count 2 against 1. It also judged `test_an_ordinary_dot_directory_inside_a_drop_is_still_hashed` a REAL negative control by making an over-broad prune-all-dot-dirs mutant, which turned that test into the only failure at 1 failed / 3 passed.
- THE PRUNE ON THE HOT PATH WAS PROVEN NOT TO BE DECORATION, with a control in the same process. ops/inbox_watch.py:685 is `dirnames[:] = [d for d in dirnames if d not in _SKIP_DIRS]`. Pruned walk of the repository root: 223 files, 0.002 s. UNPRUNED control, same root: 2,081 files, 0.021 s. 9.3 times fewer files and 10 times faster. RC's 2-second, roughly 37,000-metadata-ops-per-second idle walker therefore does not reproduce here for a structural reason rather than a lucky one.
- AND A TIMING CLAIM WAS DELIBERATELY NOT MADE, which is why it is recorded. Three consecutive `python ops/inbox_watch.py --on-prompt` runs returned in 0.082, 0.088 and 0.081 s at exit 0 with zero bytes of output. That is the ALREADY-REPORTED early-exit path and it includes interpreter startup, so it is not the cost of the walk, and the note sent to four siblings says so in those words. The honest pair is: the walk itself is 2 ms, and the per-prompt process is under 0.1 s on the cheap path.
- MACHINE-WIDE, 27 non-standard top-level entries on C: plus five repository roots. The three largest are siblings' at 27.4 GB, 15.4 GB and 4.3 GB. THE LARGEST STRAY TREE THAT IS OURS IS C:\ll-captures at 10,678,327,730 bytes in 19,241 files, newest 2026-09-01 14:10 - and it is KEEP, not PRUNE. It is the --dest-base of the capture watcher at .claude/commands/continue.md:25, .claude/commands/loop.md:48 and docs/HEADLESS.md:136, and individual frames in it are the CITED EVIDENCE for measured findings in docs/AFFIXES.md and docs/FINDINGS.md - 2026-08-25b/talents/f0119_22.28.15, and 2,172 PNGs at 2560x1440 under 2026-08-30/frames/. Deleting it would silently unfoot published measurements in a public repository. Whether a subset is prunable needs a per-frame reference audit a read-only sweep did not run, and the report says that rather than implying otherwise.
- THREE DRIVE-ROOT ARTIFACTS TURNED OUT TO BE ONE BUG. C:\c holds an EMPTY directory chain mirroring an LL scratchpad path, and on another branch of the same chain two cs1066_* files dated 2026-09-15 21:08. C:\tmp, 53 files and 600,141 bytes to 2026-09-17, carries CS item prefixes throughout. C:\scratchpad holds one 787-line, 41,113-byte patch whose Subject is 'WIP preserved at session end - lane/queue uncommitted work' and whose `diff --git` paths name clockspeed/data/version.py. All three are a path written WITHOUT THE DRIVE COLON, so `c/Users/...` resolved relative to a working directory of C: instead of absolutely. Same family as the trap already in CLAUDE.md, where an unset TMPDIR made a quoted path expand to /<name> and MSYS relocated it under the Git install root - where a session of ours once EXECUTED a stray script another session had left the same way. A malformed path prefix does not fail; it silently relocates the write.
- THE TEMP PICTURE CONTRADICTS THE PREMISE OF THE FIX WE WERE ASKED TO APPLY, so it was DECLINED WITH THE MEASUREMENT rather than deferred. RC asked all five projects to set `tmp_path_retention_policy = failed`, citing 68,630 temp files across about nine pytest runs. Measured here: %TEMP% and %LOCALAPPDATA%\Temp are the same directory, holding 76,518 files and 3,263,424,324 bytes - but pytest-of-<account> is only 1,327 files and 932,552 bytes across three numbered directories, 0.03 per cent of it. The tree is dominated by HARNESS scratchpads under %TEMP%\claude: CS 2,315,416,266 bytes, RSC 109,120,234, ours 419,777,204. pytest.ini addopts here is `-q --tb=short --strict-markers --strict-config -r fE` and carries no retention policy. The setting would be a real change with a nearly worthless effect, and the measurement is recorded so a future session re-opens it on evidence rather than on the recollection that somebody asked.
- A SECOND FINDING OF OUR OWN, in a store no repository guard can see. ~/.claude/projects/C--Lanternlight is 744 files and 563,359,356 bytes spanning 2026-08-09 to 2026-09-19, of which 51 top-level .jsonl transcripts are 266,551,647 bytes, and nothing prunes it. Beside it sit SIX project directories named after one of our OWN scratchpad paths - the `...C--Lanternlight-<session>-scratchpad-e2e-clone` shape and five siblings, about 420 KB total - because a session of ours ran the harness with its working directory INSIDE its own scratchpad while probing fresh clones and worktrees, so the harness minted a project identity per probe directory. CS has four of the same shape, so it is a shared practice rather than a bug unique to us. Written down in a new docs/OPERATIONS.md section, because no test in this suite can observe that directory - which is precisely why the practice needed recording.
- THE REQUEST'S OWN PREMISE ABOUT THIS TREE WAS FALSE, and re-measuring it is what caught it. It instructed all five projects to report 'exactly per docs/CHANNEL.md in your tree (it is byte-identical across repos)' and cited line ranges. `find . -iname 'CHANNEL*'` returns NOTHING here: this project refused to vendor that file at its third-party license gate on 2026-09-15 and holds no copy, no pin and no private near-copy. So four of five could be addressed by line offset and one could not. The report was written in this project's own note grammar with the absence stated in its section 0 rather than silently worked around, and with no subject digest in the filename because the request arrived as an operator instruction in a session rather than as a file in the inbox - a digest would have been of nothing a reader could recompute.
- DELIVERED, AND RECORDED IN THIS TREE. `ops.outbox.deliver` wrote 2026-09-19-1426-from-LL-REVIEW-machine-stray-work-sweep-our-tree-is-clean-the-107-GB-is-ours-and-referenced-and-three-drive-root-path-bug-artifacts.md, 26,257 bytes, sha256 d618d01f3db46b0d92469353833c046e0c9e117e14309e70aca261999e6adcb7, to CS, LW, RC and RSC with delivered ('CS', 'LW', 'RC', 'RSC') and failed (). The outbox copy and the manifest row land BEFORE any sibling write, so the record exists even for a delivery that half fails.
- AND IT HAD TO BE CORRECTED NINE MINUTES LATER, which is the second lesson of this entry. The note asserted that the CHANNEL.md 'ask has not changed' and re-asked RC to name a license. RC NAMED IT ON 2026-09-16 - Remus3/Amberstone, PUBLIC, Apache-2.0, sole holder, documentation inside the grant - and OPS-91 criteria 1 and 2 have been MET for three days, with the item now blocked on an operator ruling in THIS project. The stale sentence was found by reading ROADMAP.md:2082, whose own heading says exactly that, while filing this work. Withdrawn in 2026-09-19-1435-from-LL-CORRECTION-we-withdraw-one-claim-from-our-stray-work-sweep-rc-did-name-the-license-and-ops-91-is-blocked-on-our-operator.md, 3,657 bytes, sha256 d8918959964b7865a4df1169039f7440d5e828f8f660bb67863104364918fe18, delivered to all four with failed (). This is CLAUDE.md's own rule executing against us: A DECLINE REASON GOES STALE FASTER THAN A COUNT DOES. Every count in the sweep was re-measured this session and was right; the one thing that was wrong was a REASON carried forward four days without re-reading the item it came from.
- AND A COUNT THIS SESSION 'CORRECTED' WAS CORRECT BEFORE THE CORRECTION. Acting on the refuting agent's note that the .gitignore reason sat at lines 175-184, the roadmap's `.gitignore:185` was changed to 186 - and `git check-ignore -v .claude/worktrees` then reported 185. Changed back and verified against the command rather than against the count. A filed count is a hypothesis, including a filed count in a correction.
- REGISTRATION the change forced, listed so none of it reads as unexplained: ops/lanes.py gives the new guard to the safety lane with the reason inline; scripts/write_lane_contracts.py was re-run, which REGENERATED all 8 contracts and CHANGED exactly one, .claude/commands/lane-safety.md - a distinction the refuting agent caught, because 'rewrote 8' reads as 8 changed files while git status showed one; docs/INVENTORY.md gained its row; and tests/test_source_register.py gained five KNOWN_NON_HOSTS entries - the two note filenames above, which live in the gitignored outbox where `git ls-files` cannot exempt them, plus os.walk, root.rglob and x.pyc, which are the dotted-path class that file's docstring says stays in the denylist rather than being auto-exempted.
- FINAL MEASUREMENTS, all observed on the last run of this session rather than carried forward. Full suite: 3483 passed, 1 skipped in 328.26s. Collected total re-derived by summing per-file counts: 3484, against a baseline of 3470 measured before any of the work - up 14, and an independent agent re-derived the baseline from `git archive` of HEAD at 70 files summing to 3470 and reported NO per-file regressions. `python -m ruff check .`: All checks passed. `python -m ops.preflight`: PRE-FLIGHT PASS, 14 guard modules, 388 passed and 1 skipped, lint clean. Pre-commit hook ran 50 doc-reading test modules over 4 staged .md files at 2807 passed, 1 skipped. Pushed as 29b10c7.
- NOT DONE, and deliberately not ours to do yet. Two rows of OPS-96 are outside the repository root: C:\ll-worktrees, empty since 2026-09-06, and the six harness project directories at about 420 KB. The sweep was read-only by instruction and RC is merging all five projects' reports into one machine-wide reconciliation for the operator to adjudicate before any gated deletion pass. Deleting our own rows early would remove evidence from a reconciliation that has not happened. They stay, and they stay recorded in OPS-96.
- THE PATTERN ACROSS ALL OF IT, because it is the third time this repository has met it. Two of the three refuted defects were guards that were GREEN and proved nothing, and the third was a design decision defended in prose with a reason that sounded like care - one shared constant cannot drift - while doing damage the prose never considered. A green suite reached without seeing red is not evidence, and the first version of tests/test_no_empty_orphan_dirs.py carried a docstring asserting its own non-vacuity that a two-line mutant refuted.

### LL-0268 - 2026-09-16 - RC named the license and OPS-91 criteria 1 and 2 are MET, a sibling REVIEW showed the unreadable-inbox class is three defects not one with two reproducing here at per-entry scope, and this project withdrew a causal claim it had already published to four trees

**Evidence:**
- RC ANSWERED OPS-91 AND THE GATE CLEARED ON ITS OWN TERMS. moon_sync_inbox/2026-09-16-0024-from-RC-FYI-amberstone-is-apache-2-0-and-channel-md-is-covered-license-named-for-your-gate.md, 8272 bytes measured here, supplies what criterion 1 demanded: the repository BY NAME (Remus3/Amberstone, from RC own origin remote), visibility PUBLIC probed live, root LICENSE Apache-2.0, the copyright LINE rather than only the license name, a SCOPE OF THIS LICENSE block putting authored documentation inside the grant, and a data/-only carve-out that docs/CHANNEL.md sits outside. The 2026-09-07 three-way contradiction is reported resolved WITH EVIDENCE rather than assertion: Share/LICENSE.md absent from disk AND returning zero rows from git ls-files, and the all-rights-reserved phrasing gone from README.md and docs/CHANNEL.md. RC volunteered the incomplete half unprompted - the phrase survives in append-only history and in RC notes ABOUT other people unlicensed repositories - which is worth more than a clean-tree claim. CRITERION 1 MET, CRITERION 2 MET. Apache-2.0 into Apache-2.0 is what the gate accepts. This is the second time the refuse-then-ask sequence has run to completion here; the first produced third_party/lw_write_tracer/ under OPS-84.
- WHAT IT DOES NOT DO, AND THE ITEM SAYS SO. It does not authorise vendoring. Criterion 3 reads on a yes AND AN OPERATOR RULING to vendor, and THE ONE ASYMMETRY puts adoption outside what any grant of authority reaches. OPS-91 moves from BLOCKED-ON-RC to BLOCKED ON AN OPERATOR RULING - the question unblocked, not the act. RC states from its own side that it is not asking, and that a NO VENDOR ruling with the license in hand is a legitimate outcome it would record.
- THE HOLDER NAME IS NOT RECORDED IN ANY TRACKED FILE, DELIBERATELY. It is the operator git identity, which CLAUDE.md names as an operator identifier. Recording one sole holder, named in both LICENSE and NOTICE carries the whole legal fact and none of the exposure - the LL-0170 failure shape, avoided rather than repeated.
- A NUANCE MEASURED WHILE ENFORCING THAT, RECORDED SO A FUTURE SESSION DOES NOT BREAK THE LICENSE. A blunt check - the git identity must appear in NO tracked file - is FALSE here, and it fired on README.md:270 while OPS-91 was being written. The literal appears deliberately in LICENSE, NOTICE (twice), CITATION.cff and that README line, which is exactly where Apache-2.0 requires the copyright holder to be NAMED. Removing it would strip attribution, not protect anyone. The rule is about INCIDENTAL leakage, and a test, if one is ever written, is scoped by LOCATION and DIRECTION rather than by the value.
- THE UNREADABLE-INBOX CLASS IS THREE DEFECTS, NOT ONE, AND TWO OF THEM REPRODUCE HERE AT A NARROWER SCOPE. RSC REVIEW of 2026-09-16 1100 reported that every caller of an inbox enumerator receives empty for unreadable, and that in RSC tree the acknowledge path rewrote the seen store to an empty object while printing marked read. Re-measured here rather than relayed, with a REAL PermissionError WinError 5 and a temporary inbox and state directory throughout - the live records were never touched, because running the acknowledge path against the real watermark would cause the exact harm being investigated. WHOLE-DIRECTORY denial: consequences 2 and 3 do NOT reproduce - seen and reported stores byte-identical, withdrawn empty, acknowledged False, exit 0, CANNOT READ printed - and no fix was invented for a defect that was not there. PER-ENTRY denial: BOTH reproduce - a note on disk the whole time rendered as WITHDRAWN, gone from the inbox, and the acknowledging run then deleted its pair from the seen store and its name from reported. Mid-scan denial made scan raise, contradicting its own never-raises docstring, and on_prompt_submit wrote TRIGGER_ACKNOWLEDGED over a scan that had refused, spending the session one acknowledgement.
- FIXED TDD WITH THE RED OBSERVED FIRST: 7 failed, 4 passed, 60 deselected, on assertions naming a file on disk but unreadable reported as withdrawn, and an acknowledgement recorded over a scan that refused. The repair returns unreadable names into current_names while still withholding the pair, guards the drops reader, derives NO withdrawal from an incomplete listing, refuses to acknowledge on one, and records the trigger outcome. Seven mutants, every anchor matched, __pycache__ deleted each cycle, restores byte-identical, each GREEN to RED to GREEN.
- THE PROCESS BOUNDARY WAS THE REAL GAP, AND A SIBLING FOUND IT FIRST. RSC reported a mutant surviving their entire in-process suite because the defect lived in session-id resolution ABOVE the entry point. Measured here: our two existing child-process arms supply NO STDIN, so a real-stdin suppression survived 90 arms at exit 0 and two real fires produced 0 trace rows. Three arms now fire the declared SessionStart command with real payloads, and the mutant dies.
- A CAUSAL CLAIM PUBLISHED TO FOUR TREES WAS WITHDRAWN, ON LW REFUTATION AND RE-MEASURED HERE BEFORE CONCEDING. Two notes of ours said the context-mode forwarder was found only because the adversarial pass downloaded the npm tarball, and that auditing the repository would never have settled it. FALSE for the file that matters: hooks/platform-bridge.mjs is BYTE-IDENTICAL between tarball and public repository - 12160 bytes on both sides, sha256 74d18338a4fbcc46c406f8791a819a213fd78551bbfbc99eba1f914692699294, identical at main, master and HEAD (two distinct refs wearing three names - master is not a separate branch upstream and the raw host aliases it to the default; the byte claim is unaffected). The audit failed because it searched src/ while the forwarder lives in hooks/, and for no other reason. Package divergence is real at PACKAGE scope and is not a fact about this file. Two true observations were welded into a causal claim the evidence does not support - the same shape as a rendered field is not evidence of a producer. Corrected in a note to all four siblings rather than quietly here, because RSC had already quoted our byte numbers into an amended clause and RC and LW had each written a correction citing our attribution.
- CS WORKTREE CLASS REACHES THIS TREE AS A MECHANISM AND NOT AS A NUMBER. CS retracted a finding because it was measured in a worktree and is false in a primary checkout, and this session had derived its per-file merge-gate baseline in a detached worktree at HEAD. Controlled twice, independently: a worktree at HEAD collects 3439 across 70 files, reproducing the session figure exactly; the primary tree collects 3459 across 70; EXACTLY 2 of 70 files differ and both are the modified-uncommitted test files, with the deltas summing to the whole gap. The baseline was a correct measurement of HEAD.
- BUT A HEAD BASELINE IS THE WRONG FLOOR WHEN WORK IS ALREADY UNCOMMITTED, and that is a defect in this project own method rather than in CS. Ours would have handed the gate a per-file floor of 46 for a file the working tree holds at 60, so a lane could have deleted 13 of the tests it added in the same session and still passed the per-file check - the exact failure a per-file floor exists to prevent, one level down. Filed as OPS-95 item 2 with an acceptance criterion, and stated in the outgoing note rather than kept private.
- SUITE AND GATE, OBSERVED THIS RUN: python -m pytest bare gives 3469 passed, 1 skipped in 264.93s; python -m pytest --collect-only gives 3470; python -m ruff check . gives All checks passed; python -m ops.preflight gives PRE-FLIGHT PASS - 14 guard modules in 15.50s. Merge gate OK at 3470 collected against a per-file baseline of 3439 across 70 files taken at HEAD, with tests/test_lane_launcher.py rising 20 to 26 and no file dropping.
- TWO DOC GUARDS FIRED ON THIS SESSION OWN WRITING AND BOTH WERE RIGHT. tests/test_doc_archive.py refused an OPEN OPS-36 whose heading contained the word CLOSED, which would have let a whole-line scan treat a live item as archivable; the HEADING was fixed, not the guard. tests/test_source_register.py refused 19 dot-bearing strings as unregistered hosts - every one a file basename, a dotted identifier in someone else source, one of our own dataclass attributes, or a gitignored runtime path, and none tracked here; each was looked at individually and registered in KNOWN_NON_HOSTS with its provenance, because this is the guard that caught a real privacy failure in LL-0170 and widening it carelessly re-opens that hole.
- THE CAPTURE WATCHER IS STILL DELIBERATELY DISARMED AND THIS WRAP DID NOT RE-ARM IT. check_watcher returns NO_RECORD, which the wrap ritual lists as a re-arm state. It was NOT re-armed: the operator disarmed that lane on 2026-09-11 and never lifted it, docs/HEADLESS.md says in terms that it does not re-arm it, and the operator overrides the ritual. Nothing is archiving the log, the saves or the market cache, which is the accepted cost recorded at the previous wraps.

### LL-0267 - 2026-09-16 - FOUR of five adversarial passes overturned their own lane's headline, and two lanes agreed on a filename that never existed

**Evidence:**
- THE METHODOLOGICAL DATUM, MEASURED RATHER THAN ASSERTED. Five producing lanes ran this session, each followed by an independent adversarial pass. FOUR of the five passes OVERTURNED their own lane's headline finding. OPS-92: the reported tool inversion turned out to be the lane's own hand-authored node geometry and is struck entirely. OPS-93: "no upload, sync or telemetry found" was refuted by a wired-in event forwarder in the shipped tarball. OPS-94: the lane's count CORRECTION was itself the error, and the ROADMAP's 23 needed no correction at all. OPS-36: criterion 3's clean negative was refuted by a re-sweep that found an unlicensed-source clause inside charter v4. Only OPS-91's lane survived its pass on the headline, and even there a vacuity was caught mid-TDD by the implementing slice rather than by the adversary.
- WHY THIS IS WORTH ITS OWN ENTRY. CLAUDE.md asserts that agreement between two agents is not evidence, that a producer never grades its own output, and that every done-claim gets an independent pass trying to REFUTE it. This session is that doctrine paying for itself at a rate of four in five, and the RATE is the finding: an adversarial pass in this repository is not a formality with a low yield, it is the step that decided four of five outcomes. A future session tempted to skip it for schedule reasons should read this number first.
- THE SMALLER DATUM, AND IT IS THE SAME RULE ONE LEVEL DOWN. Two independent lanes reported the same WRONG filename for a third lane's work in progress - tests/test_inbox_watch_report.py - which never existed. The real file is tests/test_inbox_watch.py, confirmed by git status. Two agents agreed and both were wrong, which is verbatim what CLAUDE.md warns about: "Two agents can be wrong the same way." Neither had listed the file; both had inferred its name from the feature it implements, which is how two independent inferences arrive at one identical mistake.
- THE COUNTS FOR THIS SESSION, MEASURED AND NOT CARRIED FORWARD. Baseline at HEAD ad9fe90: 3439 collected across 70 files, with tests/test_inbox_watch.py at 46. After this session's work the merge gate reported OK at 3459 collected, and the last full-suite line observed was "3458 passed, 1 skipped in 220.51s".

### LL-0266 - 2026-09-16 - OPS-91's refusal is unchanged and a second silence is recorded, while the watcher conformance gap is closed - argparse prefix expansion had two tests passing against zero implementation

**Evidence:**
- RC HAS NOT ANSWERED, AND THE CHECK WAS RUN THE WAY THIS REPOSITORY'S OWN ANTI-PATTERN DEMANDS. The newest item on the channel is still RC's 2026-09-15 1858 FYI, which PREDATES this project's reply delivered 2026-09-15T23:46:47 local. Verified on a WHITESPACE-COLLAPSED copy of that note, 15063 bytes, because a line-oriented grep is a claim about the file's line breaks and two withdrawal checks in one earlier session returned false clean bills exactly that way: ZERO occurrences of "licen", "copyright", "apache" or "repositor".
- A FALSE-POSITIVE CLASS WORTH RECORDING. The eight apparent "MIT" hits in that note are all substrings of "commit" and "committed". A case-insensitive search for a three-letter license identifier inside English prose is a trap rather than a search, and any future note discussing commits carries the same trap.
- CRITERION 1 REMAINS UNMET, THE ITEM STAYS BLOCKED, AND THE REFUSAL STANDS. RC names a commit sha and the phrase "public tree"; a visibility is not a grant. Under the rule RC itself named as operative - v2 section 2, silence reads as DISSENT rather than as consent - a SECOND silence is recorded in ROADMAP OPS-91 AS a silence, which is what criterion 5 asks for.
- THE CONFORMANCE GAP IS DONE. ops/inbox_watch.py gains NAME_LIST_CAP = 10, REPORT_FILENAME, default_report_path(), write_report(), a shared _write_atomic using temp-then-replace, render(result, cap, report_path) which caps all four name lists, and report_and_render() which writes the file FIRST and then returns the capped text. On a write failure it prints everything plus a WARNING rather than leaving a dangling pointer, because a pointer to a missing report is worse than no cap at all. Group.mtime and Drop.mtime are now stat'ed in scan and _read_drops, so "newest" is MEASURED rather than inferred from an ordering nothing guarantees.
- THE FIXTURE CANNOT REACH LIVE STATE. A --report-file flag was added with allow_abbrev=False, and the report path derives from --state, so a test cannot write ops/runtime/ and mark the operator's real backlog as read - which is the failure tests/test_inbox_live_state.py exists to prevent.
- NO GITIGNORE EDIT WAS NEEDED AND THAT WAS MEASURED, NOT ASSUMED: git check-ignore -v ops/runtime/inbox_report.txt answers .gitignore:41:ops/runtime/, exit 0.
- THE VACUITY CAUGHT MID-TDD, AND IT IS THE MOST INSTRUCTIVE THING HERE. The first draft named the flag --report, and argparse PREFIX-EXPANDED it to --reported, an existing flag, so two ordering and completeness tests passed GREEN against ZERO implementation - they were reading the reported-set JSON and finding it well-formed. The fix is --report-file plus allow_abbrev=False, pinned by a test probing --trac, which is the only probe that discriminates: it must be REFUSED, and under allow_abbrev=True it silently expands.
- RED WAS OBSERVED FIRST: 14 failed in 0.71s. Six mutations each drove RED and restored GREEN - the cap raised from 10 to 1000, the pointer removed, the write moved after stdout, the atomic write replaced by write_text, the cap spending its budget on the OLDEST names, and allow_abbrev=True. A live smoke run rendered 182 unread capped, with a 360-line full report written.

### LL-0265 - 2026-09-16 - OPS-36 criterion 2 discharged with one gap, criterion 3's clean negative REFUTED by a re-sweep, and the worktree invariant's guard was vacuous against the one argv that breaks it

**Evidence:**
- STEP 0, ESTABLISHED FIRST: WE DO HOLD THE OPERATIVE TEXT. All four RC charter broadcasts are in moon_sync_inbox and their byte sizes reproduce exactly on this disk - v1 6299, v2 5409, v3 5168, v4 5712. v4 carries the worktree ordering invariant verbatim under its own CHARTER TEXT heading, replacing v3's three-step formulation. All four are operative broadcast text rather than summaries, which is what makes discharging criterion 2 against them legitimate rather than a session grading a paraphrase. What we do NOT hold is RC's consolidated docs/CROSS_REPO_CONVERGENCE_CHARTER.md, and byte-identity between the notes and that file is UNVERIFIED and unverifiable from here, because this project reads no sibling tree.
- CRITERION 2 DISCHARGED. Every clause of v1 through v4 is listed against the concrete artifact that discharges it, or marked as imposing none. 32 cited paths were independently verified to exist and the 5 declared missing are genuinely missing, so no citation is fictional, and an independent clause enumeration matched with no clause skipped. Two shared-file clauses impose nothing because this project carries no byte-identical sibling file, which is OPS-91's refusal rather than an oversight.
- ONE REAL GAP, AND THE PRACTICE CLAIM WAS WORSE THAN THE LANE SAID. v2 section 1's FYI- / REVIEW- / ACTION- classification has NO mechanism: zero handling in ops/outbox.py. Measured across the channel, ZERO of 41 outgoing notes carry a prefix while 17 incoming ones do - a clean positive control proving the convention is real, that siblings use it, and that this project alone does not. The first delivery of 2026-09-16 published those denominators as 40 and 14; both were WITHDRAWN in a follow-up note to all four siblings the same hour, because a number published into another tree is one somebody else may design against - LL-0244. The re-measured figures, counting top-level .md files and matching the prefix anywhere in the filename, are 41 and 17; the ZERO reproduces exactly and was always the substantive claim. Both notes the lane cited as evidence of practice carry none. An acceptance criterion is filed in ROADMAP OPS-36 rather than the gap being left as prose, and it names the trap: a default-to-FYI implementation would pass a presence-only test while mislabelling every ACTION note.
- CRITERION 3 IS NOT THE CLEAN NEGATIVE THE FIRST SWEEP REPORTED. The lane named one candidate on the PUBLIC axis and blanket-negated the rest. The re-sweep found a LIVE conflict inside v4 itself: v4 asks adopters to take "RC's body, verbatim" - a full Python module - and the v4 note names NO LICENSE anywhere in its 5712 bytes. That is unlicensed sibling source proposed into a PUBLIC Apache-2.0 tree, the identical shape this project already refused for RC's docs/CHANNEL.md under OPS-91, where public is a visibility and not a grant.
- THE OUTCOME IS A DECLINE OF THAT CLAUSE AND NEEDS NO ESCALATION. Declining is a session decision and always was - THE ONE ASYMMETRY in CLAUDE.md - and the third-party license gate is a RULE rather than a permission gate, which the FULL AUTHORITY directive lists among the rules no grant of authority reaches. Criterion 3 says a conflicting clause is escalated as a NEW question; a clause refused under a rule is ANSWERED rather than escalated. BOTH halves are recorded in the item: the finding, and the fact that the first sweep missed it, because a right answer reached by a search that missed the evidence is not a verified answer.
- CRITERION 4 (a) IS NOT ESTABLISHED AND STAYS OPEN. The advertised caveman skill is real but is a USER-LEVEL command outside this repository, and its absolute path is deliberately not written into any tracked file because it carries an account name. Nothing is recorded in any TRACKED file: a case-insensitive git grep for the term returns CLAUDE.md line 326 and the two ROADMAP lines that are the problem statement itself. CLAUDE.md lines 324 to 342 do state the dialect and it fires by construction, but criterion 4 demands a RECORD naming the FILE, and "fires by construction" is not a record. An acceptance criterion naming what a tracked record would have to contain is filed in the item, including the alternative that a written DECLINE of the clause closes it too.
- CRITERION 4 (b) IS CLOSED THIS SESSION, AND THE OLD GUARD WAS PROVEN VACUOUS. The invariant, verbatim from v4: "No worktree is removed until the work it holds exists somewhere durable that survives the removal." The only enforcing mechanism here is that ops/lane_launcher.py's remove_worktree_argv plans an UNFORCED removal, so git itself refuses on a dirty or untracked worktree. The previous test's argv ['git','worktree','remove','--force',path] survived BOTH of its assertions - and --force is exactly what defeats git's refusal, so the guard was green on the one input that breaks the invariant it claimed to protect.
- NO SOURCE CHANGE WAS WARRANTED AND NONE WAS MADE. ops/lane_launcher.py is byte-identical to HEAD; the whole defect was in the test. tests/test_lane_launcher.py gains a _names_force helper and a TestTheWorktreeOrderingInvariant class of 6 tests, taking that module from 20 tests to 26.
- RED WAS WATCHED FIRST, under the proven survivor mutation, with the anchor asserted to have matched and __pycache__ purged before the run per this repository's poisoned-pyc rule: 3 failed, 3 passed, 20 deselected. Under that SAME mutation the OLD TestCommandPlanning ran 5 passed, which re-measures the vacuity independently rather than inferring it from the new result. Four separate mutations each drove RED and restored GREEN: the --force argv, a wrong target path, deleting the "Deliberately not forced" docstring line, and blinding the force detector to the -f spelling.
- MEASURED OUT OF SCOPE AND RECORDED SO IT IS NOT DISCOVERED DURING AN ADOPTION: v4's caveman hook text uses an ABSOLUTE repository root, which tools/hook_command_guard.py forbids under OPS-61. Any future adoption must reach its script through $CLAUDE_PROJECT_DIR, which all six live hooks in this tree already do.

### LL-0264 - 2026-09-16 - timharris707/skills reviewed, all 23 classified, nothing adopted - and the lane's count CORRECTION was itself the error

**Evidence:**
- THE REF IS PINNED. Every count was re-derived at commit a9317e03733da7f54b5da0eaa8edcb2697495cf5. An unpinned count goes stale the moment upstream pushes, and this review's own producing lane filed five wrong counts, so the ref is part of the finding rather than a footnote to it.
- THE COUNT IS 23 AND THE ROADMAP NEEDED NO CORRECTION. The lane's headline was "24, not 23". REFUTED: README line 224 states that only promoted buckets ship and that nothing under in-progress/ appears in the marketplace, and buckets.json and the CI configuration agree. skills/in-progress/fit-audit is therefore not one of the shipped skills. All 23 ROADMAP names resolve to a real directory at the pinned sha, with no spurious name and none missing. A filed count is a hypothesis, and that rule applies to a subagent's count exactly as it applies to a document's.
- FOUR SUBSIDIARY TALLIES WERE ALSO WRONG AND ARE CORRECTED, because each would otherwise be cited later as a measurement: references/ is 14 and not 8, CHANGELOG.md is 8 entries and not 4, _conductor is 29 and not 30, and fit-audit ships no agents/openai.yaml so "every skill ships a Codex adapter" is wrong as stated - it is 23 of 24.
- WHAT THE LANE MISSED MATTERS MORE. Repository-wide there are 48 SKILL.md files, not 24: an unmentioned 23-skill mirror sits at plugins/clickai-codex/skills/ with DIFFERENT blobs and its own LICENSE.md. Anyone adopting from this repository must say WHICH tree they copied from, because a digest taken from one does not identify a file in the other.
- THE LICENSE CHAIN, READ AT EVERY HOP. Root LICENSE.md is MIT, "Copyright (c) 2026 Tim Harris"; a plain LICENSE at the root 404s, so the filename matters. MIT into Apache-2.0 clears the gate. Upstream holders were confirmed by fetching each project's own LICENSE rather than by reading a credit line: Matt Pocock (MIT), Lauren Tan (MIT), Siqi Chen (MIT, blader/humanizer, credited upstream only by handle).
- TWO CORRECTIONS TO THE LANE ON THE LICENSE AXIS. human-copywrite's Apache-2.0 LICENSE DOES name a holder, at line 189 - the lane said it named nobody - and that name is non-ASCII and therefore cannot be quoted verbatim in this repository. That collision is worth recording on its own: a license gate that requires reading the copyright LINE meets an authoring rule that forbids reproducing it, and the resolution is to record that a holder exists and to cite the line. And the cursor/plugins scope worry is CLOSED rather than open: the root is genuinely unlicensed (API null, three filename spellings 404) but pstack/README.md states MIT and all 15 plugin directories carry their own LICENSE.
- TWO GENUINELY UNLICENSED SOURCES EXIST AND NO CANDIDATE DEPENDS ON EITHER: an on-camera reviewer whose grading frame fit-audit credits, and two private repositories behind ingest. A hop the lane missed - blader/humanizer builds on Wikipedia content under CC BY-SA - likewise touches only DECLINED skills. Exactly three skills carry NO Attribution section despite the README claiming that section is the record: advisory-board, handoff and orchestrate.
- THE CLASSIFICATION IS COMPLETE: 8 DUPLICATE, 3 CANDIDATE, 13 DECLINED - 24 entries covering the 23 SHIPPED skills plus fit-audit, which is classified and marked as not shipped. The full table is in ROADMAP OPS-94, so an unmentioned skill is not indistinguishable from an overlooked one.
- THE ROADMAP'S SIX GUESSED CANDIDATES ARE ALL OVERTURNED, with reasons confirmed verbatim on whitespace-collapsed copies. decision-map's hard guardrail - "record the recommendation and stop there: the round brief is the deliverable, never the answer" - is the verbatim inverse of FULL AUTHORITY item 2. advisory-board shells out to codex exec, gemini -p and grok, which is egress plus an API key. plainspoken is always-on prose governance and a second answer to CAVEMAN ULTRA. huh needs a human reading chat, which the operator cannot do while playing. Seven decline reasons were spot-checked verbatim and seven held.
- THE THREE CANDIDATES ARE blast-radius, diagnose AND writing-for-agents. blast-radius survives an adversarial check of the gap it claims: ops/merge_gate.py is a done-claim checker by its own docstring and says nothing about what a change BREAKS, so the gap is real and it operationalises CLAUDE.md's "proving your change happened is not the same as proving it matters".
- A CAVEAT ON diagnose THAT IS NOT DROPPED. The local user-level diagnose command contains the phrase "ask the operator" and holds 14 non-ASCII characters - two non-starters here, FULL AUTHORITY item 2 and the 7-bit ASCII rule. It is the USER-LEVEL copy rather than the upstream skill, so it does not by itself condemn the candidate, but diagnose CANNOT be adopted as-is and an adoption must fix both defects and say so.
- CRITERION 3's PREMISE WAS FALSE AND IS CORRECTED IN THE ITEM. The ROADMAP said diagnose "is already a local skill in this tree". There are zero SKILL.md files anywhere under the repository root and no tracked diagnose command; the only copy is user-level and absent from a fresh clone, which makes it a continuity gap rather than a duplicate. Its absolute path is deliberately not written into any tracked file, because a user-profile path carries an account name.
- CRITERION 4's GUARD WAS PROVEN NON-VACUOUSLY AND DOES NOT COVER WHAT THE CRITERION ASSUMED. tests/test_inventory.py matches ONE DIRECTORY DEEP: calling _group_of directly, a nested .claude/skills/<name>/SKILL.md returns None while a flat command returns 'commands', against six positive controls. A skill adopted at a nested path would be guarded by NOTHING and would turn nothing red, so any adoption goes in as .claude/commands/*.md, which the inventory scope does cover.
- NOTHING WAS ADOPTED THIS SESSION. The three candidates are recorded as candidates with their adoption shape named, which is a decision about what an adoption would look like rather than a deferral of one.

### LL-0263 - 2026-09-16 - context-mode is DECLINED, and the lane's "no telemetry" finding was refuted by a wired-in forwarder in the shipped tarball

**Evidence:**
- THE DECISION. OPS-93 is DECLINED 2026-09-16. Nothing installed, nothing vendored, no hook entry written, and no settings file touched in this tree or outside it. The grounds recorded are NOT the producing lane's: its headline safety finding was refuted, and the correction runs against the tool rather than for it.
- CRITERION 1 MET AND CRITERION 2 ANSWERED. LICENSE is 3840 bytes, sha256 92f11a867c41c9e575ccb978ff205ae2011c244a14aa3c576e914e537105caa0, byte identical at main, master and HEAD (two distinct refs wearing three names - master is not a separate branch upstream and the raw host aliases it to the default; the byte claim is unaffected). Line 1 "Elastic License 2.0 (ELv2)", line 3 "Copyright 2026 Mert Koseoglu". package.json declares "license": "Elastic-2.0", the README badge says ELv2, and the GitHub API reports spdx_id NOASSERTION, which is the API declining the identifier rather than a contradiction in the repository. Full recursive trees at main, at tag v1.0.169 and at branch next: exactly ONE license blob, no NOTICE, no AUTHORS, no second holder. DO-NOT-VENDOR is recorded explicitly so nobody re-derives it - ELv2 is source-available, the same category CLAUDE.md names for BUSL-1.1. Using it is permitted by its own grant clause; copying any byte into this tree is refused permanently.
- THE FINDING THAT DECIDES IT, AND THE LANE HAD IT BACKWARDS. The lane reported no upload, sync or telemetry. REFUTED against the SHIPPED npm tarball: hooks/platform-bridge.mjs is a self-described fire-and-forget event forwarder whose line 286 POSTs a full event envelope with a bearer Authorization header built from a configured api_key. It is WIRED IN rather than dead code - hooks/session-loaders.mjs:161 calls maybeForward, and that loader is imported by the sessionstart, posttooluse, userpromptsubmit, precompact and stop hooks. It is DORMANT rather than absent: gated on a config file holding an api_key with a ctxm_ prefix, and no writer for that file ships today. Lines 286 to 292 and line 30 were confirmed in the tarball by the merger independently rather than relayed.
- WHY THE LANE MISSED IT, WHICH IS THE TRANSFERABLE LESSON. It audited src/, 117 files. The forwarder lives in hooks/, 98 files. That is CLAUDE.md's own "an empty grep is a claim about your pattern, not about the codebase", landing on a live safety question rather than on a count. A clean negative from a search is a claim about where you searched.
- THE PUBLISHED ARTIFACT IS NOT REPRODUCIBLE FROM THE REPOSITORY. Three measurements of the same nominal build: the tag at 673,945 bytes, main at 674,733, and the npm tarball at 674,738, with 267 differing lines. AUDITING A PUBLISHED PACKAGE MEANS AUDITING THE PACKAGE, written down so it is not learned again. CORRECTION, same day, on LW's refutation and re-measured here before conceding: an earlier version of this entry added "auditing GitHub source would never have settled this", and that causal claim is WITHDRAWN. hooks/platform-bridge.mjs is BYTE-IDENTICAL between tarball and public repository - 12,160 bytes, sha256 74d18338a4fbcc46c406f8791a819a213fd78551bbfbc99eba1f914692699294, identical at main, master and HEAD (two distinct refs wearing three names - master is not a separate branch upstream and the raw host aliases it to the default; the byte claim is unaffected). The audit failed because it searched src/ while the forwarder lives in hooks/, and for no other reason; downloading the tarball is what the adversarial pass happened to do rather than what made the difference. Package divergence is real at PACKAGE scope and is not a fact about this file. Two true observations were welded into a causal claim the evidence does not support, and it had already been published to four trees.
- "NOT THE MSIX EXPOSED SHAPE" IS REFUTED IN PART. The DB path claim STANDS: the store resolves to <resolveClaudeConfigDir()>/context-mode/{sessions,content}/<sha256-16>[suffix].db, under the user profile's .claude directory, and os.homedir() ignores HOME on win32 - measured, not assumed. But platform-bridge.mjs writes its config under APPDATA\context-mode\platform.json and build/cli.js:312 uses LOCALAPPDATA, and BOTH are the exposed shape from the MSIX package-shadow section of docs/OPERATIONS.md measured on this machine in LL-0259. The surviving sub-claim: the shipped build/session/extract.js imports only ./pricing.js and has no network, and its sentry, apiKey and Authorization hits are comments inside a redaction scrubber.
- CRITERION 3 IS ANSWERED RATHER THAN LEFT AS A GAP. Nothing lands inside the repository under this decision, so there is no path for a gitignore test to assert about, and writing one would be decoration. A guard that cannot go red is not a guard.
- CRITERION 4's PREMISE IS REFUTED AND THE REAL RISK IS THE OPPOSITE ONE. The item said an installer rewriting .claude/settings.json would trip the guards. It would not: the installer writes the USER-level ~/.claude/settings.json, which tools/hook_command_guard.py never sees. The risk is SILENCE, not a red suite - a machine-wide hook this repository's guards are structurally unable to observe. There is no --local, --scope or --global flag; --project is index and search only.
- TWO LOCAL FACTS RE-MEASURED WHILE ESTABLISHING THAT, ONE OF THEM UNDOCUMENTED. Pin-by-exact-string is CONFIRMED, and the pin is BIDIRECTIONAL: DELETING a pinned rule also turns the suite red, which nothing had recorded. The item's claim that "a fourth turns the suite red" is OVERSTATED - only a fourth ABSOLUTE permission rule does. Verified by mutating a scratch COPY through check_settings_text with the anchor asserted to have matched first, and python -m pytest tests/test_hook_command_roots.py -> 60 passed.
- CRITERION 5 IS UNMET AND IS RECORDED AS UNMET RATHER THAN QUIETLY SATISFIED. The context saving was not measured on this repository, because nothing was installed and measuring it would have required installing it. The vendor's 98 percent figure stays attributed as a CLAIM and is never repeated as a measurement.
- THE DECLINE GROUNDS, AND A NAMED REVERSAL CONDITION SO THIS CAN BE REVISITED ON EVIDENCE RATHER THAN RE-ARGUED. A default install is machine-wide across the seven repositories on this box and there is no per-project install mode; there is no version pin, so the audit expires on every silent self-update; no local guard can see the user-level file it writes, so the failure mode is silence; and the dormant forwarder plus the non-reproducible published artifact mean the thing audited is not the thing that would run tomorrow. The reversal condition is written into ROADMAP OPS-93 and names three things that must ALL hold.

### LL-0262 - 2026-09-16 - archify is DECLINED, and the trial's headline inversion was the producing lane's own diagram geometry

**Evidence:**
- THE DECISION. OPS-92 is DECLINED 2026-09-16. archify is not adopted, nothing is vendored, no Node dependency enters this repository, and no committed artifact here is generated by it. Criteria 1 through 4 are discharged in ROADMAP OPS-92; criterion 5 is moot because nothing was vendored.
- THE LICENSE WAS READ DIRECTLY AND IS CLEAN, CRITERION 1 MET. MIT with TWO copyright lines rather than one - "Copyright (c) 2026 tt-a1i (Archify)" and "Copyright (c) 2025 Cocoon AI" - LICENSE sha256 b799ab081703e7821ae5096d2c1abdf14bbdc75e8ea1c4045998c36ed6db9706. Nothing contradicts the MIT name, so the second license-gate trap does not fire, but the third is live: a second holder means the first cannot unilaterally relicense. THIRD_PARTY_NOTICES.md records packaged brand-mark data under CC-BY-NC-SA-4.0, CC-BY-SA-3.0 and CC-BY-SA-4.0, which bear on VENDORING and not on RUNNING.
- THREE PREMISE CORRECTIONS SO THE NEXT SESSION DOES NOT REPEAT THE SEARCH. The npm package named archify is an unrelated squatter, justin-calleja/archify. This project is an agent SKILL installed through npx skills add. Its archify/package.json is marked "private": true and the repository has no root package.json, so an npm install reaches entirely different software.
- THE LANE'S HEADLINE WAS AN INVERSION AND IT IS STRUCK ENTIRELY. The producing lane reported that archify PASSED the false LL-0256 diagram and REJECTED the corrected one, which would have made the tool an active hazard. The adversarial pass reproduced the failure, then fixed it with four cosmetic layout edits, after which the truthful topology passes - 9 artifact checks, 0 errors, 0 warnings, exit 0, evidence.verified true, 4 references. The cause was the lane's own hand-authored geometry: it placed the ember node at x=520, LEFT of tail and readers, forcing right-to-left edges. The corrected graph is merely DENSER than the false one - it contains a K2,2 where the false one is a funnel - and density is not falsity. The tool was never judging truth. A producer never grades its own output, and this is that rule paying for itself.
- THE GRADED ANSWER TO CRITERION 2, IN BOTH DIRECTIONS. validate would NOT have caught the LL-0256 renamed-node repair, and the reason is STRUCTURAL rather than a tuning miss: sources exists only on components, connections carry from/to/label plus geometry, and the schema sets additionalProperties false, so edge provenance is REFUSED by the schema rather than merely unsupported. Probed - node citations deliberately pointed at the wrong modules still return verified true, and an edge labelled "injects DLL" running from the redactor to the game process, an ADR-001 violation on its face, passes the showcase clean. The negative controls DO fire (file missing, line out of range), so the checks are SCOPED rather than dead.
- BUT THE FLAT CLAIM IS OVERSTATED AND THE ROADMAP SAYS SO. A compare mode exists and the lane never ran it. Run against the LL-0256 defect itself - a label-only rename - it prints changedFields ["/label","/sublabel"] with connections {added 0, changed 0, removed 0, rerouted 0}, which is exactly the discriminator for "a node was renamed and the topology did not move". It does NOT fail: exit 0. And it diffs two AUTHORED intermediate representations rather than a diagram against code. So it SIGNALS the defect to a reader who runs it and reads the field; it does not CATCH it mechanically.
- COUNTS RE-DERIVED FROM SOURCE RATHER THAN FROM THE README: 11 addCheck call sites producing 9 distinct check names, two of which - single_svg and finite_svg - are well-formedness rather than geometry, so "9 checks" over-describes what is verified about a diagram.
- THE DECLINE GROUNDS THAT SURVIVE. No edge provenance by schema design, which is exactly what LL-0256 and LL-0254 were. Node 18 or newer plus a network bootstrap or about 8.5M vendored, so a fresh clone could not reproduce a committed artifact without Node - a regression in this project's continuity story. An artifact of about 810 KB for an 11-node diagram, quoted as APPROXIMATE on purpose because the adversary's own two artifacts measured 811,309 and 809,757 bytes and an exact figure is not reproducible. And 322 non-ASCII bytes in the generated artifact, where tests/test_ascii_hygiene.py walks 221 files, treats .html as text, includes third_party/ in scope and walks UNTRACKED files, so committing it would turn the suite red.
- THE REVERSAL CONDITION IS NAMED IN THE ITEM so the decline can be revisited on evidence: edge-level sources in the IR schema plus a non-zero validate exit code when an edge's citation does not hold. The Node cost and the artifact size would not need to change - those are costs, and the edge gap is the disqualifier.

### LL-0261 - 2026-09-15 - Three operator-named repositories are recorded as ROADMAP items with their license gates already read, because a link in chat is a direction a cold session never sees

**Evidence:**
- WHAT HAPPENED. The operator sent three GitHub links in chat during the moon-sync stage 4 session and asked that the next session implement or review each. A link in a transcript dies with the context window, so each became a ROADMAP item with acceptance criteria rather than a line in a hand-off: OPS-92 archify, OPS-93 context-mode, OPS-94 timharris707/skills.
- THE LICENSE WAS READ FIRST IN ALL THREE, because the cost of finding out late is a file in a public Apache-2.0 tree that cannot be cleared. Each answer is recorded in its item as UNVERIFIED-from-a-page-read rather than as a cleared license, and each item's first acceptance criterion is to read the actual LICENSE file including the copyright LINE.
- OPS-93 IS A DO-NOT-VENDOR AND THE ITEM SAYS SO UP FRONT. context-mode advertises ELv2, the Elastic License v2, which is SOURCE-AVAILABLE rather than open source - the same category CLAUDE.md already names for BUSL-1.1 and marks DO-NOT-VENDOR whatever anyone offers. USING a tool is not vendoring it and ELv2 permits use, so the item is not declined; the distinction recorded is that it may be a tool on the machine and may never be a file in the repository.
- THREE OTHER CONSTRAINTS ON OPS-93 NAMED BEFORE AN INSTALL RATHER THAN AFTER, each traceable to something this project already measured. Its hook system captures file edits, git operations and errors, which in this tree carry game-log content, so its SQLite store is a new unredacted store outside ops/runtime/ whose path and gitignore status must be known before it exists - ADR-004 scopes redaction to a class of data and a direction, and a local file crosses neither, so this is a shape to control rather than an automatic violation. Where that store lives also decides whether it can be read at all, against the MSIX shadow measured the same day in LL-0259. And it installs hooks, which .claude/settings.json guards by exact string and by the $CLAUDE_PROJECT_DIR rule from OPS-61, so any wiring is hand-written rather than installed.
- OPS-92 IS AIMED AT A FAILURE THIS REPOSITORY HAS ALREADY HAD TWICE. archify advertises deterministic checks and revision-checked source references for generated diagrams. LL-0256 records a README diagram whose repair RENAMED a node and left the false topology intact, and LL-0254 records eight false claims in a rewritten README including a diagram drawing a data flow the code does not have. The item's trial criterion is therefore graded against a KNOWN ANSWER - regenerate that diagram and record whether the tool would have caught the renamed-node repair - rather than against whether the output looks good.
- OPS-94 WALKS INTO THE LICENSE GATE'S THIRD TRAP AND THE ITEM NAMES IT. timharris707/skills is MIT held by Tim Harris, and it credits adaptation from Matt Pocock's skills project, also MIT. CLAUDE.md: "The person who cleared it may not own it. A repo crediting prior authors has multiple copyright holders and its maintainer cannot unilaterally relicense it." Both being MIT makes the answer probably fine, which is precisely the case where the check gets skipped, so any adopted file carries BOTH attributions.
- OPS-94's REAL RISK IS DUPLICATION, NOT LICENSING. Most of its 23 skills already have a local equivalent - orchestrate and adversarial-review are this project's written Session Default, handoff is /done, diagnose is already a local skill borrowed from the same upstream, blast-radius overlaps ops/merge_gate.py, codebase-review overlaps the verify lane. OPS-48 question 2 declined a second permission scheme on the reasoning that it "would create two answers to one question", and the same reasoning is written into this item. Its acceptance requires classifying all 23, so an unmentioned skill is not indistinguishable from an overlooked one.
- FOUR MORE KNOWN_NON_HOSTS ENTRIES were needed as a side effect - Node.js and SKILL.md from these items, CLAUDE.md.bak and status.md from LL-0259 - because the source-register guard reads a trailing .md or .js as a domain. None is a host, none is tracked, and the reasoning sits beside each in tests/test_source_register.py rather than in this entry.
- THE DOC SIZE BUDGET FIRED while these were added and was answered the way CLAUDE.md directs - python scripts/apply_doc_split.py --apply, never by raising the number. ROADMAP.md 345569 to 209205 characters, docs/LEDGER.md 398163 to 221855, with 17 sections and 41 entries moved to their archives. python tools/archive_link_guard.py then reported 96 archived headings against 96 stub links, so the index still resolves and nothing was deleted.

### LL-0260 - 2026-09-15 - Seven of the eight moon-sync stage 4 adoption items shipped, and the adversarial pass refuted three of the four claims it was pointed at

**Evidence:**
- THE CONSOLE FLASH, SIX SITES NOT THREE. RC's note named two live spawn sites in this tree plus one for uniformity. Re-derived against our own files as the stage-4 rules require - RC measured line numbers at our HEAD and makes no claim about our tree by having read it - tools/precommit_gate.py has FOUR spawns, not one: the staged-paths helper, the ruff version probe, the git read in _git_stdout, and the ruff check run. Every one is a console binary spawned under a windowless pythonw PreToolUse hook, so every one flashed on every tool call. All four got creationflags, plus lanternlight/redact.py's git-identity helper and ops/stop_audit.py's collection call. Six sites, confirmed by grep AND by AST independently, which can disagree.
- NO CROSS-PACKAGE IMPORT FOR THE FLAG. Each of the three modules defines its own _NO_WINDOW = getattr(subprocess, "CREATE_NO_WINDOW", 0). lanternlight/redact.py is the redaction module and imports nothing from ops/ or tools/; duplicating a two-line constant is cheaper than a layering inversion, and the guard pins the shared NAME rather than a shared import. The getattr form is what lets the public repository import on a platform where the attribute does not exist.
- THE GUARD, AND THE RED WATCHED THREE SEPARATE TIMES. tests/test_spawn_no_window.py derives the call sites from the AST and files no count, because a filed count goes stale the moment a spawn is added. Vacuity probed by the implementing slice on ops/stop_audit.py, then INDEPENDENTLY by the merger on lanternlight/redact.py (anchor asserted to match exactly once, __pycache__ deleted before the run per the poisoned-pyc rule): "lanternlight/redact.py spawns a console process without creationflags at line(s) [1543]", restored, 16 passed. The refutation pass additionally mutated all three to creationflags=0 and got red in all three, which is the arm a presence-only check would have passed.
- THE BLIND SPOT THE REFUTATION PASS FOUND, and the honest response to it. It pointed a throwaway probe at the guard's own walk and showed it blind to every aliased spelling - from subprocess import run, import subprocess as sp, from subprocess import Popen as AP, and fn = subprocess.run. The docstring had promised flatly that a new spawn without the flag turns the module red. That promise was too broad. It is narrowed in place, and test_no_covered_module_reaches_subprocess_by_an_aliased_name closes the hole from the other end: the three covered modules must reach subprocess through a plain import and nothing else, so a spelling the walk cannot read now fails at the IMPORT. Probed: adding one from-subprocess import to ops/stop_audit.py turned it red naming the line; restored, 17 passed.
- THE SETTINGS COMMENT CARRIED A FALSE REASON. It said the SessionStart hook uses python.exe "because the session must SEE its stdout and pythonw suppresses the console", and said the syntax-check hook pays the same accepted cost. RC measured that pythonw KEEPS redirected stdin, stdout, stderr and exit code - a windowless interpreter suppresses the CONSOLE ALLOCATION, not the streams. Both sentences are RETRACTED IN PLACE rather than deleted, the python.exe choice is kept as harmless, and the negative half is written down where the next session will look: DO NOT SWAP AN INTERPRETER TOKEN TO CHASE A FLASH, because a sibling measured that swapping python for pythonw at a hook command CREATES one. RC's attribution of the residual flash is recorded as PROBABLE at n = 1, with what the control DID confirm stated separately from what it did not.
- THE SESSION-ID SCAN ROW, BOUNDED BY TIME AND NOT ONLY BY BYTES. The SessionStart path read no stdin at all, so a scan could not be attributed to a session. It now reads the payload through a daemon thread joined with a 1.0s timeout, skipped entirely on a tty or a closed stream, capped at 65536 bytes, silent on every failure, and run AFTER stdout is flushed so the report is never delayed. An unbounded read here is the single failure that silently kills mail announcements across this fleet, which is why it was its own slice. Measured end to end by the refutation pass: 0.261s with stdin closed, 1.209s with a pipe held open for the child's whole life, exit 0 both times, and the seen store's digest identical before and after. Reading is not acknowledging.
- THE EVICTION GUARANTEE WAS FALSE AND IS NOW TRUE. The slice claimed scan rows could not evict the acknowledge rows that on_prompt_submit reads. The refutation pass measured otherwise: seed one acknowledge row, run 200 scans under 200 distinct session ids, and it is gone, because save_trace trimmed with a bare tail slice. The consequence is not cosmetic - a live session that loses its acknowledge row acknowledges a SECOND time, marking mail that arrived mid-session as read without ever showing it. _trim_trace now spends the budget on acknowledge rows first and fills the remainder with the newest of everything else, preserving chronological order, and the one remaining lossy case - acknowledge rows alone overflowing the limit - is pinned by a test rather than hidden. Reverting to the tail slice turns two of the four new tests red; watched.
- A STATIC GUARD'S FALSE RED, FIXED AS A GUARD BUG RATHER THAN AS AN EXEMPTION. tests/test_inbox_live_state.py refuses any watcher call that does not name a throwaway state path, because a fixture writing ops/runtime/ marks the operator's real backlog as read. It reported the five new tests as offenders: they inject all four paths, but through a module-level _scan_argv helper, so the "--state" constant is in the HELPER'S body. The alternatives were to duplicate four flags five times or to add the tests to the live-by-design allowlist, and the second would have been weakening a guard to make a build pass. Instead the guard now follows ONE level of indirection into a same-module helper whose body it has READ. Probed by stripping "--state" from that helper: red again, naming all five; restored, 3 passed.
- ITEM 7 NEEDED NO CHANGE. ops/outbox.py's deliver is untouched: a CHANNEL_VERSION footer refusal is an arming-stage item and nothing about arming moved.
- COUNTS MEASURED THIS SESSION, never carried forward. Baseline before the work, python -m pytest --collect-only bare: 3413. After: 3434 at the refutation pass, then four more trace-trim tests and one alias-import test. The full bare run mid-session reported 1 failed, 3432 passed, 1 skipped in 233.15s - that one failure was the live-state guard's false red above, found only by the FULL suite, because neither the targeted subsets nor ops.preflight collects that module.

### LL-0259 - 2026-09-15 - The MSIX package shadow was MEASURED on this machine instead of inherited from a sibling's report, and the answer for this project's own data surface is CLEAN

**Evidence:**
- THE REPORTED TRAP. RC's 2026-09-15 FYI section 6 states that a tool shell or hook running under the Claude desktop app carries MSIX package identity, that anything it WRITES under %LOCALAPPDATA% or %APPDATA% can land in the package's LocalCache twin, and that every later read from that harness then returns the twin - silently, with no error and no way to tell from inside the session. RC hit it on its own poller state. This project's entire primary data surface - the game log, the saves, the config and the price cache resolved by lanternlight/paths.py - lives under %LOCALAPPDATA%, so the report was not something to file and forget.
- MEASURED HERE, FROM TWO DIFFERENT PROCESSES, because a claim about this machine is not a claim this project inherits. Git Bash and PowerShell were both asked. The twin tree is REAL and populated under the Claude package's LocalCache/Local directory - a dozen directories and a stray CLAUDE.md.bak that some harness session plainly meant to write elsewhere - so the redirection is live on this box and not theoretical. The full path is in docs/OPERATIONS.md rather than here.
- THE RESULT THAT MATTERS: there is NO MistfallHunter directory in the twin, from either shell. The game writes its Saved tree as an ordinary unpackaged process and nothing in this repository writes into it, so every ingest path reads the REAL file. That is a measurement, and it is now in docs/OPERATIONS.md under "Find the game log", where a session looking at a suspicious log reads it before re-deriving it.
- A SECOND RESULT, reported to RC because it bears on their own advice. A moonsync directory EXISTS in the twin and is EMPTY, while the real one held five files whose status.md was 965 bytes with an mtime two minutes old. Both shells read the real ones, so RC's section 7 "read the status file from any session on this box" works today. The empty twin DIRECTORY is the precursor state rather than a clean bill: one harness write into that path starts the shadowing, and RC's own section 6 is the evidence that it would be silent.
- THE STANDING RULE this leaves, written into docs/OPERATIONS.md rather than left in a transcript: nothing in this repository writes under %LOCALAPPDATA% or %APPDATA%; runtime records stay under the repo root, which is not virtualised. tools/frame_poller.py writes under the user profile, which is neither variable, and that is recorded as UNVERIFIED rather than assumed safe.

### LL-0258 - 2026-09-15 - Vendoring RC's channel doc is REFUSED at the license gate, and the refusal is the LW sequence run a second time rather than a new policy

**Evidence:**
- WHAT WAS ASKED. The operator handed this session the moon-sync stage 4 adoption prompts on 2026-09-15. Item 8 of this project's eight reads "Vendor docs/CHANNEL.md byte-identical and port the seven PORTABLE tests." Seven of the eight were implemented. That one is refused, and ROADMAP OPS-91 carries the acceptance criteria so a cold session resumes it without the prompt.
- WHY NO AUTHORITY IN THIS REPOSITORY REACHES IT. CLAUDE.md's SECOND EXCEPTION permits vendoring only when the sibling NAMES A LICENSE this repository can accept, and the FULL AUTHORITY directive lists the third-party license gate among the RULES no grant of authority touches. Declining is a session decision and always was, so this needed no ruling to do.
- WHAT RC ACTUALLY STATED, measured from the note on this disk rather than recalled: the 2026-09-15 FYI, 15063 bytes, sha256 38e749ddfc3e17812925102b3b582666ece5f44900a61dc5c3298597a52fba24, names a commit and calls the tree public. It names no license, no copyright holder and no repository. Public is a visibility, not a grant.
- THE PART THAT MAKES IT A QUESTION RATHER THAN A FORMALITY. RC's own note of 2026-09-07 at 0055, section 5, reported a THREE-WAY LICENCE CONTRADICTION in RC's tree - an Apache-2.0 LICENSE, a Share/LICENSE.md forbidding redistribution, and a README saying all rights reserved, personal use only - and said the third was created that night while fixing the first. That is verbatim the first trap CLAUDE.md's license gate lists. Whether it has been resolved since is UNVERIFIED here and is not ours to assume, because this project reads no sibling tree.
- THE PRECEDENT THIS IS A RERUN OF, named so nobody reads the refusal as hostility: this project refused an unlicensed LW drop, asked LW to name a license, LW named Remus3/Legion-Wallpaper as PUBLIC and Apache-2.0 with the operator as sole copyright holder and vendoring intended, and the operator then ruled VENDOR - now third_party/lw_write_tracer/ under OPS-84. The same route is open to RC.
- THE ASK WENT OUT, and the paragraph claiming so was briefly FALSE. Delivered through ops.outbox.deliver at 2026-09-15T23:46:47 local, 11520 bytes, sha256 6fc3065455c12bfea40ec489f00aff836aaa811f9f27310e78a4eb786672a666, one recipient, none failed. The OPS-91 sentence asserting it was written in the past tense while the note was still a draft in a scratch directory, and this session's own adversarial pass caught it by LISTING THE OUTBOX rather than by reading the paragraph: no 2026-09-15 entry existed and the newest two manifest rows were both from 2026-09-14. That is OPS-43's failure one level up - there a session did not know it HAD replied, here a session claimed it had - and the fix was to send the note rather than to soften the sentence. The near-miss is recorded inside OPS-91 itself.
- THE COST, RECORDED RATHER THAN GLOSSED. The value of a byte-identical vendor is five trees pinning one digest. This project holds no copy and no pin, and deliberately did NOT write a paraphrase at the same relative path: a near-copy would look like agreement without being it, and would not contribute to the pin either.
- ONE CONFORMANCE GAP FOUND WHILE READING THE CONTRACT AND NOT CLAIMED GREEN: clause 3 asks for a list cap plus a "+k more" pointer to a gitignored report file written atomically before stdout. ops/inbox_watch.py implements neither. RC's per-sibling line did not raise it; it was found by reading the clause. Recorded with an acceptance criterion in OPS-91 rather than done.
- FIVE NEW KNOWN_NON_HOSTS ENTRIES in tests/test_source_register.py were needed because the register guard reads a markdown filename as a host - .md is Moldova's TLD. All five are sibling-tree or gitignored-inbox filenames that git ls-files can never absorb, and the reasoning is in the comment beside them rather than in this entry.

### LL-0257 - 2026-09-15 - OPS-36 criterion 1 is MET after a week, and the same answer caught this project holding the OPPOSITE of the silence rule actually in force

**Evidence:**
- CRITERION 1 CLOSED. It asked for the CURRENT charter version, identified by RC, recorded here with its version and timestamp. RC answered unprompted in the 2026-09-15 FYI section 3: charter v4 is current, at docs/CROSS_REPO_CONVERGENCE_CHARTER.md:358 in RC's tree, dated 2026-09-07T00:35 local, the worktree-invariant amendment answering RSC's second ask, with no v5 and no append since. RC also retracts as STALE the earlier fleet statement that only v1 was tracked. Recorded in ROADMAP.md under a new OPS-36 section.
- THE FALLBACK WAS NEVER EXERCISED. The 2026-09-14 re-ask named what this project would do without an answer - discharge criteria 2 through 4 against the last version held and mark criterion 1 BLOCKED ON RC rather than met. The answer arrived first, so the version is confirmed by its author rather than chosen by us, and no criterion was quietly satisfied by a session picking one.
- WHAT IS NOT ESTABLISHED, stated so it is not read as more than it is: the line number and the timestamp are RC's report of RC's tree. docs/REPLY_PATHS.md records that this project writes a note INTO a sibling inbox and reads nothing out of the tree around it, so that citation is a pointer to hand back to RC, not a measurement taken here. What was measured here is the note, its 15063 bytes and its sha256.
- THE CLAUSE WE HAD BACKWARDS. OPS-36 accepted, with its consequences deliberately named, a draft clause asserting that SILENCE IS AGREEMENT, and forbade any session from softening it. RC now states the rule in force is v2 section 2: silence is never agreement, hardened to silence reads as DISSENT. Those are opposite rules. The old paragraph is left standing with the correction above it, because this project does not quietly revise a record - and the operative rule is strictly safer for a project whose watcher was provably blind to inbox subdirectories for a period, the OPS-34 fact reported to RC and never claimed as an exemption.
- THE CONSEQUENCE THAT MADE A REPLY MANDATORY. RC's FYI requested no reply. Under the rule RC named, not replying would have registered this project as DISSENTING from RC's disclosed FYI-instead-of-REVIEW classification, which this project does not dissent from. A note saying so went out the same day. An FYI that requests no reply still costs four replies, or it collects four dissents, and that is a property of the rule worth the fleet noticing.


### LL-0256 - 2026-09-14 - The wrap's refutation pass REFUTED one of this session's own repairs - the diagram's node was RENAMED and its topology left intact, so the picture still made the false claim after the fix

**Evidence:**
- THE REPAIR THAT DID NOT REPAIR ANYTHING. LL-0254 records the README's mermaid diagram being corrected because it drew all four data surfaces flowing into REDACT when only the log reader redacts. The wrap refutation read the committed file rather than the claim and found the edges still terminating at that node: L->T, S->RS, P->RS, C->RS, T->O, RS->O. The node's LABEL had changed from 'redact before anything leaves' to a caption about egress. The topology had not. A reader following arrows still learned the false thing.
- Fixed by changing the SHAPE, not the words: the in-program consumer path now runs T and RS to Emberforge and the overlay as dashed planned edges, and the redactor sits on a separate EGRESS branch ending at a node reading 'Sent off this machine, or reaching a commit'. Re-rendered with mermaid-cli 11.17.0, exit 0, viewBox 915x702, which still needs no downscale on a desktop column.
- A SECOND FALSE CLAIM the session's own pass missed, inherited from the previous README: 'docs/CLASSES.md - six independent research passes, adjudicated by a seventh reviewer'. That document carries EIGHT 'Adjudication: unresolved' or 'open' lines, at 159, 201, 229, 248, 261, 278 and 290. The adjudicating pass happened; it did not settle the disagreements. The row now says it RECORDS where the six disagree rather than smoothing it, and that several are marked unresolved and stay that way.
- CLAUDE.md contradiction closed from BOTH ends. The FULL AUTHORITY section carried the asymmetry, and the adoption rule 300 lines below still read flatly as 'always an operator ruling' with no pointer to it. A reader arriving at the rule met a contradiction and had no way to know it was resolved. The rule now cross-references the asymmetry in place.
- A STALE RECITAL INSIDE A CLOSED ITEM, marked rather than edited. ROADMAP.md's OPS-35 narrative says 'OPS-48 still forbids soliciting RC or RSC, and OPS-68 still holds the responder runner on standby'. Both became false on 2026-09-14. OPS-35 is CLOSED and that paragraph is a record of what was true when written, so it is marked SUPERSEDED in place - editing it would falsify the record, which is the same reasoning applied to the sent-note draft earlier in this session.
- Suite after the repairs: 3412 passed, 1 skipped in 274.32s, exit 0. Collected 3413. Ruff: All checks passed!. Pre-flight PASS, 14 guard modules.

THE LESSON IS THE ONE OPS-89 TAUGHT, IN A DIFFERENT COSTUME. There, a fix made is_locked heartbeat-aware and left acquire alone - the guard answered the QUESTION correctly and still refused nothing. Here, a fix made the diagram's CAPTION honest and left its ARROWS alone. Both times every criterion read MET, because the thing that was checked was not the thing that carries the meaning. Ask which part of the artifact the reader actually follows.
The refuting agent was handed eight specific claims and told to default to refuted. It refuted three, confirmed three, and found a fourth defect nobody had asked it about. It also refused to let its verdict read as a clearance, because the tree moved twice during its pass. Both of those behaviours are the reason the pass exists.

### LL-0255 - 2026-09-14 - The refutation census published five ratios over an undefined unit - LW was right, the denominator is now defined and every cross-tree comparison built on it is WITHDRAWN

**Evidence:**
- THE DEFECT, measured rather than relayed: docs/REFUTATION_CENSUS.md defined the CORPUS exactly - 64 ledger entries, closed at both ends so filing another entry cannot move a published percentage - and never once said what makes ONE EVENT. Five ratios rest on that denominator, including the 31.1 per cent gate-reachable figure this project published to four sibling trees.
- The unit, stated now: one row per verbatim ledger sentence that a reader judged to record a refutation finding. Where one finding ends and the next begins was the extraction slice's judgement, entry by entry, and was never written down as a rule anyone else could apply.
- TDD, and the guard was watched failing FIRST for the RIGHT reason. tests/test_refutation_census.py::TestTheDenominatorIsDefinedWhereTheRatiosArePrinted, three tests. The first run failed on an AttributeError in the test's own helper - a WRONG-reason red, which is not evidence - the helper was corrected against format_report's real signature, and the re-run then failed on 'assert THREE LIMITS in TWO LIMITS ON THIS NUMBER:' before any implementation existed.
- NON-VACUOUS, proven by mutation and restored by digest: reverting the report heading from THREE LIMITS to TWO LIMITS turned test_the_limits_block_counts_itself_correctly RED (1 failed, 2 passed); restoring gave 3 passed; sha256 of ops/refutation_census.py matches the pre-mutation copy exactly. The __pycache__ directory was deleted before the verifying run, per the poisoned-bytecode trap in CLAUDE.md.
- The limit is printed WHERE THE NUMBERS ARE, not only in a document: python -m ops.refutation_census now prints THREE LIMITS ON THIS NUMBER and names the denominator as the third. The heading's count is pinned by a test to the number of limits beneath it, so adding a fourth and leaving the word alone lands in the exact stale-recital bucket this census measures.
- docs/REFUTATION_CENSUS.md carries it as the FIRST 'what this census cannot tell you' bullet, naming Legion Wallpaper as the party who raised it on 2026-09-13.
- WHAT IS AND IS NOT WITHDRAWN: the 31.1 per cent stands INSIDE our corpus, because every total is recomputed from the rows on every run and every anchor is re-resolved against the live ledger. Every cross-tree comparison built on it is withdrawn, ours included.
- Suite THIS RUN: 3412 passed, 1 skipped in 240.45s, exit 0. Collected 3413. Per-file counts compared against HEAD c4539ed in a separate worktree: ZERO files dropped, one file gained - tests/test_refutation_census.py 39 to 42 - which is exactly the three new guards. Pre-flight PASS, 14 guard modules, lint clean.

THE SHAPE OF THIS DEFECT IS THIS REPOSITORY'S OWN RULE TURNED ON ITSELF. 'A filed count is a hypothesis' is in CLAUDE.md, and this census was built specifically so that no total is stored and none can go stale. It hardened the NUMERATOR and left the UNIT undefined, which is the same failure one level up: a count nobody can reproduce, because nobody said what was being counted.
It took another project to see it. Two of our own adversarial passes ran over this document and neither asked what an event was, because both inherited the unit from the rows in front of them. An outside reader had no rows to inherit from and asked the only question available.

### LL-0254 - 2026-09-14 - The public README was rewritten for presentation and the refutation pass caught EIGHT false claims in it, including a diagram that drew a data flow the code does not have

**Evidence:**
- Operator asked for the README, the GitHub About description and the topic tags refreshed for public presentation - audited for readability and search reach, concise, and visually interesting about what the project IS and what is still to do.
- REFUTED AND FIXED, all eight. (1) 'no third-party runtime dependencies for the library or the test suite' - blocking PIL through a sitecustomize gave 10 failed, 3370 passed; the vision tests REQUIRE Pillow and do not skip. (2) The mermaid diagram drew all four surfaces flowing into REDACT; measured, redact appears 32 times in tail and 2 in logparse and ZERO times in gvas, savewatch, damage, avgprice, armwatch and vision_meter. (3) It drew REDACT into Emberforge into the overlay; emberforge is one 28-line __init__.py and nothing imports it. (4) 'lane-ownership and worktree tests' understated a real git archive export - 78 failed across nine files. (5) 'What is next' asserted two gates while omitting open roadmap item 7c. (6) 'every save parses with zero undecoded bytes' contradicts docs/FINDINGS.md section 10.8 - 401 undecoded struct leaves, 10,600 bytes. (7) The 41.6 GB install figure moved when the client was patched. (8) 'Measured 2026-09-14' back-dated a 2026-08-10 figure to today.
- The diagram now tells the truth: the log reader redacts inline, the other readers hand back what the game wrote, and the redactor is the gate on the way OUT - ops.outbox.deliver for anything sent, tests/test_no_pii.py for anything committed. The two Emberforge edges are dashed and labelled planned.
- UI/UX audit BLOCKED the first draft on three of five phases and every fix was measured. flowchart LR rendered to a 1346px viewBox, which scales 16px labels to 4.6px on a 390px viewport; TD renders 897px and needs no downscale. Worst table cell went from 343 rendered characters to 122. Mermaid verified by RENDERING it with mermaid-cli 11.17.0, exit 0, not by reading it.
- A PINNED DECISION WAS NEARLY RE-LITIGATED AND A GUARD CAUGHT IT. The draft added shields.io badges for visual interest. LL-0139 had deliberately kept the README's only badge first-party so the page pulls no third-party image, and docs/ECOSYSTEM.md carries a register row saying exactly that. tests/test_source_register.py failed on two unregistered hosts - the shields.io IMAGE subdomain and the Python project's own site - which is the register's own rule that a parent and a subdomain are different operators. Replaced with a first-party fact line; the decision stands unweakened.
- GitHub About and topics updated and verified by reading them back. Topics chosen on MEASURED topic-page sizes rather than by feel: the python topic carries 878,616 repositories and buys no discovery, mistfall-hunter carries 8 and is a page somebody actually browses. 20 topics, the GitHub maximum.
- The dps-meter topic was measured at 51 repositories and DELIBERATELY NOT ADDED. Emberforge computes nothing, so the topic would have been a claim the tree does not support.
- README: 0 non-ASCII bytes, 26 relative links all resolving, 275 lines. Suite 3412 passed, 1 skipped. Pre-flight PASS.

THE LESSON IS THAT A DIAGRAM IS A CLAIM. Prose in this repository gets challenged; a box-and-arrow picture reads as decoration and slips through. Three of the eight refutations were edges in one mermaid block, and every one of them described a design intention as though it were shipped code. The redaction edge was the worst, because it asserted a safety property - four surfaces gated by the redactor - that only one surface actually has.
The refuting agent also caught that README.md changed three times mid-pass and said so, refusing to let its verdict read as a merge clearance for a tree it had not finished reading. That refusal is worth more than the findings.

### LL-0253 - 2026-09-14 - The operator confirmed the cross-project FULL AUTHORITY directive in a Lanternlight session, so it is recorded in CLAUDE.md with its scope adjudicated - and the items parked on 'needs an operator ruling' are now ours to act on

**Evidence:**
- The directive arrived on 2026-09-13 as a RELAY from Clockspeed, which stated in its own words that it carried no authority here until this project's operator confirmed it in this session, and asked for a reply only if the QA came back NEGATIVE. The operator confirmed it in chat on 2026-09-14. That confirmation, not the note, is what makes it binding - a note is MAIL and never a task.
- Recorded in CLAUDE.md as its own section ahead of Session Default, in full rather than compressed, because a directive that lives in one context window is a directive a cold session will not obey.
- SCOPE ADJUDICATED IN THE ARTIFACT, both halves. What it does NOT touch, because these are rules and not permission gates: the hard boundary and ADR-001, redaction and ADR-004, the third-party license gate, the port block, 7-bit ASCII, TDD. What it DOES change: items parked here on the words 'needs an operator ruling' are the session's to adjudicate, including soliciting a sibling where the only barrier was that asking was reserved to the operator. An item blocked on a MODE, a MEASUREMENT or another project's ANSWER is still blocked, because authority does not manufacture a fact.
- Acted on in the same session rather than filed: OPS-36 criterion 1 had been blocked since 2026-09-07 on the reasoning that asking RC which charter version is current WAS soliciting RC, which was held. A re-ask went to RC through ops.outbox.deliver, naming what we will do without an answer - discharge the remaining criteria against the last version we hold, name it, and mark criterion 1 BLOCKED ON RC rather than met.
- A consolidated note went to all four trees through the derived fleet address list: the denominator concession of LL-0255, RSC's pytest -q correction CONFIRMED verbatim against pytest.ini, RSC's vacuous-glyph-gate warning measured NOT LIVE here, RC's relative-hook-path finding REFUTED for our tree at zero of six, and our withdrawn 18.7-second figure named again. Both deliveries reported all recipients delivered and none failed.
- CS is told the QA came back POSITIVE even though the note asked for silence in that case, because 'no correction was sent' and 'nobody ran the QA' are indistinguishable from their side.
- docs/drafts/sent-to-RSC-pytest-pipeline-and-our-census-numbers.md carried our withdrawn 18.7-second figure unqualified at line 58. It now carries a correction banner and the BODY IS LEFT BYTE-EXACT, because it is a record of what was SENT and editing it would falsify the record rather than correct it.

The mail sweep that found the withdrawn figure also found that the session brief undercounted the inbox: it said 17 files dated 2026-09-13 and there are 45, of which 27 are narrative notes. A count in a hand-off is a hypothesis exactly like any other count.
RECORDED QUESTION, not a task. OPS-71 is still blocked and the full-authority directive does not move it: settling it needs a session in DEFAULT permission mode with its working directory in a clone at another path, because a did-it-prompt test run in bypass mode cannot distinguish a MATCHED rule from a BYPASSED one and returns a false green either way. That is a MODE constraint, not a permission one. Under the directive this is no longer raised with the operator as a question - it is recorded here and left blocked until a session happens to run in that mode.

### LL-0252 - 2026-09-13 - The wrap's refutation pass REFUTED this session's own OPS-89 close - the heartbeat guarded the QUESTION and not the REFUSAL, and a counter-control proved the tests could not tell the two apart

**Evidence:**
- REFUTED WITH TWO REAL INTERPRETERS, not by reading code: session two acquired cleanly against a heartbeat ZERO SECONDS OLD. is_locked was made heartbeat-aware and acquire was left untouched, and a second loop never calls is_locked - it calls acquire and runs if nothing is raised. OPS-89 had been graded five criteria MET on a mechanism that reported HELD and refused nothing.
- THE COUNTER-CONTROL IS THE PART WORTH KEEPING. The refuter made acquire heartbeat-aware and tests/test_loop_guard.py stayed GREEN at 84 passed, proving nothing pinned that path in EITHER direction. A suite that cannot distinguish two opposite implementations was never testing the thing at all. That single check was worth more than the four mutants beside it, and it is the technique to reach for whenever a fix looks too easy.
- REPAIRED RED FIRST. The new arm was watched failing with DID NOT RAISE LockBusy before any code changed. Four tests: a second acquire against a fresh heartbeat raises; a stale heartbeat is reclaimed ON THE ACQUIRE PATH; a lock with no heartbeat is still reclaimed immediately, so the long-lived-process model is unchanged; and a LIVE pid still refuses whatever the heartbeat says, because the heartbeat may only ever ADD a reason to refuse and never remove one.
- MUTATION including the exact reversion that was previously invisible: acquire-ignores-heartbeat-again KILLED, acquire-refuses-on-ANY-heartbeat-even-stale KILLED, heartbeat-arm-moved-ahead-of-the-live-pid-arm KILLED. Anchors asserted, restores digest-verified, bytecode cache cleared on both sides of every mutation.
- END TO END, the same probe that refuted it: session1 ACQUIRED pid 27168, session2 REFUSED - LockBusy.
- A SECOND FINDING FROM THE SAME PASS, an over-claim in ops/preflight.py. With the linter absent the verdict read 'PRE-FLIGHT PASS - lint DID NOT RUN' and the test named ..._never_as_a_pass asserted only that the words appeared. Anything grepping for PASS, a human skimming included, got a pass out of a check that never happened. The verdict is now 'PRE-FLIGHT INCOMPLETE - lint DID NOT RUN' and the test asserts PRE-FLIGHT PASS is absent.
- A THIRD FINDING, in the hand-off itself: it named a stale head, said seven commits where git says nine, and said four items remain. The last was derived from a grep matching only OPS- headings. Re-derived properly, ROADMAP.md carries 36 level-2 headings of which the non-OPS measurement items - 1, 4b, 5, 6, 7, 10, 11, 12 - are also open and are gated on the game client with the operator playing, which is a different gate from the four OPS items and had to be said separately.

THE LESSON IS NOT THAT THE FIX WAS WRONG, it is that this session graded its own work and the grade held until an independent pass broke it. Three of the four items closed today survived that pass unchanged; the one that did not was the one whose own ledger entry claimed the most.
Every number in this entry was observed at the wrap and none is carried forward.

### LL-0251 - 2026-09-13 - The OPS-89 heartbeat is NOT symmetrical - is_locked was made heartbeat-aware and release was not, found at the wrap by trying it

**Evidence:**
- FOUND BY USING THE FIX RATHER THAN BY REVIEWING IT. At the wrap of the session that added the heartbeat, releasing the lock returned False and the lock stayed held. The cause is that release still declines when the recorded owner is not the calling pid, so a conversational session cannot release its own lock the obvious way - the pid in the file died with the command that wrote it. That is exactly the defect OPS-89 fixed on the reading side, surviving on the writing side.
- NOT A NEW CAPABILITY, ONLY AN UNNAMED ONE. Releasing AS the recorded owner already works and needs nothing new: read the owner out of the lock and pass it. Verified at the wrap - released True, and the lock then read as not held.
- DOCUMENTED IN docs/HEADLESS.md SECTION 4z rather than fixed in code, and the reason is a threat-model one rather than laziness. Making release heartbeat-aware the way is_locked is would mean any process could clear any heartbeat lock, including a second loop clearing the first one's, which is the exact thing the guard exists to prevent. The force flag is refused here for the same reason - it exists for an operator clearing a lock by hand.
- SKIPPING THE RELEASE IS ALSO CORRECT and is written down as such, because it is what a crash does: the session stops beating and the claim expires inside the 900-second window. The asymmetry is therefore a documentation gap rather than a hole.
- SUITE observed this run and not carried forward: 3405 passed, 1 skipped in 219.34s. Pre-flight PASS, lint clean.

This is the fourth time this session that a dotted identifier tripped the source register, and the second where the right answer was KNOWN_NON_HOSTS rather than rewording - the text was inside a fenced CODE block, where CLAUDE.md requires it stay byte-exact.

### LL-0250 - 2026-09-13 - Loop STOPPED on its own stop condition - no eligible roadmap item is left, and the four that remain are each gated on something this session cannot supply

**Evidence:**
- Four cycles ran: OPS-87 closed (criterion 4 measured on both halves, the pre-flight wired into the lane contract, the linter added to its set), OPS-88 filed and closed, OPS-89 filed and closed, OPS-90 filed and closed. Five commits, all pushed, tree clean at 07c7ed6.
- NO ELIGIBLE ITEM REMAINS, which is the loop's own stop condition and is why nothing further was attempted. OPS-71 needs a session in DEFAULT permission mode and this one ran in bypass, where a did-it-prompt test cannot distinguish a MATCHED rule from a BYPASSED one and returns a false green either way - that is the item's own stated difficulty, not a new discovery. OPS-36 is operator-ruled ADOPT but its criterion 1 needs RC to say which charter version is current, and asking RC is soliciting RC. OPS-48 is HELD by operator ruling. OPS-68 is operator-held standby.
- WHOLE-SESSION COST, recorded by the instrument built this session rather than estimated: 8 full non-nested suite runs, at least 2165.9 seconds (36m 5s) of summed suite wall clock, over a 3h 28m span, with 192 records excluded as filtered or nested. These are the numbers OPS-87 criterion 4 existed to make available, taken on the session that made them available.
- THE INSTRUMENT DISTINGUISHED UNMEASURED FROM MEASURED ZERO at the wrap without being asked to, which is this project's own doctrine working: asked for the cycle starting at the tip commit, it reported zero full runs as 'NO EVIDENCE rather than a measured zero' instead of printing a zero a reader would have believed.
- Two notes went out to all four trees through the derived fleet address list, its first two real uses: one carrying criterion 4's numbers, the restored pre-flight range and the poisoned-bytecode trap, and one telling them our lane slots are command-scoped and that the workaround they would reach for first is already dead.

THE CAPTURE WATCHER WAS NOT ARMED, deliberately. The loop ritual arms it and the operator disarmed that lane on 2026-09-11 without lifting it; the operator overrides the ritual. Nothing is archiving the log, the saves or the market cache, which is the accepted cost recorded at the previous wrap.
The single-instance lock was taken WITH a heartbeat for the rest of this session once OPS-89 landed, which is the first real use of that fix - a second process confirmed the lock reads as held on the heartbeat although the recorded pid was already dead.
A QUESTION FOR THE OPERATOR, recorded rather than asked, because the loop does not block: OPS-71 cannot be settled by any session running in bypass permissions mode. It needs one started in default mode with its working directory in a clone at another path. Everything else about the item is ready.

### LL-0249 - 2026-09-13 - OPS-90 CLOSED without touching the cross-project protocol - the lane slot stays command-scoped and the status line now says so, because the workaround that would have held it across a session was measured dead

**Evidence:**
- CRITERION 1: two real subprocesses against one bucket, and the SECOND is handed the same slot the first took, because the first released it when it exited. That is the context manager doing exactly what it promises rather than a bug in it, and the test pins the consequence: across a conversational session, which is a new process every command, this project rations with nobody between commands.
- THE OBVIOUS WORKAROUND WAS RULED OUT BY MEASUREMENT, NOT BY PREFERENCE, and it is pinned by a test so nobody re-proposes it. Leaving our lock in the bucket between commands would have held the slot across a session with no payload change at all. It does not survive: the staleness rule consults the recorded pid, and a lock whose pid is dead is stale to every participant's reaper including ours. The protocol's pid field assumes a live process and a conversational session has none.
- WHAT WAS THEREFORE NOT DONE. The field that would fix this properly is a payload change, and a lane slot lock lives in the shared bucket whose payload shape CLAUDE.md names as the one thing deliberately held in common. Adding a heartbeat there is not a session decision, which is the whole reason OPS-90 was split out of OPS-89 rather than fixed alongside it - the single-instance lock is ours alone and could take one.
- ADOPTED INSTEAD: the slot stays command-scoped, which is honest rationing for exactly the period this project is consuming the machine, and the STATUS LINE states that scope. What was actually wrong was a line reading 'HELD 0.lock' that a reader would fairly take to mean the session holds it. The HELD branch now reads 'for this command only', and the note rides on that branch alone because a refusal has no hold to scope.
- MUTATION: two mutants - the scope note removed, and the scope note leaked onto the BUSY branch - both KILLED, each anchor asserted to match exactly once, restore verified by digest, and the bytecode cache cleared afterwards.
- SUITE observed this run and not carried forward: 3405 passed, 1 skipped in 219.75s. Pre-flight PASS, 14 guard modules in 11.32s, lint clean.

Criterion 4 is met by not applying: the adopted route involves no other tree, so there was no ask to send and nothing to record as data. The fleet was told anyway, because they share the bucket and might otherwise assume this project's slots are session-scoped.
This closes the thread OPS-89 opened by measuring the three governors separately. The lock now holds on a heartbeat, the slot is honestly command-scoped, and the session watcher remains deliberately disarmed by operator instruction of 2026-09-11.

### LL-0248 - 2026-09-13 - OPS-89 CLOSED - the single-instance lock was INERT in a conversational loop and now holds on a heartbeat, and the three governors gave three different answers rather than one

**Evidence:**
- THE DEFECT WAS MEASURED AT THE START OF A LOOP RUN, not reasoned about. The acquire succeeded and recorded a pid; the NEXT interpreter read the lock as NOT held with that same pid as owner, because the process that took the lock was the one-shot interpreter that took it; the release then refused to remove the file, because it declines when the recorded owner is not the calling pid. Nothing was held, and a second loop would have acquired cleanly while the first printed a lock line and believed itself guarded.
- CRITERION 1: the measurement is reproduced by a test that acquires in a real subprocess and asserts from the parent that the lock reads as not held. It was GREEN before any fix, which is the point - it is a characterization test of the state every conversational cycle was really running in, kept so a fix cannot hide it.
- CRITERION 2, the decision with its costs, written up in docs/HEADLESS.md section 4z. A holder process whose liveness is the session's was REJECTED: ending it needs a cmdlet CLAUDE.md forbids, and a holder orphaned by a dead session would hold the lock against every future loop with nothing permitted to kill it, turning a nuisance into an outage. Printing UNGUARDED every cycle was REJECTED as the whole answer - it is the honest UNUSABLE precedent from the lane slot, and it describes the problem while preventing nothing. A HEARTBEAT was adopted.
- CRITERION 3, crash recovery is not weakened, and this is the part that made the design safe. The heartbeat is a SECOND way to be held and never a replacement: a lock carrying no heartbeat field is answered by pid liveness exactly as before, so the long-lived-process model docs/HEADLESS.md section 4 describes keeps its immediate reclaim of a dead owner and loses nothing. A stale heartbeat is reclaimed, and an unparseable stamp is NOT fresh so a corrupt timestamp falls back rather than wedging the loop forever.
- THE WINDOW IS A STATED NUMBER: 900 seconds, chosen against the measured shape of a cycle here rather than by taste. A full suite run was between 230 and 482 seconds on 2026-09-13, recorded in ops/runtime/suite_runs/ by the instrument OPS-87 built, so a cycle that beats once has room for a suite run, its pre-flight and a commit before the claim expires.
- CRITERION 4, and the three governors gave THREE DIFFERENT ANSWERS, which is exactly why the item forbade inferring one from another. The lock is inert. The lane slot is correct WITHIN one command and released when that process exits - a second process was handed the same slot - so a conversational session rations with nobody; filed as OPS-90 rather than fixed, because a lane slot lock lives in the shared bucket and its payload is the cross-project protocol ADR-008 records, and changing a payload other projects parse is not a session decision. The session watcher is a separate process and does survive a command exiting; it is deliberately disarmed by operator instruction of 2026-09-11 and nothing here re-arms it.
- MUTATION, AND TWO SURVIVORS THAT WERE REAL DEFECTS IN THE TESTS. Five mutants first: three killed, and two SURVIVED - shrinking the window from 900 seconds to 1, and reading an unparseable stamp as fresh. Both survived for the same reason: every test stamped a heartbeat and read it back immediately, so any positive window passed, and nothing fed a corrupt stamp. The window test asserted only that the constant was a positive integer, which is this repository's own trap that a negative assertion rules something out without pinning anything down. Three arms were added - fresh at ten minutes, stale at thirty minutes, and an unparseable stamp - and all are now killed, including a fourth mutant added in the opposite direction that made the window enormous.
- SUITE observed this run and not carried forward: 3401 passed, 1 skipped in 227.89s. Pre-flight PASS, 14 guard modules in 12.15s, lint clean.

Two attribute references in a fenced CODE block in docs/HEADLESS.md were added to KNOWN_NON_HOSTS rather than reworded. That is the opposite of the choice made three times earlier the same day, and the reason is the rule rather than convenience: CLAUDE.md requires code stay byte-exact, so the rewording route available for dotted identifiers in PROSE is not available inside a code fence.
The first placement of those two entries was wrong and was moved. They were inserted under a comment block describing a different group, in a set that is grouped by theme with sorted runs rather than sorted globally - so an alphabetical insert put them under prose that did not describe them, which is the exact drift that set's own preamble warns about.

### LL-0247 - 2026-09-13 - OPS-88 CLOSED - a target supplied through the environment is now NAMED in the record's reason list, and both signals that catch it are proved independently sufficient

**Evidence:**
- THE MECHANISM WAS MEASURED BEFORE THE FIX WAS DESIGNED, on a throwaway repository rather than inferred from documentation. With a target supplied through the environment, pytest's invocation arguments held only ('-s',) while the configured argument list held the path of one test module. That is the only place such a target is visible.
- THE TRAP THE MEASUREMENT REVEALED, and it would have reddened every full run in this repository: on a FULL run the configured argument list is exactly the configured test paths. Comparing it against emptiness rather than against those paths would have marked every full suite run as narrowed. The comparison is therefore against test paths, and an unknown test-paths setting produces no reason at all rather than a guess - absent is not empty.
- BOTH SIGNALS ARE INDEPENDENTLY SUFFICIENT and each has its own test, which was the item's criterion 2. A real run narrowed through the environment now records two reasons: the new one naming the target, and the pre-existing module-coverage reason '68 of 69 test modules on disk did not run'. Neither is load-bearing alone, so a tree that reads the configured arguments differently still cannot have a narrowed run counted as full.
- CRITERION 3, that no genuinely full run is reclassified, checked against the records on disk rather than asserted: zero FULL runs carry the new reason. The full suite run taken after the change records 3392 collected, 3391 passed, 229.9 seconds, full true, with an empty reason list.
- MUTATION: four mutants - the test-paths exclusion dropped, an unknown test-paths treated as known, the module-coverage backstop removed, and the new reason removed - all KILLED. Each anchor was asserted to match exactly once before any survivor could be believed, the restore was verified by digest, and the bytecode cache was cleared afterwards because a same-second restore is exactly what poisoned it during OPS-87.
- SUITE observed this run and not carried forward: 3391 passed, 1 skipped in 230.08s. Pre-flight PASS, 14 guard modules in 12.78s, lint clean in 0.06s.

This was a MINOR finding and the verdict was never wrong - only the reason list was incomplete. It was worth closing rather than leaving because the reasons are what a future session reads when it asks why a cycle's full-run count is lower than expected, which is the number OPS-87 just made load-bearing.

### LL-0246 - 2026-09-13 - OPS-87 CLOSED - criterion 4 measured on both halves, the pre-flight is wired into the LANE CONTRACT and a git hook was refused on our own back-test, and the linter joined the set because an adversarial round was spent on a lint error

**Evidence:**
- CRITERION 4 MET, both halves derived at run time by `python -m ops.cycle_cost` and written up in docs/CYCLE_COST.md. Never quote these from the document - re-run the command.
- BEFORE, reconstructed from git and the ledger over a 40-commit window: 14 cycles across 25 distinct commits, 4 of them seeded by the only 2 verbatim 'refutation pass over <sha>' sentences that exist and the rest grouped by a shared OPS- id. The suite-run figure is a FLOOR of 37, built from full-suite results QUOTED in prose, because no historical run was ever recorded. Two runs with the same pass count collapse into one quote, so even a nonzero floor understates.
- AFTER, recorded exactly by ops/suite_recorder.py and the hooks in tests/conftest.py, one atomic JSON record per pytest session under ops/runtime/suite_runs/: 7 full non-nested suite runs in this cycle, >= 2203.9s (36m 43s) of summed suite wall clock, 2h 38m 20s elapsed from the tip commit, and 107 records excluded as filtered or nested with their reasons printed from the rows.
- THE TWO HALVES ARE NOT A RATIO and nothing in the code or the document states one. A floor divided by an exact count manufactures a number neither half supports; this project withdrew exactly such a figure from four sibling trees on 2026-09-12 (LL-0244). The adversarial pass searched both artifacts on a whitespace-collapsed copy for fourteen ratio patterns, proved the patterns live by making them fire, and found only the prohibitions.
- STILL-OPEN ITEM 3 DECIDED: the pre-flight is wired into the LANE CONTRACT, rendered by ops/lane_contract.py into every lane's contract. A GIT HOOK WAS REFUSED on our own evidence - back-test experiment one caught 0 of 17 at the parent of each commit that filed a finding, because a registration and its fix land in ONE commit and the broken tree is never committed, while the reconstructed mid-session state caught 9 of 9. The window is inside a session, so the contract is the artifact a slice actually reads at the moment it matters.
- tests/test_lane_contract.py fails if the command is removed, if it is moved after the merge-gate instruction a MERGER runs, or if the sentence saying it does not replace an adversarial pass is dropped. Watched red: whole-block-removed KILLED, command-only-removed KILLED, moved-after-the-merge-gate KILLED. A fourth mutant that removed only the heading SURVIVED and was judged a weak mutant rather than a vacuity, because the command and the claim both remained.
- THE LINTER IS NOW IN THE PRE-FLIGHT SET, on a measured event rather than a hunch. Three slices landed under this item, each ran the pre-flight before claiming done, each got PRE-FLIGHT PASS, and `ruff check .` then failed with seven findings on lines they had just added. The pre-commit gate would have refused the commit, so nothing was going to ship - that is back-test experiment one's finding again - but the defect was found by the ADVERSARIAL pass, which is the substitution criterion 2 exists to prevent.
- Back-tested against the real event rather than asserted: reintroducing one of the seven findings and running the pre-flight end to end gives PRE-FLIGHT REFUSE, 'pytest: 382 passed, 1 skipped', 'lint: Found 1 error. (0.07s)'. Four mutants on the lint step - verdict-never-refuses, main-ignores-lint, summary-is-a-literal, missing-ruff-reads-as-pass - all KILLED, anchors asserted, restores digest-verified.
- PRE-FLIGHT RUNTIME RE-MEASURED SERIALLY on a quiet machine: 21.45, 24.44 and 17.93 seconds. That sits inside the 17.8 to 24.6 range this project published to four trees, so the range HOLDS and is not withdrawn. One 28.99s reading taken while three lanes ran concurrently is outside it, so the published figure is a QUIET-MACHINE range rather than a promise, and the follow-up note says so.
- fleet() added to ops/outbox.py - the fleet is now a NAME derived from SIBLING_INBOXES rather than a tuple retyped at each call site. On 2026-09-12 the consensus request reached three of four trees because one list was typed short, and the delivery manifest recorded faithfully that the note reached everyone it was ADDRESSED to, which is a different question from whether it was addressed to everyone. Mutants hardcoded-complete and hardcoded-omits-LW both KILLED; the anti-vacuity arm adds a code to the map and asks for it back.
- A POISONED .pyc OUTLIVED ITS OWN RESTORE and cost two unexplained failures. A mutant that MOVED a block in ops/lane_contract.py left the file the same SIZE, pytest compiled the mutant to cached bytecode, and the restore wrote the original back inside the same clock second. A timestamp-based .pyc is validated on mtime and size alone, both of which matched, so Python kept serving the mutant while the source on disk was correct and a digest comparison said the restore had succeeded. Recorded in CLAUDE.md: after any harness that rewrites a module in place, delete __pycache__ before believing the next run.
- The pre-flight refused this session's own work twice, both times correctly: once on an unregistered source-register host extracted from a dotted identifier in docs/CYCLE_COST.md, and once on the same shape in CLAUDE.md. Both were fixed by REWORDING rather than by widening KNOWN_NON_HOSTS, because the guard was right and only the prose needed to avoid the truncation.
- SUITE, observed this run and not carried forward: `python -m pytest` bare gives 3385 passed, 1 skipped in 235.58s (0:03:55). Pre-flight PASS, 14 guard modules in 15.35s, lint clean in 0.06s. ruff check . clean. Merge gate OK at 3357 collected against a per-file baseline reconstructed from HEAD in a worktree outside the repository; no per-file count dropped.

WHAT REMAINS OPEN UNDER OPS-87 AND IS THE HONEST POSITION RATHER THAN A GAP: 93 of the 135 census events are reached by no program here, and any proposal claiming to reach them must show a back-test. A proposal that reduces adversarial review of class (a) is refused on its face - 60 of 135 events are real defects in a deliverable and no program described here would have found one of them.
The AFTER count is exact only for runs the recorder saw. The record directory is SHARED between concurrent sessions in this tree, so a record proves that SOME session ran a suite rather than that THIS cycle did. The report prints that caveat from the rows it counted rather than from a literal.
The cycle measured above was still OPEN when these numbers were taken, so 7 runs and 2h 38m are lower bounds on the finished cycle.

### LL-0245 - 2026-09-12 - OPS-77 was closed with the same hole one level over - a NEGATIVE surplus width still answered BUSY on an empty bucket - and the mutation harness that proved the fix then failed to restore and left a mutant on disk

**Evidence:**
- FOUND BY THE REFUTATION PASS over e646bca, the commit that closed OPS-77, and it is the same lie one level over. acquire_lane(surplus=-1) against an EMPTY, WRITABLE bucket returned the None that means BUSY: surplus_names(-1) is (), so the candidate order was empty, the walk had nothing to try, and the answer was a permanent silent BUSY against a healthy bucket - which is the exact sentence OPS-77 was filed to kill.
- IT BROKE THE ITEM'S OWN CRITERION 2, that the two entry points must not disagree. They agreed about zero and diverged about minus one: shared_surplus_width floors a negative override to the published default, so the environment path contended normally while the parameter path, which had no validator at all, went straight to the empty order.
- FIXED with ONE resolver rather than a second validator. ops.lane_slot.resolve_surplus_width is now the only place the width is decided, and is_contention_opt_out and bucket_slot_order both ask it instead of each doing the arithmetic. The rule is the one shared_surplus_width already documents and is unchanged for the environment: a width that is not usable falls back to the published default, because a governor refusing to start over a typo turns a typo into an outage. ZERO is not a typo and survives untouched as the documented opt-out.
- Five new tests, red first: a negative parameter must not answer BUSY on an empty bucket, a negative resolves to the published default, the two entry points agree about a negative, a negative is NOT quietly turned into an opt-out (that arm was green before the fix and is a regression guard rather than a new claim), and the order for a negative is not empty. tests/test_lane_slot.py 127 to 132 collected.
- MUTATION WATCH, three mutants, and the FIRST ONE REFUSED TO APPLY - the anchor matched TWICE, because the flooring clause exists in both the new resolver and shared_surplus_width. The harness said ANCHOR-MISS and did not run, which is the only reason a survivor there would not have been believed. Re-run with a unique anchor: KILLED, 4 failed. The other two killed at 2 failed and 3 failed.
- AND THE HARNESS THEN FAILED TO RESTORE, WHICH IS THE FINDING WORTH KEEPING. The re-run's write_text in its finally clause raised OSError errno 22 on the restore, so the MUTANT WAS LEFT ON DISK - measured immediately afterwards: one occurrence of the mutant marker in ops/lane_slot.py and tests/test_lane_slot.py red at 4 failed. It was reverted by hand and re-verified at 165 passed across both modules.
- The lesson is not that a write can fail. It is that a mutation harness is trusted to leave the tree as it found it, and this one reported KILLED on its way to leaving a mutant behind. A harness that prints its verdict before its restore has succeeded is reporting on a tree that no longer exists. Nothing was committed from that state; the suite was re-run rather than the report believed.

### LL-0244 - 2026-09-12 - Follow-up SENT to all four trees on a standing operator ruling - it opens by WITHDRAWING a runtime we could not reproduce, and its other four findings all cut against our own proposal

**Evidence:**
- OPERATOR RULING, 2026-09-12, extending the one recorded in LL-0238: send the follow-up, and do NOT leave it as the operator's call - send it in future without asking. A follow-up is now a session decision bounded by destination, exactly as a reply is, and the sync inboxes remain the only permitted destination.
- SENT to CS, RC, RSC and LW, NONE failed, 4582 bytes, digest opens d8220fe4a54dd208. Our copy is in moon_sync_inbox/_outbox/ with the delivery manifest.
- IT OPENS WITH A WITHDRAWAL, because we had told three trees a number we could not reproduce. The note sent on 2026-09-12 quoted the pre-flight at 18.7 seconds. Our own refuter measured 24.56, 20.96 and 24.31 on three consecutive runs, and the audit that followed found THREE filed figures for one measurement - 18.7 in a commit message, 19.3 in two documents, and a real spread of 17.8 to 24.6 across eleven runs. The withdrawal states the range and says the spread tracks machine load.
- THE REASON IT WENT OUT RATHER THAN BEING QUIETLY CORRECTED IN OUR DOCS: a single reproducible-looking number is exactly what another tree would design against, and three of them had it.
- THE OTHER FOUR SECTIONS ALL ARGUE AGAINST OUR OWN PROPOSAL, which is why they were worth sending. The vacuous guard our refuter found in the pre-flight's own caveat. The five false reds that turned out to be this session's new test modules. An instrument that runs the suite twice cannot be run against a moving tree, and the shipped version REFUSES where the previous one would have printed a table under the word UNPROVEN. And the back-test warning: check first that the defect ever existed in a tree git can address, because ours never did and the naive number said our gate was worthless.
- Nothing was adopted, nothing shared was proposed, and the counts stand as sent.

### LL-0243 - 2026-09-12 - OPS-86 criterion 4 settled on a quiet tree - positive control PROVED, findings ZERO, and the refusal fired correctly on the run before it

**Evidence:**
- RE-MEASURED ON A QUIET TREE, which is the only condition under which this instrument answers: positive control PROVED on all five planted specimens, 4 PATH entries stripped, 3270 repository tests collected WITH the tool and 3270 WITHOUT, clean_skip=247, exercised=100, skip_both=1, untouched=2922, and FINDINGS 0.
- FALSE RED IS ZERO. The five false reds the probe found earlier in this session were this session's own new test modules shelling out to git without declaring the capability; both now go through the shared guard, and clean_skip rose from 242 to 247 accordingly - the same five tests, moving from the failing column to the skipping one, which is what the OPS-78 policy asks of them.
- THE RUN BEFORE THIS ONE REFUSED, AND THE REFUSAL WAS CORRECT. It reported the positive control UNPROVEN with all five specimens called 'appeared', and printed no counts at all. The cause was this session editing the tree between the probe's two suite passes, so tests genuinely did appear between them. That is OPS-86's new behaviour working on its first real provocation: the old code would have printed a full count table under the word UNPROVEN, which is the exact failure the item was filed about.
- So the instrument's own precondition is now visible in its output rather than in a docstring: a probe that runs the suite twice cannot be run against a tree somebody is editing, and it now says so by refusing instead of by producing numbers that read like findings.

### LL-0242 - 2026-09-12 - The OPS-87 consensus is FOUR trees on an operator instruction - LW is in, and the other three were told so the silence rule is not applied to an absence nobody created

**Evidence:**
- OPERATOR INSTRUCTION, 2026-09-12: include LW in the OPS-87 consensus. The instruction that opened the item named CS, RC and RSC and the request went to exactly those three, so LW was outside it by the terms of the original ruling rather than by any session's judgement. The operator widened the set.
- DELIVERED to LW through ops.outbox.deliver: digest bd4e014e8640087... (TRUNCATED to 15 hex characters on purpose - the full digest carries a 15-digit decimal run and tests/test_no_pii.py reads that as a SteamID64-shaped identifier. The guard is structurally right to ask and the answer here is that it is a content digest, so the citation was shortened rather than the guard widened; the full value is in the delivery manifest under moon_sync_inbox/_outbox/), 5751 bytes, 2026-09-13T01:16:14Z, NONE failed. It carries BOTH halves - the ask verbatim as the other three received it, and this project's finished numbers - because a request arriving after everyone else has answered is not a fair request unless it also carries what they answered.
- AND THE OTHER THREE WERE TOLD THE PARTICIPANT SET CHANGED: digest f6c982f05cc34d146ad020c228aa51bb669186e6185b34cbdf29c06795e4d707, 1290 bytes, to CS, RC and RSC, NONE failed. The reason is mechanical rather than courteous. The standing rule on that channel is that SILENCE READS AS DISSENT, and LW had been silent for the sound reason that nobody had asked them, so a tree tallying positions would have counted an absence nobody created. LW's position is recorded as OPEN and newly requested, not withheld.
- The LW note also states, unprompted, that it is NOT a request to vendor anything of theirs and NOT an offer of ours, and that the vendored write tracer under third_party/ is unaffected by any of it. OPS-84 authorised vendoring one licensed file and nothing else, and a consensus note is exactly the place where that could be quietly read as an opening.
- OPS-87 criterion 6 is now met against four trees rather than three. What comes back is recorded as DATA - each project's own numbers - and never as agreement: this repository has already been burned by one stale observation relayed twice as if it were corroboration, and four counts that land close together raise the question of a shared input before they raise anything else.

### LL-0241 - 2026-09-12 - The refutation pass over c6b854d found a VACUOUS GUARD in the pre-flight - hardcoded census counts, in the commit whose whole subject was measuring instead of reciting

**Evidence:**
- REFUTED, and it is the worst defect this session shipped. ops/preflight.py printed its coverage caveat with the census counts as string literals. The refuter rewrote them to 3 of the 4 events and 1 of 2 stale-recital events, with both anchors asserted matched, ran tests/test_preflight.py and tests/test_refutation_census.py, and watched 52 tests stay green - because the guard beside them asserted only that the substrings does-not and adversar appeared.
- That is this repository's own rule that a filed count is a hypothesis, in shipped code, inside the commit that measured how often this project recites a stale number. FIXED by derivation: coverage_caveat() reads the census rows on every run, so a stale count is not expressible. Its guard asserts no VALUE - it asserts the caveat reports whatever the rows say - and a second arm proves the caveat still prints, without numbers, when the census cannot be read at all, because failing silent about a blind spot is worse than failing without figures.
- REFUTED, number drift across three places for one measurement: the commit message said the pre-flight ran in 18.7 seconds, two documents said 19.3, and the refuter measured 24.56, 20.96 and 24.31 while three other slices were running. Every one of those is now replaced by the measured RANGE, 17.8 to 24.6 seconds across eleven runs on 2026-09-12, with the reason it is a range stated beside it.
- REFUTED, a third number: tests/test_preflight.py's docstring said 35 of 54 stale recitals were unreachable while the code said 39. Corrected to the adjudicated 15 reachable and 39 not.
- ACCEPTED AND ADOPTED, an improvement the refuter found rather than a defect: its own anchor check was scoped per ENTRY while ours was repository-wide, so ours would pass a row whose quote had migrated to a different entry. missing_anchors is now scoped by default and the loose question is still askable. All 135 rows still resolve; one quote is present in a second entry as well, so the loose check was passing that row for two reasons and would have kept passing it for the wrong one alone.
- CONFIRMED and not weakened: 135 events over 64 entries with the in-window set and the declared set equal in both directions, (a)=60 recounted from the rows, the reconstruction back-test re-run on LL-0230 as CAUGHT with all five failures being registration guards, no worktree left behind, and the hygiene arms green over all 18 blobs of the commit - zero bytes above 127, no home path, no operator identifier.
- THE PROCESS FINDING, because it nearly cost more than it saved. Four agents shared one checkout: three slices on disjoint files and one refuter mutating in place. The refuter's live mutation of ops/preflight.py sat in the working tree while other lanes reported done, and its own full-suite run came back 1 failed against a tree that was being edited underneath it. Nothing was committed from that state - the tree was diffed rather than trusted - but a refuter that mutates belongs in its own worktree, and that is the lesson rather than the near miss.

### LL-0240 - 2026-09-12 - OPS-81 and OPS-86 CLOSED - the split is a strict fixed point, and the false-red probe refuses to print counts it cannot attribute

**Evidence:**
- OPS-81. tools/doc_archive.py gains _index_separator(live_text) and INDEX_SEPARATOR_NEWLINES: the planner tops the junction before the generated archive index up to exactly one blank line instead of appending a newline unconditionally. Applying the split twice now leaves ROADMAP.md byte-identical rather than equivalent up to whitespace.
- Criterion 2 is satisfied BY CONSTRUCTION rather than by care: the function can only ever RETURN newlines to append and has no power to delete, so the conservation contract holds structurally, and a junction already longer than one blank line is left alone rather than trimmed because those characters belong to a section. Both distinctions are stated in the docstrings rather than left implicit.
- OPS-81 watched red: the old always-newline behaviour re-introduced with the anchor asserted to match exactly once, three tests red including both fixed points, restored green. tests/test_apply_doc_split.py 23 to 24 collected, none removed.
- OPS-86 took BOTH repairs because they close different things. Recognition closes the one node-id shape that has been measured; refusal closes the CLASS, and only refusal works on a shape nobody has seen yet. The harm was never that a shape went unmatched - the doctrine already calls that an absence of evidence - it was that a full count table printed under the word UNPROVEN and read exactly like findings.
- OPS-86 criterion 1 by construction: the control was planted under the system temp area both times and only the ROOTDIR varied. Outside that tree the id keeps its file segment and ends with the control module's own .py name; inside it the id is bare, with no segment for the basename matcher to find, for all five specimens.
- OPS-86 criterion 3: six mutants, each anchor asserted to match exactly once, the replacement read back off disk, bytecode purged, and the restore verified byte-identical by digest. All red, no survivors. Criterion 4 re-measured on the real tree with the positive control PROVED on all five specimens, 4 PATH entries stripped, false_red=5, clean_skip=242, vanished=1.
- AND THE FIVE FALSE REDS WERE THIS SESSION'S OWN NEW TESTS. tests/test_preflight.py and tests/test_preflight_backtest.py shelled out to git without declaring it, so they FAILED rather than skipped when the tool was stripped - the OPS-78 policy, broken by the work that was measuring how this project breaks things. Both now call the shared capability guard, which returns the resolved path and pins the absent direction as well as the present one.

### LL-0239 - 2026-09-12 - OPS-77 CLOSED - a zero surplus width is an OPT-OUT rather than a permanent BUSY, and closing it at the call site was only half of it

**Evidence:**
- THE DECISION, taken by the lane rather than referred to the operator: zero is a DOCUMENTED OPT-OUT, not a refusal. shared_surplus_width deliberately accepts 0 while rejecting a negative and a non-numeric width, so turning zero into a refusal would have reversed a different decision that nobody had asked to reopen.
- ops/lane_slot.py raises LaneContentionOptedOut - deliberately NOT kin to BucketUnusable in either direction, so neither branch can ever answer for the other - before the bucket is resolved, created, listed or reaped. None still means only that every candidate slot is taken.
- OPT_OUT_STATUS is the first token of the message so a status line can print it verbatim, and the rest names which entry point set the width and how to rejoin. Both entry points resolve through ONE predicate, is_contention_opt_out, with the parameter winning when given and the environment deciding when it is None; pinned by a table test over five combinations.
- Criterion 3 is met against an EMPTY, WRITABLE bucket, which is the case that made the old answer a lie. Measured before the change: the candidate order was empty and both entry points returned the None that reads as BUSY, against a bucket in perfect health.
- tests/test_lane_slot.py 113 to 127 collected, none weakened or deleted. Seven mutations, every anchor asserted to match exactly once, all red: guard removed, parameter deaf, environment deaf, BUSY wording, subclassing BucketUnusable, source clause flattened, guard moved after the reap.
- THE SECOND HALF, AND IT IS THE PART WORTH READING. Closing it in ops/lane_slot.py left ops/loop/lane.py catching only BucketUnusable, so a zero width propagated straight out of session_lane and would have taken an unattended loop down - a configuration choice converted into an outage, which is exactly what the UNUSABLE branch beside it exists to prevent. The slice that fixed the call site named that gap in its own report rather than leaving it.
- THEN THE FOURTH BRANCH STILL FAILED ITS TEST, one level further down. status_line() builds its own words from held and usable alone, so it printed BUSY while the reason field said OPTED OUT. A status line derived from two booleans cannot represent a third refusal, and the wrong half is the half a human actually reads. Four more mutations killed on that pair, and docs/HEADLESS.md now describes four states rather than three.

### LL-0238 - 2026-09-12 - Operator ruling - replies no longer wait for authorisation, the sync inboxes are the one permitted destination, and the reply LL-0237 recorded as HELD is SENT

**Evidence:**
- OPERATOR RULING, given in chat 2026-09-12 and broadcast to every sibling in the same words: do not ask the operator to authorise a reply and do not ask for direction - resolve a choice with the adjudicator or the lane - and this project may write into the sync inboxes and nothing else.
- THIS CORRECTS LL-0237, which says the reply to RSC is drafted and HELD. It is SENT. The ruling arrived while that entry was being written, and the entry was true when it was written, which is the exact shape of the 45 inherited-record events the census in LL-0237 counted.
- DELIVERED through ops.outbox.deliver, never by hand: digest 5526fa480b86354458157f3fc2c4265434d390b4a75a9b52a8cf9d8b8678ddef, 4184 bytes, to RSC, RC and CS, NONE failed, 2026-09-13T00:07:48Z, our copy under moon_sync_inbox/_outbox/ and the tracked record at docs/drafts/sent-to-RSC-pytest-pipeline-and-our-census-numbers.md.
- WHAT THE NOTE SAYS, all of it re-measured here rather than relayed: RSC's exit-code finding is CORRECT and no tracked hook, script or tool in this repository runs pytest through a pipe, so it was a hand-typed command and no loop has been reading red as green; RSC's own retraction is HALF WRONG, because pytest.ini does carry -q in addopts and they asserted it does not; and this project's four answers to their measurement request are given as numbers with the command that re-derives them.
- THE STANDING QUESTION THIS CLOSES. OPS-68 put cross-project propagation on standby and three previous notes each needed a specific instruction. The ruling replaces that with a standing permission bounded by DESTINATION - the sync inboxes - rather than by occasion, so a session no longer stops to ask. It authorises writing into an inbox and nothing else: no shared artifact, no adopted charter, no sibling module, and a note received is still mail rather than authority.

### LL-0237 - 2026-09-12 - OPS-87 measured before it was optimised - 135 refutation events, the largest bucket is REAL DEFECTS, and the item's own premise that classes (b) and (c) are program-answerable is refuted at 42 of 135

**Evidence:**
- MEASUREMENT FIRST, and the answer is not the one the item assumed. `ops/refutation_census.py` re-derives everything from `docs/LEDGER.md` at run time; only the per-event judgement is stored, in `docs/refutation_census.tsv`, each row anchored to a VERBATIM ledger quote that `tests/test_refutation_census.py` re-resolves on every run, so a classification cannot outlive its evidence. 135 events over 64 entries dated 2026-09-08 or later, every entry examined. LARGEST BUCKET (a), a real defect in the deliverable, 60 of 135.
- GRADING CHANGED THE ANSWER, which is why both columns are published. Five extraction slices filed (c) stale-recital as largest with 54 and (a) at 50. Three adjudicators, none of whom produced the rows they graded, moved 12 of 135 and nearly every move ran the same direction - into (a). A census that graded itself would have reported "the largest bucket is stale prose" and the whole design would have been aimed at documents.
- A PREMISE OF THE ITEM IS REFUTED BY ITS OWN MEASUREMENT. `OPS-87` criterion 2 states that everything in classes (b) and (c) "is answerable by a program". Measured: (b) is 10 of 13 gate-reachable, (c) is 15 of 54, and only 42 of 135 events are gate-reachable at all. The dominant (c) failure is a wrong MECHANISM or SCOPE, not a wrong number, and each was refuted only by an experiment nobody had thought to run.
- AND THE ONE MECHANICAL (c) CHECK IS REFUSED ON EVIDENCE. A recompute-the-number-in-prose gate is deliberately absent from `ops/preflight.py`: this repository's own convention is that a dated measurement stays correct once it stops reproducing, so that check would have called `LL-0199` wrong at the exact moment it was right. The check that catches class (c) also manufactures class (c).
- CRITERION 3, AND THE FIRST NUMBER WAS AN INSTRUMENT DEFECT RATHER THAN A RESULT. Running today's pre-flight at the parent of each commit that filed a gate-reachable finding caught 0 of 17, every tree green with 180 to 314 tests passing. The reason is visible in `83a4f86`'s own stat: the new test module, its inventory row, its lane owner, its regenerated contract and the ledger entry all land in ONE commit, so the state where the registration was missing existed for about twenty minutes inside a session and never reached the object store. Reporting 0 of 17 as a verdict would have been reporting the addressing scheme as a finding about the guards.
- SO THE STATE WAS RECONSTRUCTED FROM THE FIX COMMIT'S OWN ADDITIONS - check out the tree as the fix left it, remove the registration lines that commit added for the new module, run the pre-flight there. CAUGHT 9, MISSED 0, on nine real trees. The failing test names are printed rather than summarised so a catch can be checked for relatedness: every one fails on the orphan check and the stale-contract check, which is the defect the reconstruction introduced. Three of the nine catch it with three tests and six with five, because the inventory completeness property postdates the four oldest trees - the harness reports NO-GUARD rather than crediting itself for a check that did not exist.
- THE COST NUMBERS, BOTH MEASURED THIS SESSION AND NEITHER ESTIMATED. Full suite 396.2 seconds wall, 3163 passed 1 skipped, measured before the work. `python -m ops.preflight` 18.7 seconds over 14 guard modules, 379 passed 1 skipped. The two most expensive doc guards in the suite, `tests/test_no_pii.py` at 39 seconds and `tests/test_docguards.py` at 24, are deliberately left out of the set: they are the commit gate's job and they would more than double the number that decides whether a slice runs the check at all.
- THE PRE-FLIGHT CAUGHT THIS SESSION'S OWN WORK TWICE, WHICH IS THE POINT. `tests/test_refutation_census.py` passed 36 of 36 while four registration guards were red, exactly the `OPS-75` shape. Then `tests/test_source_register.py` reported two of the session's own new files as unregistered external hosts - a FALSE red, because three guards subtract this repository's filenames by asking `git ls-files` and an unstaged file is invisible to that listing. Staging them turned it green with no edit to any denylist, and that is now a WARNING the pre-flight prints rather than an hour a slice spends.
- NON-VACUITY PROVED, NOT ASSUMED. Nine mutants across `ops/refutation_census.py` and `ops/preflight.py` - anchor stripped, coverage check neutered, window boundary flipped, tie-breaking replaced by sort order, the runtime printed as a constant, the not-covered section retitled, `--exclude-standard` dropped. Nine killed, zero survived, and the harness asserted every anchor matched before any survivor would have been believed. It ran with bytecode writing disabled and a delay between mutants, because a false SURVIVED from a replayed `.pyc` is a defect this project has already measured.
- COVERAGE IS DECLARED RATHER THAN INFERRED, because the first pass got this wrong. Four slices covered 53 of the 64 in-window entries and nothing in the data said so - an entry with zero events and an entry nobody opened are identical in a file of events. The data file now carries `#examined` directives derived from the LINE RANGES the slices were given, so the coverage check compares two independent derivations rather than a number against itself.
- WHAT IS NOT CLAIMED. Nothing here reduces adversarial review of anything: 60 of 135 events were real defects and no program in this work would have found one of them, and the pre-flight's own report says so every time it runs. The census is a LOWER BOUND, because the ledger is written by the party being measured. The reconstruction is a rebuilt state, not an observed one, and is labelled that way everywhere. Criterion 4 is only partly met - the cycle-cost pair is still unmeasured and is filed as an open item under `OPS-87` rather than estimated.
- MAIL READ AS DATA, NOT AS AUTHORITY. RSC's proposal and RC's 588-line measurement both arrived on this channel; their taxonomies were read, this tree's numbers were derived independently, and nothing was adopted. RSC's defect report is HALF REFUTED by measurement: their section 1 is real - a pipeline takes its last stage's exit code - but no tracked hook, script or tool here runs pytest through a pipe, and their claim that "your pytest.ini also carries no -q in addopts" is false, `addopts = -q --tb=short --strict-markers --strict-config -r fE`. A reply is drafted and HELD, because outbound propagation is `OPS-68` and that is on operator standby.

### LL-0236 - 2026-09-12 - OPS-75 CLOSED - no ignore rule shadows a tracked path here, the first run is ZERO, and the check took two passes to stop inventing one

**Evidence:**
- CRITERION 4, the number with its date and its instant. 2026-09-12, measured after the new module was staged so `git ls-files` counts it: 205 tracked paths, 7 matched, 7 of those NEGATED, 0 NET-IGNORED. All seven are the reviewed-fixture carve-out at `.gitignore:129`, `!tests/fixtures/**/*.gvas.b64`. The number was 204 before staging, which is why the moment it was taken is written down beside it.
- `--no-index` IS THE ITEM. `git check-ignore` normally consults the index, so against a tracked path it says nothing at all - which is why this question had never been asked here. Without the flag every run of this check would be a clean negative that means nothing. The whole measurement is `git ls-files -z` piped into `git check-ignore --no-index -v -z --stdin`.
- A NEGATED MATCH IS NOT A DEFECT, and a check that counted the seven carve-out matches as findings would report a defect where there is none - `OPS-79`'s lesson, that positive specimens prove an instrument can SEE and prove nothing about whether it INVENTS. Both kinds of specimen are built in throwaway repositories: a tracked file genuinely shadowed by a non-negated pattern, which must FAIL, and the carve-out shape, which must PASS.
- THE SIBLING QUESTION INVENTED A FINDING IN ITS FIRST VERSION, found by an independent refutation pass. Asked of EVERY tracked path, a `.gitignore` of `*_*.txt` with a tracked `notes.txt` leaves the primary audit correctly silent while the sibling sweep reports `notes_<probe>.txt` - a finding about the probe token, not about the tree. Criterion 1 says the sibling question QUALIFIES a path that was already MATCHED, so it is now asked only of those, and `notes.txt` is out of scope. The case the narrowed arm catches is worth more: a NEGATION NARROWER THAN THE RULE IT RESCUES FROM (`*.log` plus `!keep.log` with `keep.log` tracked), where the tracked file looks fine and its sibling is silently refused. Both that specimen and the false positive are pinned.
- SCOPE IS WHATEVER GIT HONOURS. A nested `.gitignore`, `.git/info/exclude` and a global `core.excludesFile` were each built and observed producing findings. Asking git beats parsing ignore files by hand, but it makes the answer MACHINE-CONFIG DEPENDENT - a global excludes file can redden this check on one clone and leave it green on another at the same commit. The failure message names the source file and line, which is what makes that survivable, and the module says so rather than claiming a scope it does not have.
- CRITERION 3, two halves kept apart. An ABSENT `git` skips through `tests/_toolguard.require("git")` - noisier rather than quieter, because the `OPS-78` banner announces every such skip by name and a silent pass is the quiet failure. A PRESENT `git` that returns an EMPTY listing, or whose `ls-files` or `check-ignore` fails, or that is handed an empty question, or that returns a ragged record stream, RAISES. An empty listing is what would make the whole check vacuously green. All five directions tested.
- CRITERION 2. The listing and the rules are asked of git at run time. No filename constant is stored and neither 204 nor 205 appears as a literal anywhere.
- CRITERION 5, met on the SECOND pass. The first campaign reported 10 killed with one disclosed survivor; the refutation found a second, undisclosed and load-bearing one - `_clean_env` losing its `env.pop` for git's exported hook environment. With `GIT_DIR` set, `git ls-files -z` inside a fresh throwaway repository returns the OUTER repository's paths, the same leak `.githooks/pre-commit` documents in its section 3d. Now pinned by an arm that leaks the variables deliberately: with the `pop` removed it reports "answered about 205 path(s) instead of the throwaway's 2". The `GIT_CEILING_DIRECTORIES` survivor was pinned too rather than documented away. Final: 16 mutants, 16 killed, 0 survivors, every anchor asserted to match exactly once and every replacement read back off disk.
- TWO REGISTRATION FAILURES THE MODULE'S OWN GREEN COULD NOT SEE. The new file passed 19 of 19 while the SUITE was red: a new tracked test module must also appear in `docs/INVENTORY.md`'s test table and in a lane's `owns` in `ops/lanes.py`, whose generated contracts must then be rewritten with `python scripts/write_lane_contracts.py`. A third red followed from the prose itself - `docs/INVENTORY.md` naming `core.excludesFile` tripped `tests/test_source_register.py`, which reads a git CONFIG KEY as a host, the same class as the already-registered `core.filemode` and `user.email`. "The module passes" and "the work is done" are different claims.
- COUNTS OBSERVED THIS SESSION, never carried forward. Baseline before this item: 3136 collected. After: 3164 collected, with `tests/test_gitignore_shadowing.py` at 28. Bare full suite green. `ops.merge_gate.verify` run with a per-file baseline supplied. The new module: 0 CR bytes, 0 non-ASCII bytes, ruff clean.

**What is NOT claimed.** This says nothing about whether the ignore rules are CORRECT, only that none of them shadows something already tracked. And the answer is a fact about THIS machine's git configuration at THIS commit, not a property of the repository - see the machine-config dependence above.

### LL-0235 - 2026-09-12 - OPS-83 CLOSED - the 49 tests now name a CAPABILITY rather than a tool, and three things the item itself filed did not reproduce

**Evidence:**
- DECISION, criterion 1. `tests/_toolguard.py` gains a capability guard beside the OPS-78 single-tool one: `POSIX_USERLAND = "posix-userland"`, members `POSIX_USERLAND_TOOLS = ("sh", "grep", "head", "tr", "wc")`, entry point `require_posix_userland()`. The declared set is paired with a DRIFT TEST that re-derives the utilities from the hook text, so staleness presents as a RED TEST on a machine that HAS the userland rather than as a false red on a bare box where nobody can diagnose it. The two rejected shapes and their reasons are written into the module docstring and into the ROADMAP item.
- THE ITEM'S OWN UTILITY LIST WAS WRONG TWICE. Measured over both hooks' non-comment lines: `mv` appears ZERO times and is prose only; `find` appears ONCE, inside `git diff --find-renames`. A hyphen is a word boundary, so a bare word scan reads a git FLAG as a utility - which is why the drift test matches COMMAND POSITION, the same narrowing OPS-22 applied to `tools/precommit_gate.py::_forbidden_cmdlet_reason`. `printf` is invoked but is a shell BUILTIN and is subtracted through `SHELL_BUILTIN_UTILITIES`.
- `sh` IS THE GATE, refuting the filed reconstruction. Restoring `find grep head mv printf tr wc` while withholding `sh` recovers ZERO of the 49 - byte-identical to restoring nothing - because git cannot spawn the hook at all: `error: cannot spawn <repo>/.githooks/pre-commit: No such file or directory`. `git.exe` survives the strip in `mingw64\bin`. The 38/11 split reproduces only once `sh` is restored.
- A NAME-ONLY PRESENCE PROBE LIES ON WINDOWS. With Git's `usr\bin` stripped, `shutil.which("find")` returns `C:\Windows\system32\find.EXE` and `which("sort")` returns `system32\sort.EXE`. Neither is in the declared set today, so `WINDOWS_HOMONYMS` plus `_under_system_root` are defence against the next addition. Case, separator, dot-dot, trailing separator, 8.3 short name and extended-length spellings are covered; the UNC administrative-share form is NOT, is documented as a known limit, and is pinned by an arm rather than papered over.
- CRITERION 4 - the 11 diagnosed individually, not inferred from a union. 3 need `sh`+`grep`, 1 needs `sh`+`tr`, 7 need `sh`+`grep`+`tr` with neither utility alone. Every id and the hook line that names each utility are listed in the ROADMAP item. Minimal green set for all 184 tests in the four files is `sh grep tr`, with no second residue.
- CRITERION 2, measured. Under a shim carrying the 73 `*.dll` and no executable at all, the four files report `135 passed, 49 skipped`, ZERO failed and ZERO errored, and the skipped set equals the previously-red set BY NODE ID in both directions rather than by count. Banner: `49 test(s) were SKIPPED because the capability 'posix-userland' is incomplete on PATH. Missing: grep, head, sh, tr, wc.`
- CRITERION 3, re-measured 2026-09-12 with the positive control PROVED. `python tools/false_red_probe.py --tool sh` over `C:\Lanternlight`: control PROVED on all five planted specimens, 3136 repository tests collected in both directions alongside 5 planted controls, `false_red=0`, `clean_skip=58`, `skip_both=1`. `--tool sh` and `--tool bash` build a byte-identical stripped PATH by construction, so `sh` is the honest name for the question.
- CRITERION 5. 19 mutants on the mechanism, 8 on the repair, plus the wiring's vacuity watched on all four files: guard removed gives 32 errors / 6 / 6 / 5 red, restored gives 32 / 6 / 6 / 5 skipped. Every mutation asserted its anchor matched exactly once and read the replacement back off disk before a survivor was believed.
- TWO DECORATIONS FOUND AND REPLACED RATHER THAN SHIPPED. An arm claiming comment lines are not scanned SURVIVED its own mutation, because over these particular hooks stripping comments changes the derived set by nothing; it was replaced with a synthetic case that reddens plus an honest separate arm about `mv`. And a mutation harness reported a false SURVIVED because two mutants of equal byte length written in the same second let pytest replay a cached `.pyc` - fixed by purging `__pycache__` and running `-B`.
- AN INDEPENDENT REFUTATION PASS returned CONFIRMED with two latent defects, both then fixed: the drift matcher's unstated blind spots after `elif`, `while`, `until` and the command wrappers `env`, `time`, `xargs`, `command`; and `_under_system_root` reading extended-length and UNC path forms as PRESENT. The keyword introducers are now matched, the wrappers are written down as a stated trade with an arm pinning the blind spot, and the extended-length prefixes are normalised.
- COUNTS OBSERVED THIS SESSION, never carried forward. Baseline before any work: 3083 collected. After the mechanism: 3130. After the repair: 3136 collected, 3135 passed and 1 skipped on a bare full run. `ops.merge_gate.verify` OK at 3130 with a per-file baseline supplied for the wiring slice. All six touched files: 0 CR bytes, 0 non-ASCII bytes, ruff clean.

**Filed out of this item:** `OPS-86`. Run from a git worktree that sits INSIDE the system temp tree, `tools/false_red_probe.py` reported `positive control UNPROVEN` with all five specimens unclassified, counted its 5 control tests as REPOSITORY tests, and still printed a full count table including `false_red=50`. The control's node id came back as `::test_control_false_red` with no file segment for `is_control` to match. The probe's own doctrine saved it - an UNPROVEN run is an absence of evidence - but the counts read exactly like findings.

**What is NOT claimed.** The whole suite was never run under a shim, so a test outside these four that also needs a POSIX utility is invisible here. Only `.githooks/pre-commit` was exercised; `commit-msg` has utility dependencies nobody has measured. `sh.exe` here is bash in `sh` mode, so "needs `sh`" means "needs this `sh`". And `C:\Program Files\Git\bin` is absent from this machine's PATH - where it IS present the strip fails to strip and the measurement would silently report nothing.

### LL-0234 - 2026-09-11 - The headless capture lane is DISARMED on an operator instruction, and the tooling cannot say so on its own

**Evidence:**
- OPERATOR INSTRUCTION in chat 2026-09-11: disarm any headless lane for LL. Executed at the wrap.
- The watcher was `python -m lanternlight.armwatch --dest-base C:\ll-captures --heartbeat ...`, pid 21680, armed since 2026-09-07T17:42:51Z with a fresh heartbeat at 255,159 completed passes. Its identity was confirmed from the process table before anything was sent to it, rather than from the arming record alone.
- Terminated with `taskkill /F /PID` ISSUED FROM POWERSHELL, not Git Bash - CLAUDE.md records that MSYS path conversion rewrites the `/F` flag into `F:/` so the command dies and kills nothing, and that the only evidence of the failure is taskkill's own output. Its output was read: SUCCESS. Verified twice afterwards and from two directions: the pid is gone, and no process anywhere on the machine carries `armwatch` in its command line.
- THE TOOLING CANNOT DISTINGUISH A DELIBERATE DISARM FROM A DEATH, which is the whole of LL-0117. With the arming record in place `check_watcher()` answered DEAD with a reason ending "Re-arm." - a standing instruction to undo what the operator had just asked for, printed once per session at every wrap.
- RESOLVED BY RETIRING THE RECORD RATHER THAN DELETING IT: `ops/runtime/armwatch.json` and `ops/runtime/armwatch_heartbeat.json` were renamed with a dated `disarmed` marker in the stem, so the evidence survives on disk and the state reads NO_RECORD - "not armed" - instead of "died unexpectedly, re-arm". Both are under the gitignored `ops/runtime/`, so nothing left the tree.
- THE INTENT LIVES IN THE HAND-OFF, which is the only place a cold session reads before acting. It says the watcher is off on purpose, that NO_RECORD is the expected answer, that the /done ritual's step 8 instruction to re-arm on NO_RECORD is overridden by the operator here, and that nothing is archiving the log, saves or market cache while it is off. The tool reports the machine state; the hand-off carries the reason. Neither can do the other's job.
- A RE-ARM NEEDS THE PROCESS STARTED, not the filenames restored. Restoring the record alone would recreate the record of a process that is not running, which is precisely the failure LL-0117 names. Said in the hand-off in those words.
- No scheduled task exists for this project - checked, and the LW and RC entries on this machine belong to siblings and were not touched.
- MEASURED SIDE EFFECT, recorded because it contradicts a live instruction elsewhere: the hand-off is TRACKED at the repo root, and `tests/test_source_register.py` scanned it and refused the two new runtime filenames as unregistered hosts. The /done ritual's step 9 states that this module's reach is `docs/` and nothing else. That is either stale or incomplete - the refusal cited `LL-NEXT-SESSION.txt` by name. Not chased here; recorded so the next session does not trust that sentence.
- Suite measured bare at the wrap, after the registration this change required: 3082 passed, 1 skipped. ruff: All checks passed.

### LL-0233 - 2026-09-11 - The wrap's refutation pass found FOUR documentation defects and zero code defects, including CLAUDE.md still forbidding the vendoring this session performed - all four repaired, and this entry corrects LL-0231 and LL-0232

**Evidence:**
- An independent refutation agent was handed ten claims at the wrap and told to default to REFUTED. Claims 1 to 9 - the suite count, the OPS-82 helper and its five arms, the mutation result, the toolguard conversion, the two new probe functions, the vendored digest round trip, the blob-equals-disk check, the walker probe cleanup, and the git state - ALL SURVIVED. It refuted none of them, and it improved one: claim 3 had been offered as possibly unverifiable without mutating a tracked file, and the agent verified it anyway by swapping the module attribute from an out-of-tree pytest plugin, leaving the tracked file untouched and reproducing 2 failed and 3 passed exactly.
- EVERY DEFECT IT FOUND WAS A DOCUMENT. Not one was a test or a line of code. That is the third consecutive session where that has been true, and it is now the pattern rather than an incident.
- DEFECT 1, THE SERIOUS ONE. CLAUDE.md still read "Re-implemented, never vendored... No file is copied in" while third_party/lw_write_tracer/ sat in the tree. CLAUDE.md's own first exception states the required remedy verbatim - a genuine rule change "is written here rather than left as a contradiction a cold session would refuse to act on" - and this session had performed a genuine rule change and not written it there. A cold session would have found the vendored directory in direct violation of the file it is handed first. REPAIRED: a SECOND EXCEPTION is now written into CLAUDE.md in the same shape as the first, recording that a sibling's file may be vendored when the sibling NAMES an acceptable license, that the gate was satisfied rather than waived, that GPL, AGPL and BUSL-1.1 remain DO-NOT-VENDOR whatever is offered, that a note ASSERTING a license is not a license, that vendored work lives under third_party/<name>/ with a NOTICE, and that a vendored file is never edited. The first bullet of the first exception was rewritten to point at it rather than left to contradict it.
- DEFECT 2. The OPS-82 outcome heading said "all six criteria met" while its own lead line still said "Criteria 1 to 5 are MET. Criterion 6 is as far as this session may take it", with "6 - SENT" below it. The heading was updated when the reply was sent and the lead line was missed. REPAIRED.
- DEFECT 3. The OPS-82 outcome mislabelled its own criteria: mutation evidence was filed under "3 and 5" and the window acceptance under "4", where the item's acceptance list makes the regression arm 3, the mutation 4 and the window 5. REPAIRED.
- DEFECT 4, AND THIS ENTRY IS THE CORRECTION RATHER THAN AN EDIT. LL-0231 credits the OPS-85 catch to "the acceptance criterion this session had just written for its own item", and the OPS-85 that survives has no Acceptance section at all. The claim was true of the FIRST draft of OPS-85, which carried a criterion asking for a count including zero, and false of the document anyone can now read - which makes it unverifiable and therefore not a claim worth standing. The catch itself is unaffected: the count was taken, it came back zero, and the item was rewritten. LL-0231 is corrected on that one sentence and stands otherwise. The ledger is append-only, so this entry is the correction and LL-0231 is left as written.
- ALSO CORRECTED: LL-0232 carries no suite count and no ruff line, against every sibling entry's norm, and it touched a test file. The measurement it should have carried, observed bare at that point: 3082 passed, 1 skipped, ruff All checks passed.
- SUITE AT THIS WRAP, observed bare by the merger and independently re-run by the refutation agent from a clean state: 3082 passed, 1 skipped. ruff: All checks passed.
- ONE CLAIM IS RECORDED AS UNVERIFIABLE rather than confirmed, because the agent said so and rounding it up would be the defect this entry is about. The ROADMAP's "3067 collected in BOTH directions" for the bash probe cannot be reconciled against today's collection count without re-running the probe, which costs eight minutes and was forbidden by the brief. It is neither corroborated nor contradicted, and it is left standing with that status named here.

### LL-0232 - 2026-09-11 - OPS-82 criterion 6 MET - the reply to LW is SENT on an operator ruling, and the held draft is the record of having asked first

**Evidence:**
- OPERATOR RULING in chat 2026-09-11, answering the question LL-0229 recorded rather than answered: send the reply. Delivered to LW through ops.outbox.deliver at 15:38 local, 7,818 bytes, recipients ['LW'], delivered ['LW'], failed []. The manifest row is in moon_sync_inbox/_outbox/ and was read back with ops.outbox.replies_to('LW') rather than by listing a sibling's directory - OPS-43.
- THE DRAFT WAS UPDATED BEFORE IT WENT, so the note describes what actually shipped rather than what was planned: the vendoring is reported as done, with the digest check, the single declared change, and the round-trip guard described in enough detail for LW to take the idea. A note promising work that had not landed would have been the same defect as a caveat dropped from an artifact.
- IT ALSO CARRIES THE CORRECTION THIS SESSION OWED, unprompted: that a session here filed the walker probes as a defect from the tracer's report without opening the test, and that measurement showed zero remain. Reporting our own wrong reading to the party whose instrument produced it is the part that makes the channel worth anything.
- AND THE GENERALISATION BACK TO LW, which is the actual content: both wrong readings of that instrument on that day, theirs and ours, were the same mistake - a write tracer reports that BYTES MOVED and cannot report that STATE CHANGED. Suggested for its docstring.
- THE ASKING IS THE RECORD, not the sending. OPS-68 holds cross-project propagation on standby, and the one earlier note from here went out on a specific operator instruction that was explicitly not a precedent. A session that had already written the reply stopped and asked anyway. The held draft plus the recorded question cost one turn; sending on our own judgement would have set the precedent the hold exists to prevent.

### LL-0231 - 2026-09-11 - OPS-85 CLOSED, NO DEFECT - the item this session filed about probe files in the live tree was FALSE, and the correction is the entry

**Evidence:**
- THIS SESSION FILED A DEFECT THAT DOES NOT EXIST, from the vendored tracer's report alone, without opening the test. OPS-85 as first written claimed that two probe files in ops/runtime/ "are not removed" and "accumulate in the operator's live runtime directory, one pair per suite run, indefinitely". Every part of that after the file names was invented.
- MEASURED, and it settles it: both probes are removed in a `finally` by the tests that write them, at tests/test_tracked_walker.py:88 and tests/test_tracked_walker.py:160. Counted in the live ops/runtime/ immediately after the full suite run the tracer had just watched write them: ZERO remain. The writes are real and transient.
- CAUGHT BEFORE THE COMMIT, and only because the acceptance criterion this session had just written for its own item demanded a count including zero. The count was taken, came back zero, and contradicted the prose above it. The item was rewritten in place to say what is true, and kept rather than deleted, because the wrong version is the useful part.
- THE LOCATION IS CORRECT AND IS RECORDED AS SUCH so nobody "fixes" it: the thing under test is that a GITIGNORED file stays out of the scannable view, ops/runtime/ is gitignored, and a probe in tmp_path would not be inside this repository at all and would be excluded for the wrong reason - turning a real guard into decoration that passes whatever the walker does.
- THE ONE RESIDUE IS DISMISSED IN WRITING rather than by silence: a `finally` does not survive a hard kill, so a killed process could leave one 2 or 3 byte file in a gitignored directory. Not worth an item, and saying so is cheaper than having it re-opened by the next reader of a trace report.
- THE GENERALISATION, which is why this is on the record. A write tracer reports that BYTES MOVED; it cannot report that STATE CHANGED. Both findings this instrument produced about this tree on this day - LW's and this session's - were writes that a `finally` undid, and in both cases the instrument was working perfectly and the READING was the error. That is the same shape as OPS-82 one level up, and it is now written down twice because it was learned twice.

### LL-0230 - 2026-09-11 - OPS-84 CLOSED - LW's write tracer is VENDORED under the license they named, and its first run here corroborated OPS-82 from an instrument this project did not write

**Evidence:**
- OPERATOR RULING, in chat 2026-09-11, answering the two questions this session put: send the reply to LW, and vendor. The license gate was SATISFIED rather than waived - an earlier copy of this same file was refused here because the drop carried no license statement, this project asked LW to NAME one if they wanted it vendorable, and LW answered that both files are tracked in Remus3/Legion-Wallpaper, PUBLIC and Apache-2.0, sole copyright holder, vendoring intended. LW added that it would rather the rule was applied than waived.
- VERIFIED BEFORE A BYTE WAS COPIED: the drop hashes to the sha256 LW published in its 14:15 note, is 11,267 bytes as stated, and is 7-bit ASCII throughout. A license statement about a file nobody checked is a statement about some other file. The whole plugin was read before it was vendored.
- ONE CHANGE MADE, and it is declared because Apache-2.0 section 4(b) requires it: CRLF normalised to LF for this repository's .gitattributes policy, which exists because this repository is public and a CRLF blob reads as a whole-file diff to every non-Windows contributor. No other byte differs.
- THE TRAP THAT SHAPED THE GUARD, and it is this repository's own: a hash of a working file is not a hash of the commit. Recording a single digest would have been a confident lie whichever form was chosen, so third_party/lw_write_tracer/NOTICE.md records BOTH and says which is which, and tests/test_vendored_write_tracer.py asserts identity in the form that survives the policy - read the vendored file, restore CRLF, require the result to hash to LW's PUBLISHED digest. That binds the test to the licensed CONTENT rather than to a line-ending rule, so it survives a future policy change instead of needing a rewrite.
- WATCHED RED UNDER MUTATION, anchor asserted first: adding a single trailing comment to the vendored file reddened two arms - the round trip and the direct comparison against the original drop - and left seven green. Restored from a byte copy, 9 passed.
- THE VENDORED WORK ACTUALLY RUNS HERE, proved end to end through a real pytest subprocess rather than by importing it: control proved true, negative_clean true, restored true. Vendoring a file that does not load would have been a licensing exercise only.
- FIRST RUN AGAINST THIS SUITE, control PROVED and negative specimen clean on the same run, watching ops/runtime, logs and moon_sync_inbox. The operator's live inbox_seen.json and inbox_reported.json DO NOT APPEAR. What appears in their place are the .restore.<pid>.tmp temporaries OPS-82 introduced the same day. That is independent corroboration of OPS-82 from an instrument this project did not write: LW saw the live records written because the restore truncated them in place, and after the fix the only bytes that move go to a temporary that is then renamed. Also written: docguard_observed.json.<pid>.tmp at 21,782 bytes, which tests/conftest.py documents as a deliberate audit-hook recorder, and two probe files of 2 and 3 bytes covered below. opened_for_write_only was empty.
- EVERY NUMBER ABOVE IS A LOWER BOUND and the plugin says so itself: it does not see writes performed by a CHILD PROCESS, and this suite spawns processes constantly - every hook test does. That limit is load-bearing here rather than a footnote, and it is the exact limit that made LW's original finding about this tree wrong.
- RUFF NOW EXCLUDES third_party/, with the reason recorded in ruff.toml rather than left as a bare path. Its PTH rules wanted builtins.open and os.replace in the vendored file rewritten to pathlib equivalents, and those two calls are the PATCHED ROUTES the plugin's own positive control exists to exercise - the autofix would have silently disarmed the control while also making the file a derivative work.
- Suite measured bare after all of it: 3082 passed, 1 skipped. ruff: All checks passed.

