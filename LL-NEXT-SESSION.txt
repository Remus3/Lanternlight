LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-10 session. Overwritten every session; this
file is tracked, and its git history is the record of what each session handed
forward. It is written by ops/handoff.py, never by an editor tool.

Resume Lanternlight from disk. Read CLAUDE.md, then this file, then ROADMAP.md
and docs/LEDGER.md newest-first. Ask nothing; start work.

READ FIRST, IN THIS ORDER
  CLAUDE.md           - unchanged this session.
  docs/LEDGER.md      - newest first. LL-0207 and LL-0208 are this session.
                        Read LL-0207 first if you read only one: the refutation
                        pass broke BOTH of that item's closures before they
                        landed, and neither finding was a count.
  ROADMAP.md          - OPS-69 and OPS-70 CLOSED, OPS-71 filed, OPS-35
                        criterion 1 met with the item still OPEN.

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. A plain run REPORTS ONLY.
Acknowledge with `python ops/inbox_watch.py --acknowledge`, and only for notes
you have actually read. The UserPromptSubmit hook acknowledges ONCE per session
on the operator's own turn, so notes may be marked seen before you look - that
is OPS-41 by design and is NOT the same as your having read them.

SECOND ACTION
Run `python ops/stop_audit.py --show-last`. A FAIL line is a QUESTION about what
the last turn wrote, not a verdict - it cannot tell a claim from a QUOTATION of
one.

HELD BY THE OPERATOR - DO NOT ACT ON ANY OF THESE ALONE
OPS-68. The responder runner and cross-project propagation are on STANDBY by the
operator's own words on 2026-09-08: "standby on this decision then, i will keep
it to the test for now", answered to BOTH the authority question and the
propagation question. NOTHING WAS BUILT AND NOTHING WAS SENT, this session
included. Do not start building one, and in particular do not build "the safe
version first" - that is a session choosing a default and calling it modesty.
The four runner shapes and three propagation shapes are recorded verbatim in
OPS-68 so the standby resumes from the same menu.

OPS-48 remains HELD on RC's and RSC's results - do not answer its four questions
and do not solicit RC or RSC.

OPS-35 and OPS-36 are OPEN and operator-RULED: the operator ruled ADOPT, so the
DECISION is settled and the acceptance criteria are ours to meet. That is not
the same as the two items above, and this session acted on OPS-35 accordingly.

THREE RSC NOTES WERE READ THIS SESSION AND NONE WANTS A REPLY. All three say so
explicitly, and one exists purely to correct RSC's own broadcast error. They
confirm the standby is ORDERED rather than dissent, and ask that nobody later
cite our silence as a position we did not take. Their one transferable idea -
build a guard in BOTH directions or it reports clean against the half it cannot
see - is the same finding as OPS-69 and arrived independently. Nothing was sent.

WHAT CLOSED THIS SESSION - two commits, 49dc2a6 and 1e4b4d1, both pushed
  OPS-69  CLOSED. docs/INVENTORY.md was guarded in ONE direction. The reverse
          property now enforces a stated scope. The item predicted three missing
          modules; the guard found 37, and tools/ had no section at all.
  OPS-70  CLOSED. tools/hook_command_guard.py now reads permissions.* as well as
          hooks.*, and its OK line states its own scope. Criterion 1 was answered
          with an explicit DECLINE - see OPS-71.
  OPS-71  FILED. The portable successor to the three absolute permission rules.
  OPS-35  criterion 1 MET, item still OPEN. Nothing under moon_sync_inbox/ can
          reach git. Criteria 2 through 6 untouched.

THE REFUTATION PASS EARNED ITS COST TWICE, AND NEITHER FINDING WAS A COUNT.
This is the change from the last four sessions, where the wrap kept catching
numbers in prose. Here it caught BEHAVIOUR:

  1. OPS-70's first closure shipped a WORKING BYPASS of the property it claimed.
     A permission rule with a TRAILING SPACE was never scanned, because the
     splitter required a closing parenthesis and returned the rule unsplit when
     it found none. The UNC backslash spelling was missed because the permission
     side held a hand-written near-copy of the UNC pattern instead of the
     pattern itself. Both are the item's own shape - a guard whose green line
     answers a narrower question than a reader assumes - reappearing INSIDE the
     fix for it.
  2. The guard was PRINTING "Measured for OPS-70" in its user-facing finding
     text when nothing had been measured. That is the worst place in the file
     for a false provenance, because it is read by someone who has just been
     told something is wrong. MATCHER_CLAIM_PROVENANCE now says it is an
     inference, and a test asserts the word "measured" is absent from the detail.
  3. OPS-69's new guard was TOO LOOSE FOR THE SENTENCE STANDING IN FRONT OF IT.
     docs/INVENTORY.md claimed every tests/test_*.py appears in one of its two
     tables; that was FALSE, and the omitted file was tests/test_inventory.py
     itself. The guard missed it because it asked whether a name appears
     ANYWHERE while the sentence claimed the TABLES. There is now a test
     requiring a table row and a second test pinning the sentence to the guard.

THE MERGER'S OWN REFUTATION WAS REFUTED TWICE, BY THE MERGER. Two independent
re-probes of the UNC repair reported BYPASS and both were WRONG: the probe's
escaping lost a backslash, so the string under test was not a UNC path at all.
Built with chr(92) and verified by its on-disk repr, the guard catches it. An
earlier probe of the inventory returned "80 of 81 missing" - also the probe, a
naive backtick regex desynchronizing on fenced ``` blocks. BOTH TIMES THE FIRST
INSTINCT WAS TO DISBELIEVE THE AGENT RATHER THAN THE PROBE. A result too extreme
to be plausible is a claim about your pattern.

THE COMMIT GATE CAUGHT WHAT A GREEN FULL SUITE COULD NOT, and this one is worth
carrying forward as a technique. "git.py" was added to KNOWN_NON_HOSTS while
tests/test_no_inbox_in_git.py was still UNTRACKED, and the full suite agreed it
was needed. STAGING the file made is_repo_filename cover the fragment, the entry
became redundant, and the pre-commit hook refused the commit. git ls-files
answers differently before and after staging, so a guard consulting it returns a
DIFFERENT VERDICT at suite time than at commit time. That is why the hook runs
the doc-reading subset against the STAGED tree. IF YOU ADD A DENYLIST ENTRY FOR
A FILE YOU ARE ALSO CREATING, STAGE FIRST AND THEN DECIDE.

A WRONG DATE REACHED TWO FILES IN EIGHT PLACES - a repair slice stamped
2026-09-09 for work done on 2026-09-10. Caught at the merge against the machine
clock in local AND UTC, so no midnight ambiguity could excuse it. Note for the
next wrap: this project dates by LOCAL time - the machine is UTC-5, so a session
working late is already on the next UTC day while its ledger entries are not.

NEXT ITEM - OPS-35 criterion 2, WHICH IS A DECISION GATE AND NOT A TASK.
The question is recorded inside OPS-35 itself, immediately after the acceptance
list, so you read it beside the criterion. In short: criterion 2 wants the lock
protocol in our own words, steers away from reading a sibling's source for it,
and suggests asking RC instead - but OPS-48 forbids soliciting RC, and OPS-68
put cross-project propagation on standby. BOTH ROUTES ARE CLOSED TO A SESSION
ACTING ALONE. Do not resolve this by picking the reading that lets you proceed.
Criterion 5 is separate and cannot be met by any session that cannot arrange a
REAL sibling holder; that criterion already says the item stays open if so.

IF THE GATE IS STILL UNANSWERED, the actionable queue is EMPTY of anything that
does not need the operator or the game:
  OPS-71  needs a session in DEFAULT permission mode, in a clone at another
          path. It CANNOT be settled in bypass permissions mode by any probe -
          that is the whole reason it exists as an item.
  5, 6, 7 blocked on the game. NO NEW GAME DATA SINCE 2026-08-30. Do not
          schedule a capture session; fold those into whichever session next has
          the game running.
Ask, do not guess:
    python -c "from ops.loop import watch; print(watch.check_watcher().reason)"
and read it as an UPPER BOUND.

STATE AT THE WRAP - RE-DERIVE RATHER THAN QUOTING
  suite             2835 passed, 1 skipped on the working tree; 2836 collected.
                    Run it bare - pytest.ini already carries -q, so a second one
                    prints NO summary line at all and still exits 0.
  baseline          2828 at this session's last dispatch. Measure your own with
                    `python -m pytest --collect-only -q` before dispatching; a
                    count checked into a file goes stale and becomes a confident
                    lie.
  ruff              All checks passed.
  origin/main       1e4b4d1 at the last push, plus whatever this wrap commits.
                    NOTE main has NO UPSTREAM in .git/config, so a bare
                    `git push` FAILS and its advice text reads like success when
                    piped to tail. Use `git push origin main`, then re-derive
                    with `git rev-parse HEAD origin/main`.
  loop              in_flight empty; all slices retired.
  capture watcher   ARMED, identity confirmed, heartbeat inside its threshold.
                    ARCHIVE state UNKNOWN, which is correct and is a THIRD
                    answer: the live process was armed by a build older than
                    OPS-59 and writes no archive map. Not a report that nothing
                    was copied.
  budgets           TWO CHANNELS, neither fired. Per-document: ROADMAP.md 2.1
                    sessions of headroom, docs/LEDGER.md 2.6 - both LOWER than
                    the 2.9 and 3.3 the last wrap recorded, and the next split is
                    close. When the per-document budget FIRES, RE-RUN
                    `python tools/doc_archive.py` and do NOT raise the number.
                    The OPS-62 PAIR channel reads about 10.9 and is a DIFFERENT
                    event with NO MECHANICAL REMEDY, because the pair total is
                    exactly what a split does not change. It is an OPERATOR
                    DECISION GATE. NOBODY RAISES A PAIR BUDGET SILENTLY.
  KNOWN_NON_HOSTS   five net new entries this session after one was added and
                    withdrawn, against four in the previous session. The RATE is
                    the finding, and it has a cause: OPS-69 and OPS-70 made this
                    project write more prose about its own files and config keys
                    than it ever has, and every dotted fragment parses as
                    host-shaped. OPS-63 refuted the pattern-level fix two
                    sessions ago at 45 retired against 52 introduced - that
                    measurement is stale and the input has changed.

BEFORE DISPATCHING PARALLEL WORK
Read SHARED_WORKTREE_BAN in ops/store_drift.py and put it in every slice brief -
it is ADVISORY and nothing enforces it. Record dispatches with state.dispatch(),
read state.in_flight_summary(state.load()) FIRST, and capture a per-file baseline
AT DISPATCH to hand the merge gate as per_file_baseline. A repository TOTAL is
safe for one worker and unsafe for several.

  DO NOT RUN TWO PYTEST PROCESSES AT ONCE. Give every slice a targeted run over
  its OWN test files and keep the full suite for the merge. Three pairs ran that
  way this session with no spurious red.

  RETIRE YOUR SLICES. state.retire() is ITEM-scoped, not lane-scoped.

  A NEW TEST MODULE NOW COSTS THREE EDITS, NOT ONE. It needs a row in
  docs/INVENTORY.md (OPS-69, or tests/test_inventory.py goes red), an owner in
  ops/lanes.py (or tests/test_lanes.py goes red), and a regenerated contract via
  `python scripts/write_lane_contracts.py` (or tests/test_lane_contract.py goes
  red). All three fired on this session's one new file, in that order, and all
  three were correct. Tell your slices this up front.

  A SLICE'S REPORT IS A HYPOTHESIS. Both implementation slices this session
  reported something to the merger that was not true - one said a module
  documented its own vacuousness when it did not, one claimed a bypass was fixed
  by a mechanism that was not the mechanism. Re-derive before writing anything
  into a closed section.

HOW TO WRITE THE NEXT HAND-OFF
    python ops/handoff.py --from-file <draft>
OMIT --target - OPS-65. The writer resolves its own root from its own file
location, so a wrap writes the hand-off of THE TREE IT IS WRAPPING. It runs the
redactor over the STRING and refuses BEFORE anything is written, so a refusal
leaves the previous hand-off byte-unchanged. --check-only reports without
writing.
