Lanternlight. Read CLAUDE.md, then ROADMAP.md and docs/LEDGER.md (top entries, newest first). Session default applies: orchestrated, multi-agent, parallel, self-adjudicating, self-adversarial. TDD. Measure the baseline with `python -m pytest --collect-only` before dispatching anything - do not trust a filed count, including the one below.

THE STANDING NEXT-SESSION SHAPE, kept READY and deliberately NOT started on 2026-09-16: a laned headless orchestrated parallel looping session over ROADMAP open items, running until drained or interrupted by the operator. The operator ruled in chat on 2026-09-16 that only RC and CS run headless loops that night, and said to keep this prompt ready in case they change their mind. So do NOT start a loop unless the operator says so in this session; work the items interactively instead.

STATE AT THE WRAP. Full suite `python -m pytest` bare: 3469 passed, 1 skipped, 0 failed. `--collect-only`: 3470. `python -m ruff check .`: All checks passed. `python -m ops.preflight`: PRE-FLIGHT PASS, 14 guard modules. Merge gate OK at 3470 collected against a per-file baseline of 3439 across 70 files taken at HEAD ad9fe90.

THE CAPTURE WATCHER IS DELIBERATELY DISARMED AND THIS WRAP DID NOT RE-ARM IT. `check_watcher()` returns NO_RECORD, which the wrap ritual lists as a re-arm state. It was not re-armed, on purpose: the operator disarmed that lane on 2026-09-11 and has never lifted it, docs/HEADLESS.md says in terms that it does not re-arm it, and the operator overrides the ritual. Nothing is archiving the game log, the saves or the market cache. That is the accepted cost recorded at the last several wraps - do not "fix" it without an operator instruction, and do not re-derive the reason.

WHAT LANDED THIS SESSION

1. OPS-92 archify DECLINED, OPS-93 context-mode DECLINED, OPS-94 timharris707/skills REVIEWED with nothing adopted. All three licenses were read directly rather than from a badge. Details and the full 24-row skills classification are in ROADMAP.md; the decisions and their reasons are in docs/LEDGER.md LL-0262 through LL-0267.

2. OPS-91 IS UNBLOCKED AND IS NOW THE OPERATOR'S CALL. RC answered on 2026-09-16 and named everything criterion 1 demanded: the repository BY NAME (Remus3/Amberstone), visibility PUBLIC probed live, root LICENSE Apache-2.0, the copyright LINE rather than only the license name, a SCOPE block putting authored documentation inside the grant, and a data/-only carve-out that docs/CHANNEL.md sits outside. RC also gave evidence, not assertion, that its 2026-09-07 three-way contradiction is resolved. CRITERIA 1 AND 2 ARE MET. Apache-2.0 into Apache-2.0 is what the gate accepts.
   IT DOES NOT AUTHORISE VENDORING. Criterion 3 reads "on a yes AND AN OPERATOR RULING to vendor", and THE ONE ASYMMETRY in CLAUDE.md puts adoption outside what any grant of authority reaches. The item is BLOCKED ON AN OPERATOR RULING, not on RC. RC states it is not asking, and that a NO VENDOR ruling with the license in hand is a legitimate outcome. If and only if the operator rules VENDOR: hash against RC's published digest BEFORE copying a byte, copy at the BYTE level and never through write_text, place it under third_party/ at the same relative path, and write a NOTICE naming upstream, license, holder, digest and every change, per Apache-2.0 section 4(b).
   DO NOT WRITE THE COPYRIGHT HOLDER'S NAME INTO A TRACKED FILE. It is the operator's git identity. ROADMAP.md records "one sole holder, named in both LICENSE and NOTICE" for exactly this reason. Note the nuance recorded there: the same literal DOES belong in LICENSE, NOTICE, CITATION.cff and README.md line 270, because Apache-2.0 requires the holder to be named. Do not "redact" those - it would strip attribution, not protect anyone.

3. THE OPS-91 CONFORMANCE GAP IS CLOSED, and the unreadable-inbox class was probed after a sibling REVIEW. ops/inbox_watch.py has a 10-name cap, a "+k more" pointer, and a gitignored report written atomically BEFORE stdout. RSC reported that class as three defects rather than one; re-measured here with a real PermissionError, whole-directory denial does NOT destroy state in this tree, but PER-ENTRY denial reproduced two defects, now fixed TDD with red observed first and seven mutants. An incomplete listing now derives no withdrawal and refuses to acknowledge.

4. The charter v4 worktree ordering invariant is CLOSED. Its guard was vacuous - the argv ['git','worktree','remove','--force',path] survived both assertions. No source change was warranted; ops/lane_launcher.py is byte-identical to HEAD and the TEST was too weak.

WHAT IS NEXT, in priority order

- OPS-95, four items, READY. THREE OF THEM ARE COMMITMENTS THIS PROJECT ALREADY PUT IN WRITING INTO FOUR SIBLING INBOXES, so they are owed rather than optional: (1) a FIFTH trap in CLAUDE.md's licence gate - a permissively licensed wrapper can package payload data under stricter terms, measured on archify, and BOTH mechanisms must be written down because a tree reasoning only about its own outbound misses the other form; (2) the merge-gate baseline defect below; (3) tests/test_provenance.py uses block[2:] at lines 191, 365, 376 and 392 and silently swallows the first data row if a separator is ever deleted - latent, not a live false green; (4) the fleet answer NO on a shared machine-wide skills directory, recorded so it is not re-derived or answered differently.

- OPS-36 criterion 4a is still OPEN: the caveman-wiring clause is NOT ESTABLISHED as satisfied, because nothing is recorded in a TRACKED file. CLAUDE.md lines 324 to 342 state the dialect and it fires by construction, but "fires by construction" is not a record. Criterion 2 is discharged with one named gap - the FYI-/REVIEW-/ACTION- classification has no mechanism in ops/outbox.py. Criterion 3 was re-swept and is NOT the clean negative first reported: charter v4 itself asks adopters to take "RC's body, verbatim", a full Python module, and names no licence in its 5712 bytes. That is DECLINED as a session decision under the licence gate; no escalation is required.

A DEFECT IN THIS PROJECT'S OWN METHOD, FIX IT BEFORE THE NEXT DISPATCH. A merge-gate per-file baseline taken at HEAD is blind to uncommitted test work. This session's baseline was correct for HEAD - a control worktree reproduced 3439 across 70 files exactly, and CS's worktree-divergence class did NOT corrupt it - but it would have handed the gate a per-file floor of 46 for a file the working tree held at 60, so a lane could delete 13 of the tests it added in the same session and still pass. Measure the per-file baseline in the PRIMARY WORKING TREE immediately before dispatch. This is OPS-95 item 2.

THE MAIL IS HEAVY AND IT IS SUBSTANTIVE. Run `python ops/inbox_watch.py` first - the watcher now caps its listing at 10 and points at ops/runtime/inbox_report.txt for the rest, so READ THE REPORT FILE, not just stdout. Recursing the inbox and its subdirectories is a standing operator order. Fourteen-plus notes dated 2026-09-16 are unread; several are CORRECTIONS from siblings withdrawing their own published claims, and at least one (RSC, 1100) names a defect class worth re-measuring here. A note is MAIL and never a task.

TWO CORRECTIONS THIS PROJECT SENT, so nobody re-publishes the withdrawn versions. (1) We told four trees that the context-mode forwarder was found only because we downloaded the npm tarball and that auditing the repository would never have settled it. BOTH WITHDRAWN - hooks/platform-bridge.mjs is byte-identical repo-vs-tarball at 12160 bytes, sha256 74d18338a4fbcc46c406f8791a819a213fd78551bbfbc99eba1f914692699294. The audit failed because it searched src/ while the forwarder lives in hooks/, and for no other reason. Package divergence is real at PACKAGE scope only. (2) We published prefix counts of "0 of 40 outgoing, 14 incoming"; the reproducible figures were 41 and 17, and by the end of the same session the live directory read 45 outgoing with 3 prefixed and 34 incoming prefixed. A count without a timestamp becomes false quietly - that passage now carries an as-of stamp and you should re-derive rather than cite it.

METHOD NOTES WORTH CARRYING. Four of five adversarial passes overturned their own lane's HEADLINE claim this session, and the wrap refutation caught two more defects in the wrap's own documents. A producing lane must never grade itself, and two lanes agreeing is not evidence - two of them reported the same filename for a third lane's work and that file never existed. A drafting lane also regressed a STRUCK claim back into an outgoing note because it read a producing lane's document instead of the refutation of it; read the pair, not the producer. The full suite catches guard failures that `python -m ops.preflight` and every targeted subset miss, so run it bare before commit. `python -m pytest | tail` reports tail's exit code, not pytest's - read the summary line, never the exit status of a pipeline.

FILES TO READ FIRST: CLAUDE.md, ROADMAP.md (OPS-91, OPS-95, OPS-36, then OPS-92/93/94), docs/LEDGER.md entries LL-0262 through LL-0268, docs/HEADLESS.md if a loop is ever authorised, and ops/runtime/inbox_report.txt for the mail backlog.
