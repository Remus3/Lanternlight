LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of 2026-09-20. Assume you have zero context; everything you
need is here or in the files this names.

YOUR JOB THIS SESSION, by operator direction 2026-09-20: MONITOR AND WORK THE
SYNC INBOX. The roadmap has no item you can work - that is OPS-102, and it is a
STATE rather than a failure. Do not start a blocked item and do not lower an
acceptance criterion to make one look startable.

READ FIRST, in this order:
  CLAUDE.md                - the rules. THE HARD BOUNDARY is not negotiable.
  ROADMAP.md               - read OPS-102 FIRST; it says why nothing is eligible.
  docs/LEDGER.md           - newest entry is LL-0290.
  docs/HEADLESS.md         - its STOP CONDITIONS, before starting any loop.
  docs/REPLY_PATHS.md      - where a reply actually goes. SIX codes now.

STATE AT THE WRAP
  Branch main, clean, pushed. HEAD cd0b01a plus this wrap commit.
  Full suite, observed this run: 3668 passed, 1 skipped in 416.76s.
    Collected 3669. Do NOT carry either number forward - re-measure. `-q` is
    already in pytest.ini addopts, so a second one prints NO summary line and
    still exits 0. Run bare `python -m pytest` and read the last line; this
    session hit exactly that trap once and re-ran to a file to get a number.
  Ruff: All checks passed. `python -m ops.preflight`: PRE-FLIGHT PASS.
  Loop state: cycle 95, item INBOX-MONITOR, 99 completed.
  CAPTURE WATCHER: NO_RECORD, and this wrap DID NOT re-arm it. That is the
    FIFTH wrap in a row to say so. The operator disarmed that lane on
    2026-09-11 and never lifted it; docs/HEADLESS.md says the wrap does not
    re-arm it, and the operator overrides the ritual. Nothing is archiving the
    game log, the saves or the market cache. Accepted cost, not a fault.
  Inbox: ~50 unread. Sent 14 notes on 2026-09-20 through ops.outbox.deliver,
    all five recipients each, none failed.

WHAT THIS SESSION CLOSED
  OPS-36, OPS-91, OPS-95, OPS-96, OPS-97, OPS-98, OPS-99, OPS-100, OPS-101.
  Six commits, cycles 89 to 94. Evidence in LL-0270 through LL-0290.

  The operator ruled VENDOR on Amberstone's docs/CHANNEL.md, then the channel
  voted six-of-six for a CHANNEL_VERSION 2 re-pin and RC cut it the same night.
  We hold it at third_party/rc_channel/docs/CHANNEL.md, 25425 bytes, sha256
  fc22e86eebe93bb247a91f44835257a3fe717a287c4a3184a8e7a7b9a463fb9c. The licence
  gate was RE-RUN at the new commit rather than inherited: a licence is a fact
  about a COMMIT, not about a repository.

THE FOUR FINDINGS WORTH CARRYING, all measured here
  1. A STATIC CHECK IS A CLAIM ABOUT SOURCE TEXT AND LOSES TO A NAME BUILT AT
     RUN TIME. Three guards asserting the responder could not send were defeated
     at once by importlib.import_module("ops." + "out" + "box") plus getattr of
     an assembled attribute, while the suite stayed GREEN at 50 of 50. The
     replacement is not a better static check - it is a subprocess under
     sys.addaudithook. A census then found FOUR guards over THE HARD BOUNDARY
     with no runtime arm at all, and one module, overlay/window.py, that no
     severity-1 guard had ever examined because the scope was a DENYLIST.
  2. MEASURE THE AUDIT EVENT BEFORE BUILDING ON IT. importlib.import_module does
     NOT raise the `import` event. shutil.which raises NOTHING. subprocess.Popen
     carries a STRING, not an argument vector - a guard indexing it reads one
     character. ctypes.dlsym DOES carry the real symbol name after assembly, and
     that one measurement is what makes the native arm possible.
  3. A HEREDOC EATS A DOUBLED BACKSLASH, TWICE IN TWO DAYS. A regex character
     class meant as backslash-or-slash became slash-only, and the sweep returned
     a clean, plausible, internally consistent answer with NO error message. It
     was published to five trees and withdrawn. Write the script to a FILE with
     a self-test that asserts the pattern matches before it walks anything.
  4. A COUNT FROM THIS CHANNEL CARRIES THE TIMESTAMP OF THE READ. A vote tally
     was published off a digest of 13 notes while the inbox already held 20, and
     recorded CS as silent when CS had voted. Re-derive from the notes.

WHAT IS OWED ON THE CHANNEL
  Nothing HALTS this tree. LW's HALT and the responder tasks RSC and RC disabled
  are their own trees under their own operators. LW's ROUND B lands 2026-09-22
  and we gave it a GO AHEAD: it is a VOTE and not a hash, because we carry
  neither winmutex.py nor slots.py - re-measured, zero in git ls-files and zero
  on a full tree walk. Do not let anyone count us in that round's N.

  RE-PIN STATE: v2 at four carriers - LW, RC, RSC, LL. CS is the sole
  outstanding carrier, still at v1. No mismatch reported anywhere. SS holds no
  copy, is settled as a participant and NOT a pin-holder, and must not be asked
  again.

  ALWAYS DELIVER A REPLY - standing operator instruction, now in CLAUDE.md.
  Every note gets an answer, including one saying we owe nothing. Charter v2
  makes silence read as DISSENT, so a quiet tree with no stake is
  indistinguishable from one objecting, one whose watcher never fired, and one
  whose delivery failed.

CORRECTIONS THIS SESSION MADE TO ITS OWN PUBLISHED WORK
  Two figures were published to five trees and WITHDRAWN: "six harness project
  directories" was five, and "seven git-root files" was eleven. LW's eleven and
  ours were then compared member by member and byte for byte - they agree, so
  the total was not a coincidence. LL-0290 corrects a third: three ledger
  entries and two delivered notes carry the date 2026-09-21 and were written on
  2026-09-20.

THE WRAP-TIME ADVERSARIAL PASS TRIED TO BREAK FIVE DONE-CLAIMS AND FAILED ON
ALL FIVE. It re-ran every mutation itself rather than believing the reports,
compared per-file collected counts against HEAD~6 in a detached worktree (3516
across 73 files then, 3669 across 78 now, NO file fell), fired the pre-commit
hook end to end with a banned glyph and a clean negative control, and confirmed
the vendored digest against the GIT BLOB rather than the working file. Its only
finding is the date error above. It could not verify the eleven-filename
agreement with LW's own disk, nor RC's upstream provenance without network.

HOW TO START
  python ops/inbox_watch.py          # if no MAIL block appeared, a hook missed
  python -m ops.preflight            # about 18s, before dispatching anything
  Then digest the inbox with a SUBAGENT and keep its full output in the
  scratchpad - the merger's context is the one that must not fill.

DO NOT
  - re-arm the capture watcher without the operator lifting the 2026-09-11 disarm
  - edit anything under third_party/ - it is byte-pinned and its guard fails
  - adopt anything cross-project; declining is yours, adopting is a ruling
  - raise a doc size budget - re-run `python scripts/apply_doc_split.py --apply`
  - trust a subagent's count, a digest of the inbox, or your own heredoc
