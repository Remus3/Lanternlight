LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-07 evening session. Overwritten every
session; this file is tracked, and its git history is the record of what each
session handed forward.

Resume Lanternlight from disk. Read CLAUDE.md, then this file, then ROADMAP.md
and docs/LEDGER.md newest-first. Ask nothing; start work.

READ FIRST, IN THIS ORDER
  CLAUDE.md
  ROADMAP.md          - OPS-43 is the next item. NO operator decision is
                        pending: OPS-42 and OPS-47 were both ruled and closed.
  docs/LEDGER.md      - newest first. LL-0164 through LL-0167 are this
                        session. READ LL-0167 FIRST: it is the wrap's own
                        refutation, and it corrects two things the rest of the
                        session wrote about itself.
  WAKEUP_NOTES.md     - top entry.

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. The inbox was fully
acknowledged at this wrap, so anything it lists is genuinely new since
2026-09-07 17:38. A plain run REPORTS ONLY and does not write the state file.
Acknowledge with an explicit `python ops/inbox_watch.py --acknowledge`, and
only for notes you have actually read.

STATE AT THE WRAP
  origin/main           this session's commits are 73b03e3, eee1188, 1ee0ee4,
                        0cd5221, 19b3b48, plus the final wrap commit that
                        carries this line. Working tree clean at the wrap,
                        everything pushed. Confirm with
                        `git log --oneline -6` and `git rev-parse HEAD
                        origin/main` rather than trusting this list.
  python -m pytest bare 2225 passed, 1 skipped in 167.51s
  python -m ruff check. all checks passed
  loop                  cycle 54, item OPS-43. OPS-43 was CARRIED FORWARD as a
                        retry and is NOT credited as completed, because this
                        session did not touch it.
  capture watcher       ARMED, pid 21680, identity VERIFIED, heartbeat 18 s
                        old, no stale surfaces, archiving into
                        C:\ll-captures\2026-09-07. Re-check it with
                        ops.loop.watch.check_watcher() rather than believing
                        this line.

WHAT THIS SESSION DID
Reviewed the mail under the standing OPS-34 instruction. Six unread notes read
in full and acknowledged, the acknowledge PROVEN by re-running the reporter
rather than assumed from its exit code. Four sibling questions answered by
measurement, four replies delivered and confirmed by listing each destination.
Then two operator rulings arrived in chat and were acted on: OPS-47 was filed
as a pending decision, ruled, and closed the same session, and the operator's
hand-off complaint was discharged. Then the wrap's refutation pass found two
defects in what this session WROTE about its own work and both were corrected;
that is LL-0167. Full detail in LL-0164 through LL-0167.

NEXT ITEM: OPS-43, untouched, and now carrying a measured number.
An outgoing note is written straight into a sibling's directory, no copy is
kept here, and moon_sync_inbox is gitignored, so nothing in this repository
records that a reply was ever sent. MEASURED AT 2026-09-07 17:38, BEFORE this
session's own replies were delivered: nineteen unique from-LL-* notes existed
across the four sibling inboxes, five appeared anywhere in docs/LEDGER.md, and
FOURTEEN left no trace at all here. That is a four to one undercount, and on
2026-09-07 a session reading only its own disk concluded this project had never
replied to anyone. DO NOT QUOTE THE NINETEEN AS A CURRENT FIGURE - it was
already twenty four by the end of this session, because seven more notes were
delivered. Re-derive it; the method is in OPS-43. All four acceptance criteria
are in ROADMAP.md OPS-43.

Two traps in that item. Criterion 2 interacts with the ruling that the watcher
covers the ENTIRETY of moon_sync_inbox: an outbox placed inside that folder is
watched, so it must be CLASSIFIED as outgoing rather than skipped, or every
note this project sends returns to it as unread mail. Criterion 3 must be
proven non-vacuous by removing the outbox copy, watching the test go red,
restoring, and confirming green.

ALSO OPEN
  OPS-41  report-only mid-session acknowledge trigger. Must land AFTER OPS-43.
          No UserPromptSubmit hook exists - measured absent, not empty.
  OPS-44  the source-register guard's denylist is absorbing this project's own
          filenames. It has now tripped FOUR times, most recently on this
          session's own ledger entry. Expect YOUR ledger entry to redden it
          too. The sanctioned move is to vet the token and add it to
          KNOWN_NON_HOSTS; the guard's LOGIC is left alone, because that file's
          docstring reserves logic changes for a deliberate decision and
          OPS-44 still holds it unmade.
  OPS-45  Stop-hook transcript-claim auditor. This tree has none.
  OPS-35  criterion 5, interop with a real sibling holder, is NOT met and
          cannot be while the lane slot's lock root stays ours.

NOT MEASURED, AND NOT CLAIMED CLEAN
Whether a history rewrite here left FILENAMES leaking after blob CONTENT was
scrubbed. A sibling measured that failure in its own tree: the rename table is
keyed on the HEAD path while the filter sees each commit's own path, so content
comes back clean while 2195 filename hits survive in tree objects. OPS-40
closed the CONTENT and COMMIT-MESSAGE axes here. This is a third axis and
nobody has checked it.

TRAPS THIS SESSION PAID FOR
A ref-pattern query FAILS GREEN. `git for-each-ref` wants `refs/pull/` and
`git ls-remote` wants `refs/pull/*`, and neither errors when the pattern
matches nothing, so an empty result from a broken pattern is indistinguishable
from a repository that has no pull refs. Run a positive control before writing
down any clean bill of this shape - and NAME THE CONTROL'S SUBJECT, which this
session failed to do until its refutation pass caught it. A control whose
subject is unnamed cannot be re-run by the reader, so it is an assertion
wearing a control's clothes. The named ones, re-run at this wrap:
  git ls-remote https://github.com/anthropics/anthropic-sdk-python 'refs/pull/*'  -> 1470
  git ls-remote https://github.com/psf/requests 'refs/pull/*'                     -> 4354
  git ls-remote origin 'refs/pull/*'                                              ->    0

A SCOPE WORD IS A CLAIM ABOUT YOUR SWEEP. This session wrote that no document
in this tree "ever" carried a particular false claim, on the strength of a
sweep over 25 documents in the working tree at one instant - no tests, no other
tracked files, no historical blobs. The finding survived a proper sweep of all
166 tracked files and all 1,646 historical blobs, but the sentence had not
earned the word. Write the scope you measured, not the scope you hope holds.

A line-oriented grep is a claim about the file's line breaks. Prose here is
hard-wrapped near 80 columns, so search a whitespace-collapsed copy or a
multiline matcher. Two sweeps this session would have returned false clean
bills without it. Related and still true: `grep -iF` ABORTS on this machine and
looks exactly like no matches - use -i or -F, never both.

A denylist addition is not proven by the suite going green. Green after the
edit is also what a vacuous change looks like. Watch it: green, mutate with the
mutation ASSERTED to have applied, RED, restore, green.

CROSS-PROJECT POSITION, so it is not re-derived
We answer notes, we never vendor, and we have told two siblings PROSE ONLY. We
do not read sibling trees; the inbox listings this session used were of our own
outgoing notes. OPS-47 is closed with the operator's ruling that we do NOT ask
a sibling to scrub this project's name from its public git history, and the
per-name count we had asked for was withdrawn. Sibling inboxes: C:\Riot
Commander, C:\Legion Wallpaper, C:\Clockspeed, C:\Resin Compute, each with a
moon_sync_inbox directory.
