LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-08 night session. Overwritten every session;
this file is tracked, and its git history is the record of what each session
handed forward. It is written by ops/handoff.py, never by an editor tool.

Resume Lanternlight from disk. Read CLAUDE.md, then this file, then ROADMAP.md
and docs/LEDGER.md newest-first. Ask nothing; start work.

READ FIRST, IN THIS ORDER
  CLAUDE.md           - CHANGED this session. Its note about the pre-commit gate
                        blocking any command that merely QUOTES the forbidden
                        cmdlet was STALE and is corrected in place.
  docs/LEDGER.md      - newest first. LL-0202 through LL-0204 are this session.
                        Read LL-0202 first if you read only one.
  ROADMAP.md          - OPS-61 through OPS-67 are all this session's work.

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. A plain run REPORTS ONLY.
Acknowledge with `python ops/inbox_watch.py --acknowledge`, and only for notes
you have actually read. The UserPromptSubmit hook acknowledges ONCE per session
on the operator's own turn, so notes may be marked seen before you look - that
is OPS-41 by design and is NOT the same as your having read them.

SECOND ACTION
Run `python ops/stop_audit.py --show-last`. A FAIL line is a QUESTION about what
the last turn wrote, not a verdict - it cannot tell a claim from a QUOTATION of
one.

THE SPLIT, STILL TRUE AND STILL THE FIRST THING THAT MISLEADS A COLD SESSION
ROADMAP.md and docs/LEDGER.md were split on 2026-09-08 and nothing was deleted.
Closed roadmap items are in docs/ROADMAP_ARCHIVE.md, one hop from the
`## Archive index` stub list; the oldest ledger entries are in
docs/LEDGER_ARCHIVE.md. AN EMPTY GREP OF ONE HALF IS NOT A CLAIM ABOUT THE
PROJECT - only about which half you searched.

CLOSED THIS SESSION, ALL PUSHED
  OPS-61  Every hook command resolved the PRIMARY checkout. Fixed to
          $CLAUDE_PROJECT_DIR, quoted.
  OPS-63  The source register's scope is stated: every tracked .md and .txt,
          plus the old docs/ walk verbatim, minus one enumerated exclusion.
  OPS-64  tools/archive_link_guard.py refuses unknown argv and prints its scope.
  OPS-66  FILED AND CLOSED. tools/precommit_gate.py read an unknown argument as
          a PASS.
  OPS-62  The archives are bounded as a PAIR, not individually.
  OPS-65  A wrap writes the hand-off of the tree it is wrapping.

STILL OPEN, AND OPS-67 IS THE ONLY ACTIONABLE OPS ITEM LEFT
  OPS-67  Four tools/ entry points still read an unknown argument as a pass:
          ascii_check.py, syntax_check_hook.py, probe_paks.py and
          doc_size_budget.py, all re-probed at exit 0. Filed as FOUR decisions
          rather than one batch: the first two are PostToolUse hooks whose only
          caller passes no argv and which exit 0 on every path by design, while
          doc_size_budget.py's numbers get QUOTED - including by this very
          document - which makes a confident verdict about a document nobody
          asked for the OPS-64 failure exactly.
  OPS-35, OPS-36 remain open and operator-ruled. OPS-48 is HELD by operator
  ruling: do not answer its four questions and do not solicit RC or RSC.
  OPS-52 is CLOSED by operator ruling - "leave the history as is".

EVERY OTHER ROADMAP ITEM IS BLOCKED ON THE OPERATOR PLAYING. There has been NO
NEW GAME DATA SINCE 2026-08-30. Do not schedule a capture session; fold those
items into whichever session next has the game running. Ask, do not guess, and
do not read a date out of a document:
    python -c "from ops.loop import watch; print(watch.check_watcher().reason)"
Read it as an UPPER BOUND: a QUIET answer is trustworthy, a RECENT one means a
copy happened, which is not the same as new data.

ONE THING WAITING ON THE OPERATOR, NOT ON A SESSION - FROM OPS-62
A PAIR-BUDGET FIRING HAS NO MECHANICAL REMEDY. A live-budget firing is answered
by re-running tools/doc_archive.py. A pair firing cannot be, because the pair
total is precisely what a split does not change. The only answers are a real
reduction in content - which this repository's own rules forbid for the ledger,
where an entry is written in full for a cold session - or an operator ruling:
move the archives out of this repository, or accept a higher bound.

  DO NOT SILENTLY RAISE A PAIR BUDGET WHEN IT FIRES. Headroom at this wrap is
  roughly 12 sessions on both pairs, so this is not urgent - but it is a
  DECISION GATE and PAIR_LOW_HEADROOM_SESSIONS deliberately warns further out
  than the per-document threshold so the warning arrives before the wall.

THE MEASUREMENT THAT MATTERS MOST THIS SESSION
A HOOK THAT FIRES AND ANSWERS ABOUT A DIFFERENT REPOSITORY DOES NOT MERELY
REPORT - IT WRITES. OPS-61's filed text said a worktree's hook "answers a
question about a different repository". The pre-fix control measured more than
that: with the old wiring restored, a real headless session running IN a
worktree rewrote the PRIMARY checkout's ops/runtime/inbox_reported.json AND its
inbox_seen.json, while the worktree's own records were never written. Both
hashes differed from the pre-experiment snapshot and both were restored from it.

  An acknowledgement written into the wrong tree EATS MAIL in a tree nobody was
  working in. A wrong answer is recoverable by looking again; that is not.

THREE EXISTING GUARDS WERE GREEN BECAUSE OF THE DEFECT
None was weakened to fix it. Each was measuring a proxy that only held while the
hook commands named an absolute root:

  1. test_every_hook_command_names_a_script_that_exists required every .py token
     in a hook command to be a file on THIS machine, which can only pass while
     the token is an absolute literal path. It now resolves against the running
     tree - strictly stronger, because the old form silently SKIPPED any token
     it did not recognise as a path.
  2. The same file's no-backslash assertion banned a backslash ANYWHERE in the
     raw settings text. CLAUDE.md's rule is narrower - a single-backslash
     WINDOWS PATH breaks the parse - and shell quoting needs an escaped quote.
  3. tests/test_inbox_watch.py ran the SessionStart command through subprocess
     and its own name said end-to-end, while executing a command line that
     resolved the PRIMARY checkout regardless of tree.

  If a fix makes a guard red, read the guard before believing the fix is wrong.
  Twice this session that red was the guard admitting what it had really been
  measuring.

FOUR WAYS A MEASUREMENT LIED THIS SESSION, ALL CAUGHT, ALL WORTH KNOWING
  1. A MUTATION THAT FAILED TO APPLY PRINTED A GREEN SUITE. The first OPS-66
     mutation did not match its anchor; the targeted run then said 67 passed,
     which reads exactly like a mutation that killed nothing. Only the anchor
     assertion said otherwise. CRLF was checked and ruled out. ALWAYS ASSERT
     THE ANCHOR MATCHED BEFORE BELIEVING A SURVIVOR.
  2. A MUTATION HARNESS HID SWAPPED IDENTITIES. The OPS-65 slice's first driver
     reported per-run summary lines in which a mutation could appear to kill the
     wrong test. Rebuilt per-test before any result was believed.
  3. A TEST WAS VACUOUS ONLY IN THE PRIMARY TREE. Hardcoding ops/handoff.py's
     default target did not kill test_the_default_target_follows_the_tree,
     because in the primary checkout the hardcoded and resolved values are the
     same string. It now loads the module from a copy in a temporary tree.
  4. MY OWN PROBE OF THE COMMIT GATE WAS INVALID and that is how CLAUDE.md's
     stale caveat was found. An echo naming the forbidden cmdlet inside a
     sentence RAN, because OPS-22 narrowed the check to COMMAND POSITION. The
     valid probe - the name in command position - was refused, and the refusal
     text also proved the PreToolUse hook was live on the NEW quoted command
     form mid-session, because the harness quoted the command back.

WRITING A MEASUREMENT DOWN KEEPS CHANGING THE THING MEASURED - THREE TIMES TODAY
LL-0199 named this shape and it recurred twice more. OPS-62's outcome quoted the
pair totals measured at the merge; writing that outcome into ROADMAP.md moved the
ROADMAP pair from 702,038 to 711,879 bytes and its headroom from 11.9 sessions to
11.6. Both instants are in the item now. Separately, this session's own closure
prose introduced two new host-shaped tokens - NOPE.md and sys.argv - into a guard
that had just been widened to read the document doing the writing.

  DATE A SIZE, OR DO NOT QUOTE ONE. Ask
  `python tools/doc_size_budget.py` for headroom in SESSIONS; it is the only
  figure that cannot go stale.

HOW TO WRITE THE NEXT HAND-OFF - CHANGED THIS SESSION
    python ops/handoff.py --from-file <draft>
OMIT --target. OPS-65: the writer resolves its own root from its own file
location, so a wrap writes the hand-off of THE TREE IT IS WRAPPING. The ritual
used to pass an absolute path, and measured from a real worktree it wrote the
OTHER tree's tracked hand-off while git status in the wrapping tree stayed clean.
The writer still runs the redactor over the STRING and refuses BEFORE anything is
written, so a refusal leaves the previous hand-off byte-unchanged. No exemption
list, no override flag. --check-only reports without writing.

STATE AT THE WRAP - RE-DERIVE RATHER THAN QUOTING
  origin/main       three commits this session. Confirm with
                    `git rev-parse HEAD origin/main` and `git log --oneline -5`.
                    NOTE: main has NO UPSTREAM in .git/config, so a bare
                    `git push` fails with "no upstream branch" and the advice
                    text is easy to mistake for success when piped to tail.
                    Use `git push origin main` and then re-derive with
                    `git rev-parse`.
  python -m pytest  bare. A second -q prints NO summary line at all, because
                    pytest.ini already carries one.
  loop              cycle 67 closed OPS-62 and OPS-65. Advance it yourself.
  capture watcher   ARMED, identity confirmed, heartbeat inside its threshold.
                    ARCHIVE state reads UNKNOWN, which is correct: the live
                    process was armed by a build older than OPS-59 and writes no
                    archive map. UNKNOWN is a THIRD answer, not a report that
                    nothing was copied.
  budgets           Two channels now. Per-document, and the OPS-62 PAIR channel
                    which is the one that bounds the archives. Ask the module.

BEFORE DISPATCHING PARALLEL WORK
Read SHARED_WORKTREE_BAN in ops/store_drift.py and put it in every slice brief -
it is ADVISORY and nothing enforces it. Record dispatches with state.dispatch(),
read state.in_flight_summary(state.load()) FIRST, and capture a per-file baseline
AT DISPATCH to hand the merge gate as per_file_baseline.

  DO NOT RUN TWO PYTEST PROCESSES AT ONCE. Give every slice a targeted run over
  its OWN test files only and keep the full suite for the merge. This session
  ran three pairs of slices that way with no spurious red;
  tools/doc_size_budget.py calls `git hash-object -w`, which writes loose objects
  into the real repository, and tests/test_store_drift.py counts them.

  RETIRE YOUR SLICES. state.retire() is ITEM-scoped, not lane-scoped, so it
  clears every lane recorded against an item at once - which means you cannot
  retire one lane of a two-lane item while the other still runs. Retire at the
  merge, and know that in_flight reads high until you do.
