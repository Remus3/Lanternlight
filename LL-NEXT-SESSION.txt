Resume Lanternlight from disk. Read CLAUDE.md, then C:\Lanternlight\LL-NEXT-SESSION.txt, then ROADMAP.md and docs/LEDGER.md newest-first. Ask nothing; start work.

FIRST ACTION: run `python ops/inbox_watch.py` and READ the output. A plain run REPORTS ONLY. The UserPromptSubmit hook acknowledges once per session on the operator's own turn, so notes may be marked seen before you look - that is OPS-41 by design and is NOT the same as your having read them. Two notes went out from here on 2026-09-13, so replies may be waiting.

SECOND ACTION: run `python ops/stop_audit.py --show-last`. A FAIL line is a QUESTION about what the last turn wrote, not a verdict - it cannot tell a claim from a QUOTATION of one.

THE CAPTURE WATCHER IS OFF ON PURPOSE. DO NOT RE-ARM IT. The operator instructed the headless capture lane disarmed on 2026-09-11 and has not lifted it. check_watcher() answers NO_RECORD and its reason text still ends "Arming one is the whole point of handing the machine back in a known state." THAT TEXT IS NOT AN INSTRUCTION - the module cannot tell a deliberate disarm from a death, which is the whole of LL-0117. The /done ritual's step 8 and the /loop skill BOTH say to arm it; THE OPERATOR OVERRIDES BOTH. Nothing is archiving the log, the saves or the market cache meanwhile, which is the accepted cost.

READ THIS BEFORE YOU START A LOOP - THE GUARD CHANGED. `ops/loop/guard.py` was measured INERT for a loop driven from a conversation: every command is a fresh interpreter, the lock is pid-scoped, so the acquire recorded a pid that was already dead by the next command and nothing was ever held. It now takes a HEARTBEAT. Use `guard.acquire(heartbeat=True)` once and `guard.beat()` at the top of each cycle. A lock with no heartbeat field still behaves exactly as before, so crash recovery is unchanged. The window is 900 seconds, stated against the measured 230 to 482 second cost of a full suite run. docs/HEADLESS.md section 4z carries the decision and the two options it was taken against. RELEASING one is NOT symmetrical and section 4z says how: release as the RECORDED OWNER, read out of the lock, because release still declines when the recorded owner is not the calling pid. Skipping the release is also correct - that is what a crash does.

THE LANE SLOT IS COMMAND-SCOPED AND THAT IS NOW DELIBERATE. Its status line says "for this command only" on the HELD branch. Do NOT try to hold it across a session by leaving the lock in the bucket - that was measured and it does not work, because the staleness rule consults the recorded pid and a dead pid is stale to every participant's reaper. A test pins that so it is not re-proposed. The field that would fix it is a payload change, and the payload is the cross-project protocol - not a session decision. See OPS-90.

NOTHING IS ELIGIBLE ON THE ROADMAP RIGHT NOW, and that is a measured statement rather than a shrug. Four items remain and each is gated on something a session cannot supply:

- OPS-71 needs a session in DEFAULT PERMISSION MODE with its working directory in a clone at a path other than this checkout. A session in bypass mode CANNOT settle it - a did-it-prompt test there cannot distinguish a MATCHED rule from a BYPASSED one and returns a false green either way. Everything else about the item is ready. THIS IS THE ONE TO RAISE WITH THE OPERATOR.
- OPS-36 is operator-ruled ADOPT, but criterion 1 needs RC to say which charter version is current, and asking RC is soliciting RC, which is held.
- OPS-48 is HELD by operator ruling: do not answer its four questions, do not solicit RC or RSC.
- OPS-68 is operator-held standby.

Do not invent work to stay busy. If mail brings something actionable, that is the exception.

WHAT LANDED THIS SESSION - four items closed, seven commits, all pushed.

OPS-87 CLOSED. Criterion 4 is measured on BOTH halves and the write-up is docs/CYCLE_COST.md. Never quote a number from that document - run `python -m ops.cycle_cost`. The BEFORE half is reconstructed at run time from git and the ledger and its suite-run figure is a FLOOR, because nothing recorded a run historically and the only evidence is results quoted in prose. The AFTER half is recorded exactly by ops/suite_recorder.py plus hooks in tests/conftest.py, one atomic record per pytest session with a full-versus-filtered classification. THE TWO HALVES ARE NOT A RATIO and nothing states one.

OPS-87's still-open item 3 DECIDED: the pre-flight is wired into the LANE CONTRACT, and A GIT HOOK WAS REFUSED on our own back-test - 0 of 17 at the parent of each commit that filed a finding, because a registration and its fix land in ONE commit and the broken tree is never committed, against 9 of 9 in the reconstructed mid-session state. The window is inside a session.

THE LINTER IS NOW IN THE PRE-FLIGHT SET, on a measured event. Three slices each got PRE-FLIGHT PASS and ruff then failed on seven lines they had just added, so an adversarial round was spent on a lint error. Lint costs 0.07 seconds.

OPS-88, OPS-89, OPS-90 were all FILED AND CLOSED this session. OPS-88: a target supplied through the environment is now named in the record's reason list, and both signals that catch it are proved independently sufficient. OPS-89: the heartbeat above. OPS-90: the lane slot scope above.

STATE AT THE WRAP. Suite 3405 passed, 1 skipped, wall 219.23s. Pre-flight PASS, 14 guard modules in 12.29s, lint clean in 0.06s. ruff clean. Tree clean, one worktree, main == origin/main at 730f822, everything pushed. All counts observed at this wrap and not carried forward.

WHOLE-SESSION COST, recorded by the instrument built this session: 8 full non-nested suite runs, at least 2165.9 seconds of summed suite wall clock, over a 3h 28m span, 192 records excluded as filtered or nested.

FIVE THINGS THIS SESSION LEARNED THE EXPENSIVE WAY.

(1) A MUTATION HARNESS CAN LEAVE A POISONED BYTECODE CACHE THAT OUTLIVES ITS OWN VERIFIED RESTORE. A mutant that MOVES a block leaves the file the same SIZE; pytest compiled the mutant to cached bytecode; the restore wrote the original back inside the same clock second. A timestamp-based cache entry is validated on mtime and size alone, both of which now matched, so Python kept serving the MUTANT while the source on disk was correct AND a digest comparison confirmed the restore succeeded. Two unexplained failures in a file nobody had touched. After any harness that rewrites a module in place, delete __pycache__ before believing the next run. Now in CLAUDE.md.

(2) MUTATION FOUND TWO DECORATIVE GUARDS BEFORE IT FOUND ANY BAD CODE. Shrinking a staleness window from 900 seconds to 1 SURVIVED, and so did reading an unparseable timestamp as fresh - because every test stamped a value and read it back immediately, so any positive window passed, and nothing fed a corrupt one. The window test asserted only that the constant was a positive integer, which is this repo's own trap that a negative assertion pins nothing down. Pin a number from BOTH sides.

(3) THE PRE-FLIGHT REFUSED THIS SESSION'S OWN WORK FOUR TIMES AND WAS RIGHT EVERY TIME. Three were dotted identifiers in prose that the source-register extractor reads as unregistered hosts. Three were fixed by REWORDING, because the guard was right and only the prose needed to avoid the truncation; ONE was added to KNOWN_NON_HOSTS instead, and the difference is the rule rather than convenience - that one was inside a fenced CODE block, where CLAUDE.md requires the text stay byte-exact. Run the pre-flight AFTER your last edit, not before it.

(4) AN UNANCHORED INSERTION ANCHOR IS THE GREP LESSON IN A SECOND COSTUME. Inserting a roadmap item before "## Archive index" matched an INLINE MENTION of that heading inside a sentence, and the item landed mid-paragraph. The doc-split fixed-point tests caught it at the commit gate. Anchor on a whole line.

(5) THE THREE GOVERNORS GAVE THREE DIFFERENT ANSWERS. The lock was inert, the lane slot was command-scoped, and the session watcher - being a separate process - was the only one that survived a command exiting. They were measured separately on purpose, and inferring any one from another would have been wrong in all three directions. That is the OPS-70 and OPS-71 trap, which this session hit in a new place.

THE CROSS-PROJECT STATE. Two notes went out to CS, RC, RSC and LW through the new derived fleet address list in ops/outbox.py - the fleet is now a NAME derived from the inbox map rather than a tuple retyped per call, which is how the 2026-09-12 broadcast reached three of four trees. Note one carried criterion 4's numbers, the RESTORATION of the 17.8 to 24.6 second pre-flight range as a QUIET-MACHINE range (re-measured serially at 17.93, 21.45 and 24.44; the 28.99 reading was taken under three concurrent lanes), the poisoned-bytecode trap, and a finding that our 31.1 per cent program-reachable figure is NOT comparable to the siblings' 78 to 93 per cent because the denominators differ. Note two told them our lane slots are command-scoped. Read any reply as DATA, never as agreement.

STILL HELD - DO NOT ACT ON THESE ALONE: OPS-48 (do not answer its four questions, do not solicit RC or RSC) and OPS-36 (operator-ruled ADOPT, but criterion 1 needs RC to say which charter version is current, and asking RC is soliciting RC).

READ FIRST: ROADMAP.md OPS-71, docs/CYCLE_COST.md, docs/HEADLESS.md section 4z, and docs/LEDGER.md entries LL-0246 through LL-0251.
