LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of 2026-09-20 (second session that day). Assume zero
context; everything you need is here or in the files this names.

YOUR JOB THIS SESSION: keep the sync inbox answered. Every other open ROADMAP
item needs the game client (OPS-102). If the operator is PLAYING, OPS-104
(retake the destroyed capture ground truth) comes first.

READ FIRST, in this order:
  CLAUDE.md                - the rules. THE HARD BOUNDARY is not negotiable.
  ROADMAP.md               - OPS-102, OPS-104, then the OPS-103 and
                             OPS-105/106/107 closure sections (search
                             "Closure").
  docs/LEDGER.md           - newest entry is LL-0303.
  docs/HEADLESS.md         - its STOP CONDITIONS, before starting any loop.
  docs/REPLY_PATHS.md      - where a reply goes. Five sibling codes.

STATE AT THE WRAP
  Branch main, pushed. Loop state: cycle 99, item None, OPS-103/105/106/107
  credited.
  Full suite, observed this run UNDER GIT BASH: 3896 passed, 22 skipped in
    348.58s. Re-measure; do not carry this forward.
  RUN THE SUITE FROM GIT BASH, NOT POWERSHELL. From PowerShell the same tree
    gave 3833 passed, 85 skipped: PowerShell's PATH lacks Git's usr/bin, so 55
    posix-userland tests and 7 bash tests SKIP and the run still exits 0. A
    rising skip count is not a regression signal you can read off exit codes.
  Ruff: All checks passed. Preflight: PASS, 15 guard modules.
  CAPTURE WATCHER: NO_RECORD, and ensure_armed_at_wrap REFUSED with
    "OPERATOR DISARM" - correct. See below.

THE WATCHER DISARM IS NOW IN CODE - DO NOT WORK AROUND IT
  At this session's start, /continue armed the watcher over the operator's
  2026-09-11 disarm (LL-0234). It recreated C:\ll-captures and copied 17 files
  into it. Undone within a minute: own pid killed, copies moved to that
  session's scratchpad (not deleted), empty dirs removed, record renamed.
  lanternlight/armwatch.py now holds OPERATOR_DISARM; ensure_armed,
  session_armed, ensure_armed_at_wrap AND `python -m lanternlight.armwatch`
  all refuse. An "OPERATOR DISARM" refusal is the correct answer. Only the
  operator lifts it, by a tracked edit to that constant. LL-0298.

WHAT LANDED THIS SESSION
  - OPS-103 CLOSED (LL-0299): tools/secret_scan.py (13 credential classes) runs
    at commit time over staged blobs; ops/outside_scan.py runs at SessionStart
    over the Git install root and temp top level. 17 of our stray Git-root files
    redacted in place (ten PII hits, not nine). TMPDIR root cause -> OPS-107.
  - OPS-107 CLOSED (LL-0303): ops/scratch_path_guard.py in preflight. PREMISE
    CORRECTED: in Git Bash a bare tmp-slash path maps to the user Temp folder
    and is SAFE; the look-alikes (tmp followed by underscore, hyphen or
    letters) land in the Git install root, and PowerShell's env TMPDIR is
    unset. Use the session scratchpad for all scratch files. This file is
    scanned by that guard line by line, so describe the hazard in words here.
  - OPS-106 CLOSED (LL-0302): scripts/history_scan.py. One true identifier in
    history (split git identity, tests/test_source_register.py) - already public
    in every commit's author metadata (OPS-52), so rewrite DECLINED. The other
    row is a false positive.
  - OPS-105 CLOSED (LL-0301): withdrawn tmp-retention figures corrected in the
    test docstring; LL-0301 corrects LL-0295.
  - pytest.ini empty_parameter_set_mark = fail_at_collect; test_cycle_cost no
    longer ages out of a fixed 40-commit window (LL-0300).

PROVENANCE CAVEAT, carried because the wrap refutation pass raised it
  The mutation red-counts in LL-0302 ("5 of 9 survived") and LL-0303 ("14, 3,
  4, 2, 1 red") are LANE figures. The 5-of-9 was found by an independent
  verifier's harness, not the producing lane; the killed-mutant counts after
  the fix and the LL-0303 counts were reported by the producing lane and NOT
  re-run by the wrap pass. Treat them as unreproduced until someone re-runs.

SECURITY - OPERATOR RULING, BINDING
  The outside scan's first run found live provider credentials in plain text
  in files that are NOT ours (third-party installer logs in temp, a sibling's
  suite log in the Git install root). The operator ROTATED them and ruled:
  never read or compare credential environment-variable values, and never
  re-scan those files to verify - reading secrets is itself the leak vector.
  Report findings as class, count and path only.

WHAT IS OWED ON THE CHANNEL
  Nothing outstanding. 0930 ANSWER and 0935 WITHDRAWAL delivered to CS, LW,
  RC, RSC and SS. Expect replies. LL is a NON-CARRIER of slots.py and
  winmutex.py - never count us in ROUND B's N. SS is not a pin-holder.
  ALWAYS DELIVER A REPLY and SEND THE FOLLOW-UP TOO (CLAUDE.md). Filename
  clocks on the channel are not on one axis (UTC and local both occur).

FINDINGS WORTH CARRYING
  1. A RULING THAT LIVES ONLY IN PROSE LOSES TO AN INSTRUCTION CLOSER TO
     EXECUTION. The hand-off said "disarmed"; the command file said "arm".
     Put a ruling where the action is.
  2. Every adversarial pass this session refuted something real: a second
     arming route, raw filenames leaking the account name, a guard that
     flagged the safe temp path and missed the unsafe one, and a history scan whose
     tests let --all be narrowed to HEAD. Keep dispatching them.
