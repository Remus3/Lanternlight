LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-11 session. Overwritten every session; this
file is tracked, and its git history is the record of what each session handed
forward. It is written by ops/handoff.py, never by an editor tool.

Resume Lanternlight from disk. Read CLAUDE.md, then this file, then ROADMAP.md
and docs/LEDGER.md newest-first. Ask nothing; start work.

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. A plain run REPORTS ONLY.
Acknowledge with `python ops/inbox_watch.py --acknowledge`, and only for notes
you have actually read. The UserPromptSubmit hook acknowledges ONCE per session
on the operator's own turn, so notes may be marked seen before you look - that
is OPS-41 by design and is NOT the same as your having read them.

SECOND ACTION
Run `python ops/stop_audit.py --show-last`. A FAIL line is a QUESTION about what
the last turn wrote, not a verdict - it cannot tell a claim from a QUOTATION of
one.

=== WATCH FOR LW'S REPLY. WE ASKED THEM FOR SOMETHING. ===

On the operator's direct instruction in chat - "ask LW for the plugin" - this
session sent LW a note asking for their git-off-PATH false-red probe, delivered
through ops.outbox.deliver so a copy and a delivery record exist here. If a reply
has arrived, it is the first thing to read.

  THE NOTE COMMITTED US TO SOMETHING, so honour it. It says we will NOT vendor
  their plugin: this repository is public and Apache-2.0 and a drop carrying no
  license statement cannot be copied in. It asks LW to NAME A LICENSE explicitly
  if they want it vendorable. **If their reply does not name one, read it for the
  IDEA and write anything we adopt in our own words.** If it DOES name one, that
  is a real change and the license gate in CLAUDE.md applies - GPL and AGPL are
  still DO-NOT-VENDOR whatever anyone offers.

  The note answers no cross-project question and proposes no shared convention,
  and says so. OPS-68 still holds the responder runner and all propagation on
  standby; asking LW for one named thing on a direct operator instruction is not
  a licence to open anything else.

=== OPS-35 IS CLOSED, AND THE THING THAT CLOSED IT WAS NOT ARRANGED ===

The lane was armed on 2026-09-11 and this session ran its whole loop holding
0.lock in the shared machine-wide bucket. When the governor released it, the
bucket was NOT empty: 1.lock remained, and it belongs to somebody else.

  MEASURED about that lock: its pid is ALIVE, its repo is NOT this checkout, its
  ts was 0.36 hours old, is_stale answers False so our reaper would not touch it,
  and its run_id is 8 characters where ours is 25 - an independent sign of a
  different implementation of the same protocol. Exactly the five agreed wire
  fields, nothing else.

  So this project held a surplus slot at the same time as a LIVE participant from
  another project held the adjacent one. That is criterion 5's "interoperation
  proven against a real sibling holder, not a mock", and it is what the item had
  been open on since 2026-09-07.

  TWO THINGS ARE STILL NOT OBSERVED and must not be rounded up. We have never
  been REFUSED a slot - with a surplus width of 3 and two locks held there was a
  free one the whole time, so the contention path is proven only where it
  succeeds. And we have never seen a sibling's reaper reclaim a lock of OURS.
  OPS-76 is the standing reminder that a reaper can exist and never be called.

=== YOUR ITEM: OPS-78 criterion 4, and it comes with a trap ===

The false-red instrument is now general: --tool plants a control for the tool
under test, and BOTH git and bash reported their control PROVED. git is at
findings 0 after this session took it from 187. bash reports 56 false reds across
6 files - test_no_pii 33, test_syntax_check_hook 8, test_precommit_gate_lint 7,
test_docguards 7, test_precommit_hook_globbing 6, test_ascii_hygiene 1.

  RE-RUN IT BEFORE ACTING. That 56 is the implementing slice's measurement and
  was NOT independently re-run by the merger. The git figure was.

  THE TRAP, and it narrows what 56 means. Both runs were launched from Git Bash,
  because bash is not on the PATH this machine gives PowerShell. All three
  stripped PATH entries are ONE directory - Git's usr/bin. So a false red there
  says "this broke when that DIRECTORY left the PATH", which is wider than "this
  needs bash". READ THE INDIVIDUAL FAILURES. Do not convert a call site on the
  strength of the tool name in the report; the tool name is the question that was
  asked, not the dependency that was proved.

  Then OPS-75 (a .gitignore pattern that already shadows a tracked file), OPS-77
  (a surplus width of zero is a permanent silent BUSY against a healthy empty
  bucket) and OPS-81 (each applied split adds one blank line before the archive
  index, forever).

=== HELD BY THE OPERATOR - DO NOT ACT ON THESE ALONE ===

OPS-68. The responder runner and cross-project propagation are on STANDBY by the
operator's own words on 2026-09-08: "standby on this decision then, i will keep
it to the test for now", answered to BOTH questions. Nothing has been built. The
one note sent this session was a specific operator instruction, not a precedent.

OPS-48 remains HELD - do not answer its four questions, do not solicit RC or RSC.
Its hold names RC and RSC; it does not name LW.

OPS-36 ROUTES THROUGH THE SAME CLOSED GATE. Operator-ruled ADOPT, which reads as
actionable, but criterion 1 needs RC to say which charter version is CURRENT and
criteria 2 to 4 depend on it. Asking RC is soliciting RC.

=== THE THING TO LEARN FROM THIS SESSION ===

**EVERY WRONG THING THIS SESSION SHIPPED WAS A COUNT OR A DOCUMENT. NOT ONE WAS A
TEST.** The suite was green through all of it. Only refutation passes caught them.

  - Three documents said this project never removes a lock it did not create.
    True for the module's whole life; made false by OPS-76 in the same session.
  - A status line printed once per cycle carried the operator's account name,
    because the bucket display helper elided paths INSIDE the checkout and
    printed every other one verbatim - and every bucket we use is outside it.
  - THREE COUNTS THE MERGER FILED WERE WRONG. A file count of 22 that was really
    17, taken from an adjacent line of the same report. A guard sweep described
    as covering "this tree" when it covered four files. And a set of figures
    filed as MEASURED that had been relayed out of a research agent's report and
    never re-derived - this repository's own standing rule broken while filing an
    item about not believing green things.
  - A confident diagnosis of the probe's broken control, written into ROADMAP.md
    by the merger and refuted by measurement within the hour. The arithmetic tell
    was in the failing run's own output the whole time.

  ALL OF IT IS CORRECTED IN PLACE RATHER THAN DELETED, because the corrections
  are the part worth reading.

=== WHAT CLOSED - four loop cycles, six commits, 89b0b2a..a60dba7, all pushed ===

  OPS-35  CLOSED. All six criteria. The lane is armed, wired at session scope,
          and interoperation was observed against a live sibling.
  OPS-73  CLOSED. Detection reads a wider alphabet than claiming; an unusable
          bucket raises instead of answering "busy"; both root branches agree
          about whitespace.
  OPS-76  FILED AND CLOSED. The stale arm had NO CALLER anywhere in the tree - a
          fully tested reaper unreachable from every production path, described
          in the present tense by three documents.
  OPS-74  CLOSED. The probe. Its first real run declared its own instrument
          UNPROVEN, which is the only reason its numbers are worth anything.
  OPS-80  FILED AND CLOSED. CLAUDE.md told a cold session facing a fired budget
          to re-run a tool that writes nothing. Split applied, instruction fixed.
  OPS-79  FILED AND CLOSED. Four gaps in the probe's own instrument, including a
          positive control made only of positive specimens - it proved the probe
          could SEE and nothing about whether it INVENTS.
  FILED, open: OPS-75, OPS-77, OPS-78, OPS-81.

=== STATE AT THE WRAP - RE-DERIVE RATHER THAN QUOTING ===

  suite             3061 passed, 1 skipped, observed bare this run. Run it BARE -
                    pytest.ini already carries -q and a second one prints NO
                    summary line at all while still exiting 0.
  ruff              All checks passed.
  origin/main       a60dba7, confirmed with `git rev-parse HEAD origin/main`.
                    main has NO UPSTREAM, so a bare `git push` FAILS and its
                    advice text reads like success when piped to tail. Use
                    `git push origin main`.
  loop              cycle 75, item OPS-78, in_flight empty. Governors released
                    cleanly - the lane lock is gone from the bucket.
  capture watcher   ARMED, identity VERIFIED, all four surfaces fresh. ARCHIVE
                    state UNKNOWN, which is a THIRD answer and correct: the live
                    process was armed by a build older than OPS-59 and writes no
                    archive map. NOT a report that nothing was copied.
  budgets           BOTH RECOVERED. ROADMAP.md 3.2 sessions of headroom, LEDGER
                    5.3, neither in the low-headroom warning any more. The split
                    was applied this session. THE REMEDY CHANGED: it is now
                    `python scripts/apply_doc_split.py --apply`, and
                    tools/doc_archive.py only PLANS and writes nothing. CLAUDE.md
                    says so now. Do not raise the number.

=== BEFORE DISPATCHING PARALLEL WORK ===

Read SHARED_WORKTREE_BAN in ops/store_drift.py and put it in every slice brief -
it is ADVISORY and nothing enforces it. Record dispatches with state.dispatch(),
read state.in_flight_summary(state.load()) FIRST, and capture a per-file baseline
AT DISPATCH: `git worktree add --detach <scratch> HEAD`, collect there, and hand
the merge gate a true per_file_baseline rather than the handful of files you
happened to print.

  DO NOT RUN TWO PYTEST PROCESSES AT ONCE. Targeted runs per slice, full suite at
  the merge. RETIRE YOUR SLICES - state.retire() is ITEM-scoped.

  TELL EVERY SLICE IN WORDS NOT TO TOUCH THE REAL SHARED BUCKET. The code they
  are editing now DELETES from it, and a live sibling is using it.

  THE PROBE COSTS EIGHT MINUTES A RUN because it runs the suite twice. Budget for
  it, and never let a slice run it more than twice.

=== HOW TO WRITE THE NEXT HAND-OFF ===

    python ops/handoff.py --from-file <draft>

OMIT --target - OPS-65. The writer resolves its own root from its own file
location, so a wrap writes the hand-off of THE TREE IT IS WRAPPING. It runs the
redactor over the STRING and refuses BEFORE anything is written, so a refusal
leaves the previous hand-off byte-unchanged. --check-only reports without writing.
