LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-11 session. Overwritten every session; this
file is tracked, and its git history is the record of what each session handed
forward. It is written by ops/handoff.py, never by an editor tool.

Resume Lanternlight from disk. Read CLAUDE.md, then this file, then ROADMAP.md
and docs/LEDGER.md newest-first. Ask nothing; start work.

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. A plain run REPORTS ONLY.
Acknowledge with `python ops/inbox_watch.py --acknowledge`, and only for notes
you have actually read. The UserPromptSubmit hook acknowledges ONCE per session
on the operator's own turn, so notes may be marked seen before you look - that
is OPS-41 by design and is NOT the same as your having read them.

SECOND ACTION
Run `python ops/stop_audit.py --show-last`. A FAIL line is a QUESTION about what
the last turn wrote, not a verdict - it cannot tell a claim from a QUOTATION of
one.

=== THE CAPTURE WATCHER IS OFF, ON PURPOSE. DO NOT RE-ARM IT. ===

The operator instructed the headless lane disarmed in chat on 2026-09-11, at the
wrap. It was terminated with `taskkill /F /PID` issued from PowerShell, and the
kill was verified twice - the pid is gone, and no process anywhere on the machine
carries `armwatch` in its command line any more.

  `check_watcher()` now answers NO_RECORD, and its reason text ends with "Arming
  one is the whole point of handing the machine back in a known state." THAT TEXT
  IS NOT AN INSTRUCTION HERE. The module cannot tell a deliberate disarm from a
  death, which is the whole of LL-0117, so it says the same thing either way.
  The /done ritual's step 8 says to re-arm on NO_RECORD. The operator's
  instruction overrides it. DO NOT RE-ARM WITHOUT THE OPERATOR SAYING SO.

  Nothing is archiving the game log, the saves or the market cache while it is
  off. That is the accepted cost of the instruction, stated here so nobody
  discovers it as a surprise.

  The arming record and the heartbeat were RENAMED rather than deleted, to
  ops/runtime/armwatch.disarmed-2026-09-11.json and
  ops/runtime/armwatch_heartbeat.disarmed-2026-09-11.json, so the evidence
  survives and the watcher stops reporting DEAD with a standing "Re-arm". If the
  operator asks for it back, the PROCESS must be started again - restoring the
  filenames alone would only restore the record of a process that is not running,
  which is precisely the LL-0117 failure.

=== WHAT CLOSED, AND THE ONE THING THAT CHANGED A RULE ===

Eight commits on top of 1e9edbb, all pushed. The eighth is the commit that
carries this file, so no hash for it is written here - see the note under
origin/main below.

  OPS-82  CLOSED, all six criteria. A sibling's defect report about this tree,
          refuted as filed, with a real defect one level under it.
  OPS-78  CLOSED, all five criteria, on the second tool's answer.
  OPS-84  FILED AND CLOSED. LW's write tracer is VENDORED under the license they
          named. THIS CHANGED A RULE IN CLAUDE.md - read the second exception
          there before acting on anything in third_party/.
  OPS-85  FILED AND CLOSED as NO DEFECT, after this session filed it on a defect
          that does not exist. The wrong version is kept in place on purpose.
  OPS-83  FILED, open, and it is your item.

=== YOUR ITEM: OPS-83 ===

49 tests need a POSIX userland, and no guard names what they actually need. It is
the remainder of OPS-78, which closed on the finding that only SEVEN of its 56
false reds were about `bash` at all.

  Criterion 1 is a DECISION, and OPS-78's answer does not transfer without saying
  why. That item solved `git` by guarding on ONE tool name, which works when the
  dependency is one executable. Here the dependency is a SET - a `#!/bin/sh`
  shebang plus find, grep, head, mv, printf, tr and wc. Three shapes are recorded
  in the item with what each costs, and none is obviously right.

  Criterion 4 asks for the ELEVEN that did not come back under the partial shim
  to be identified, and the utility each actually needs to be named. An
  unexplained residue is where the next wrong generalisation comes from.

  RE-MEASURING COSTS EIGHT MINUTES PER PROBE RUN because it runs the suite twice,
  and THE TREE MUST NOT MOVE BETWEEN ITS TWO HALVES. A run was discarded this
  session for exactly that: five tests were added while the probe sat between its
  runs, and the contamination presented as `appeared=5` and `other_change=1`,
  which read like findings rather than like an error.

  Then OPS-75, OPS-77, OPS-81.

=== HELD BY THE OPERATOR - DO NOT ACT ON THESE ALONE ===

OPS-68. The responder runner and cross-project propagation are on STANDBY by the
operator's own words on 2026-09-08. Two notes have now gone out on this channel,
both on specific operator instructions, and NEITHER is a precedent.

OPS-48 remains HELD - do not answer its four questions, do not solicit RC or RSC.

OPS-36 routes through the same closed gate: operator-ruled ADOPT, but criterion 1
needs RC to say which charter version is CURRENT, and asking RC is soliciting RC.

=== THE THING TO LEARN FROM THIS SESSION ===

A WRITE TRACER REPORTS THAT BYTES MOVED. IT CANNOT REPORT THAT STATE CHANGED.
That one sentence is both of this session's real findings, learned twice in a day
from the same instrument, which was working perfectly on both occasions.

  Legion Wallpaper reported that this suite writes the operator's live
  inbox_seen.json and inbox_reported.json. Re-measured here, both records are
  BYTE-IDENTICAL across that test - because LW's tracer cannot see subprocess
  writes, by its own statement, and the hook under test runs in a subprocess. What
  it saw was our own restore putting the records back. A repair looks exactly like
  the damage. The real defect was one level under it: that restore was
  Path.write_bytes, truncate-then-fill, the only non-atomic writer of those
  records in a tree whose production code writes them temp-then-replace.

  Then this session made the same mistake with the same instrument. It filed
  OPS-85 claiming two walker probe files accumulate in the live tree, written from
  the tracer's report without opening the test. They are removed in a `finally`
  and ZERO remain, counted. Caught before the commit only because the acceptance
  criterion it had just written demanded a count including zero.

EVERY DEFECT THE WRAP'S REFUTATION PASS FOUND WAS A DOCUMENT. NONE WAS CODE.
Third consecutive session where that is true, so it is the pattern now rather than
an incident. It handed back four, and the serious one was CLAUDE.md still saying
"Re-implemented, never vendored. No file is copied in" while
third_party/lw_write_tracer/ sat in the tree. CLAUDE.md's own first exception
names the remedy verbatim, and this session had made a genuine rule change and not
written it there. Repaired.

  The other three: an OPS-82 heading that contradicted its own lead line, three
  mislabelled acceptance criteria in the same section, and a ledger entry
  crediting a catch to an acceptance criterion no longer present in the document
  anyone can read. The ledger is append-only, so LL-0233 is the correction rather
  than an edit.

  ONE CLAIM IS RECORDED AS UNVERIFIABLE rather than rounded up: the ROADMAP's
  "3067 collected in BOTH directions" for the bash probe cannot be reconciled
  without an eight-minute re-run. Neither corroborated nor contradicted.

=== THE VENDORED WORK - READ THIS BEFORE TOUCHING third_party/ ===

third_party/lw_write_tracer/ is a work this project did NOT write, vendored under
Apache-2.0 on an operator ruling after LW named the license explicitly. The gate
was SATISFIED, not waived: an earlier copy of that same file was refused here for
carrying no license statement, and this project asked LW to name one rather than
assuming.

  DO NOT EDIT IT. tests/test_vendored_write_tracer.py fails if it changes, and the
  honest response to that red is to declare the change in
  third_party/lw_write_tracer/NOTICE.md, never to update the constant. Wrap it
  instead. ruff.toml excludes third_party/ and says why where it does it: its
  autofix wanted to rewrite the two calls the plugin's own positive control exists
  to exercise, which would have disarmed the control silently with every arm green.

  TO RUN IT over this suite, set PYTHONPATH to third_party/lw_write_tracer and
  pass `-p lw_write_tracer --trace-roots "ops/runtime;logs;moon_sync_inbox"
  --trace-out <path>`. Every number it prints is a LOWER BOUND - it does not see
  subprocess writes, and this suite spawns processes constantly.

=== STATE AT THE WRAP - RE-DERIVE RATHER THAN QUOTING ===

  suite             3082 passed, 1 skipped, observed bare this run and
                    independently re-run by the refutation agent from a clean
                    state. Run it BARE - pytest.ini already carries -q, and a
                    second one prints NO summary line at all while still
                    exiting 0.
  ruff              All checks passed.
  origin/main       NO HASH IS RECORDED HERE ON PURPOSE. This file is tracked
                    and is committed with the session's work, so any tip hash
                    written into it is invalidated by its own commit - a wrap
                    that pins one is publishing a value that was already stale
                    when it was written. RE-DERIVE IT: `git rev-parse HEAD
                    origin/main`, and the two must match. The session's work
                    starts at 1e9edbb and is all pushed.
                    main has NO UPSTREAM, so a bare `git push` FAILS and its
                    advice text reads like success when piped to tail. Use
                    `git push origin main`.
  loop              cycle 76, item OPS-83, in_flight empty.
  capture watcher   DISARMED ON PURPOSE. See the top of this file. NO_RECORD is
                    the expected answer and is not a defect to fix.
  mail              Two notes sent to LW this session, both on operator
                    instructions. Ask ops.outbox.replies_to("LW") rather than
                    listing a sibling's directory - OPS-43.

=== BEFORE DISPATCHING PARALLEL WORK ===

Read SHARED_WORKTREE_BAN in ops/store_drift.py and put it in every slice brief -
it is ADVISORY and nothing enforces it. Record dispatches with state.dispatch(),
read state.in_flight_summary(state.load()) FIRST, and capture a per-file baseline
AT DISPATCH with `git worktree add --detach <scratch> HEAD` rather than the
handful of files you happened to print.

  DO NOT RUN TWO PYTEST PROCESSES AT ONCE. Targeted runs per slice, full suite at
  the merge. RETIRE YOUR SLICES - state.retire() is ITEM-scoped.

  THE PRE-COMMIT HOOK RUNS A DOC-READING SUBSET AGAINST THE STAGED TREE, and it
  refused a commit outright this session - ledger entries out of newest-first
  order, plus two denylist entries that git already answers for. A full `pytest`
  run BEFORE you write the ledger entry does not cover the ledger entry. Write the
  prose first, then run the suite.

=== HOW TO WRITE THE NEXT HAND-OFF ===

    python ops/handoff.py --from-file <draft>

OMIT --target - OPS-65. The writer resolves its own root from its own file
location, so a wrap writes the hand-off of THE TREE IT IS WRAPPING. It runs the
redactor over the STRING and refuses BEFORE anything is written, so a refusal
leaves the previous hand-off byte-unchanged. --check-only reports without writing.
