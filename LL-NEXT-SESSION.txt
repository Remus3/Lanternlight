LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-08 late session. Overwritten every session;
this file is tracked, and its git history is the record of what each session
handed forward. It is written by ops/handoff.py, never by an editor tool.

Resume Lanternlight from disk. Read CLAUDE.md, then this file, then ROADMAP.md
and docs/LEDGER.md newest-first. Ask nothing; start work.

READ FIRST, IN THIS ORDER
  CLAUDE.md           - CHANGED this session. It now carries an Archives block
                        naming the two new archive documents.
  docs/LEDGER.md      - newest first. LL-0196 through LL-0201 are this session.
                        Read LL-0201 first if you read only one: the wrap's own
                        refutation refuted two of this session's closure claims.
  ROADMAP.md          - MUCH SHORTER than you may expect. See THE SPLIT below
                        before concluding anything is missing.

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. A plain run REPORTS ONLY.
Acknowledge with `python ops/inbox_watch.py --acknowledge`, and only for notes
you have actually read.

  Note that the UserPromptSubmit hook acknowledges ONCE per session on the
  operator's own turn, so notes may already be marked seen by the time you look.
  That is by design (OPS-41) and is not the same as your having read them.
  Measured this session: the SessionStart hook reported 2 unread and the manual
  run right after reported none, because the prompt hook had fired in between.

SECOND ACTION
Run `python ops/stop_audit.py --show-last`. A FAIL line is a QUESTION about what
the last turn wrote, not a verdict - it cannot tell a claim from a QUOTATION of
one.

THE SPLIT - READ THIS BEFORE GREPPING FOR ANYTHING
ROADMAP.md and docs/LEDGER.md were SPLIT this session and are now roughly a
third of their former size. Nothing was deleted.

  ROADMAP.md         open items, plus a `## Archive index` with a one-line stub
                     per closed item linking to its full text
  docs/ROADMAP_ARCHIVE.md    65 closed and refuted sections, verbatim
  docs/LEDGER.md     the 60 newest entries, and the insertion marker
  docs/LEDGER_ARCHIVE.md     the oldest 135 entries, verbatim, in order

**AN EMPTY GREP OF ONE HALF IS NOT A CLAIM ABOUT THE PROJECT** - only about
which half you searched. This repository already had two rules about empty greps
being claims about the pattern and about line breaks; this is a third axis and
it is new as of today. Search both files, or search the pair.

  When a size budget FIRES, RE-RUN THE SPLIT - `python tools/doc_archive.py` for
  a dry run - and do NOT raise the number. Raising is what the previous session
  did and it was labelled a deferral at the time. main() is a dry run and writes
  nothing; applying a plan is a separate deliberate act.

CLOSED THIS SESSION, ALL PUSHED (origin/main is at the wrap commit)
  OPS-57  Both continuity documents split into archives, budgets LOWERED rather
          than raised, and three of the item's own filed counts corrected.

FILED THIS SESSION, NOT STARTED
  OPS-61  THIS IS THE NEXT ITEM. See below.
  OPS-62  The two archives are UNBUDGETED and no growth model fits a step-wise
          document. Filed as a stated cost of OPS-57 rather than absorbed.
  OPS-63  The source register never reads ROADMAP.md. Predates the split; the
          split only made it visible.
  OPS-64  tools/archive_link_guard.py accepts any argv and silently ignores it.
          Found by the wrap's refutation pass while trying to break the guard.
  OPS-35, OPS-36 remain open and operator-ruled; do not act on them alone.

NEXT ITEM - OPS-61, and it is already measured
Every hook command in .claude/settings.json names an ABSOLUTE repository root:

    "command": "python C:/Lanternlight/ops/inbox_watch.py"

ops/inbox_watch.py resolves its repo root from its own file location, not from
the working directory. Measured this session: invoked from an unrelated
directory it still reported the ORIGINAL tree's 99 notes without a word about
which tree it had read. So in a clone at any other path, and in every git
WORKTREE - which is where lane sessions actually run - the hook does not run
that tree's script at all.

That is worse than not firing. A hook that does not fire reports nothing; this
one FIRES, SUCCEEDS, and answers a question about a different repository.

  Two answers that are already measured, so you do not re-derive them:
    wiring   TRACKED    .claude/settings.json, git index mode 100644
    inbox    IGNORED    .gitignore line 196, moon_sync_inbox/
  And the good half: a real clone running its OWN script prints
    "CANNOT READ - <path> does not exist. This is a FAILURE to look, NOT
     'nothing new'." and EXITS 0.
  So the honest-report property is already built. The defect is the path.

  WHY NO EXISTING GUARD CATCHES IT, which is the part worth keeping:
  tests/test_no_hardcoded_home_path.py exists for this class and does not reach
  this instance. OPS-38 generalised it once - from this machine's literal
  account name to the SHAPE of any home directory under any account - and
  stopped there. C:/Lanternlight carries no account name, so it passes cleanly.

  Acceptance criterion 2 is the one that will bite: the demonstration must be
  END-TO-END, in a real clone at a different path AND a real git worktree.
  Presence, registration and a green exit are three facts and none of them is
  the fact that the hook read the right tree.

THE MEASUREMENT THAT MATTERS MOST THIS SESSION
THE STAGED TREE IS A DIFFERENT TREE, and it bit three times in one session. A
fully green `python -m pytest` against the working tree was followed twice by
the pre-commit hook REFUSING the commit, because
tests/test_source_register.py's denylist-minimality check only sees a file once
`git ls-files` reports it - which happens at STAGING. ARCHIVE.md and archive.py
in the first commit, SECURITY.md in the second.

  A green suite is not a green commit. If you are about to claim green, say
  which tree you measured.

THE WRAP'S REFUTATION REFUTED TWO OF THIS SESSION'S OWN CLOSURE CLAIMS, and both
were PROSE in a CLOSED section - the seventh time in three sessions.

  1. The OPS-57 outcome stated "633,871 -> 175,390 git blob bytes" as a flat
     fact. Measured at the closing commit: 193,854. Not a mis-measurement - the
     figure was correct AT THE INSTANT OF THE SPLIT, and the document then grew
     because the session wrote its own closure into it. DATE A SIZE, OR DO NOT
     QUOTE ONE.
  2. Criterion 5's headroom (5.1 and 5.0 sessions) was derived from those
     superseded sizes; the guard said 4.5 and 4.5. Corrected in ROADMAP.md and
     in two comment blocks in tools/doc_size_budget.py.

  Note the blind spot, unchanged: the id guard scores headings and ledger
  closures and says NOTHING about whether a section's body is true.

FOUR CORRECTIONS THIS SESSION MADE TO ITS OWN CLAIMS
  1. The first anchor-rule comparison reported 84 of 84 headings disagreeing
     between the splitter and the guard. That was a claim about the CALL - the
     "## " prefix was passed into a function expecting stripped text. On
     stripped text they agree on all 84 and on 8 synthetic probes.
  2. The reasoning that archiving hides a roadmap id is WRONG. spent_ids matches
     any mention, and every archived section leaves a stub naming its id. It is
     the LEDGER half that leaves no stub.
  3. The first ops_ids repair covered two of five readers and the suite stayed
     green while over_allocated() returned {} for a repo with two known
     collisions. Changing WHERE a module reads from is a change to EVERY reader.
  4. The README ledger figure was written as 200 and corrected to 199: the two
     ledger files carry 200 `### LL-` headings and one is the format TEMPLATE.

LL-0199 IS THE ONE TO READ FOR A GENERAL LESSON
Recording WHICH ids the split had made invisible put all five back into live
documents, so LL-0197's 60-to-55 measurement no longer reproduces - it is 63/63
now. The act of writing a measurement down destroyed the condition it measured.
The archive read is therefore DORMANT in spent_ids and LIVE in over_allocated;
do not read a green spent_ids as evidence the archive read is unnecessary.

GITHUB SIDE, done this session
Community standards are at 100 percent. CODE_OF_CONDUCT.md, CONTRIBUTING.md,
SECURITY.md and .github/PULL_REQUEST_TEMPLATE.md now exist, written for this
project rather than templated. Repo topics widened 15 -> 19. README re-measured
from a real fresh clone at a foreign path.

NO NEW GAME DATA SINCE 2026-08-30 - TEN DAYS AT THIS WRAP
Every roadmap item blocked on the operator playing has been blocked that whole
time. Do not schedule a capture session; fold those into whichever session next
has the game running.

  ASK, do not guess, and do not read a date out of a document:
    python -c "from ops.loop import watch; print(watch.check_watcher().reason)"
  Read it as an UPPER BOUND: a QUIET answer is trustworthy, a RECENT one means a
  copy happened, which is not quite the same as new data.

STATE AT THE WRAP - re-derive rather than quoting these
  origin/main       three commits this session. Confirm with
                    `git rev-parse HEAD origin/main` and `git log --oneline -5`.
  python -m pytest  bare. Read the last line yourself; `-q` is already in
                    pytest.ini, so a second one prints NO summary at all.
  python -m ruff check .   clean at the wrap.
  loop              cycle 65, item OPS-61, in_flight empty.
  capture watcher   ARMED, identity VERIFIED, all surfaces fresh, heartbeat
                    inside its threshold. ARCHIVE state reads UNKNOWN, which is
                    correct and expected: the live process was armed by a build
                    older than OPS-59 and writes no archive map. UNKNOWN is a
                    THIRD answer, not a report that nothing was copied.
  budgets           ROADMAP.md 340,000 and docs/LEDGER.md 420,000, both LOWERED
                    from 700,000 and 900,000. Ask the guard for headroom:
                    `python tools/doc_size_budget.py` states it in SESSIONS and
                    is the only figure that cannot go stale.

BEFORE DISPATCHING PARALLEL WORK
Read SHARED_WORKTREE_BAN in ops/store_drift.py and put it in every slice brief -
it is ADVISORY and nothing enforces it. Record dispatches with state.dispatch(),
read state.in_flight_summary(state.load()) FIRST, and capture a per-file
baseline AT DISPATCH to hand the merge gate as per_file_baseline.

  RETIRE YOUR SLICES. This session dispatched six and retired none until the
  wrap, so in_flight read 6 while three were long finished.

  AND DO NOT RUN TWO PYTEST PROCESSES AT ONCE. A subagent's repo-wide run
  overlapped the merger's and tests/test_store_drift.py went red; it passes
  alone. tools/doc_size_budget.py calls `git hash-object -w`, which writes loose
  objects into the real repository, so the object store genuinely moves under a
  test that counts it. OPS-8, "the suite is not safe to run CONCURRENTLY", is
  CLOSED - whether that closure needs revisiting is a question recorded in
  LL-0198 for the operator, not one to answer alone.

HOW TO WRITE THE NEXT HAND-OFF
  python ops/handoff.py --from-file <draft> --target C:/Lanternlight/LL-NEXT-SESSION.txt
It runs the redactor over the STRING and refuses BEFORE anything is written, so
a refusal leaves the previous hand-off byte-unchanged and exits 1. No exemption
list, no override flag. --check-only reports without writing.

OPS-48 IS HELD BY OPERATOR RULING - do not answer its four questions, do not
solicit RC or RSC. OPS-52 IS CLOSED BY OPERATOR RULING - "leave the history as
is"; do not re-file it, and do not treat finding the operator's address in
git log as a new leak.

then start the loop
