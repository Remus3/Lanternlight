LANTERNLIGHT - NEXT SESSION PROMPT
Written at the wrap of the 2026-09-07 session. Overwritten every session; this
file is tracked, and its git history is the record of what each session handed
forward.

READ FIRST, IN THIS ORDER
  CLAUDE.md
  ROADMAP.md - OPS-43 is the next item; OPS-42 question 1 holds a pending
    operator decision that a session must NOT answer
  docs/LEDGER.md - newest first; LL-0159 through LL-0163 are this session,
    and LL-0163 is the wrap's refutation - read it before trusting the rest
  WAKEUP_NOTES.md - top entry
  docs/INVENTORY.md - new this session
  docs/adr/ADR-007-lane-slot-root-is-ours.md - new this session

FIRST ACTION, BEFORE ANY WORK
Run `python ops/inbox_watch.py` and READ the output. Reporting no longer
acknowledges, which is the whole point of this session's work: a plain run
reports only and does not write the state file at all. Three notes are unread
and were deliberately left that way. Acknowledge only what you have actually
read, with an explicit `python ops/inbox_watch.py --acknowledge`.

STATE AT THE WRAP
  origin/main            50c1ec5, working tree clean, everything pushed
  python -m pytest bare  2225 passed, 1 skipped in 139.83s
  python -m ruff check . all checks passed
  merge gate             OK at 2205 collected against a corrected 45-module
                         baseline of 2152, no file's count dropped, measured
                         before the wrap's own three fixes landed
  capture watcher        was DEAD at the wrap and was RE-ARMED. Now ARMED,
                         pid 21680, identity VERIFIED, heartbeat 4 s old,
                         archiving into C:\ll-captures\2026-09-07. Re-check it
                         with ops.loop.watch.check_watcher() rather than
                         believing this line.
  loop                   cycle 53, item OPS-43

WHAT THIS SESSION FINISHED
Five operator rulings were given in chat and are recorded in ROADMAP.md,
WAKEUP_NOTES.md and the ledger so they are never re-litigated.

1. THE INBOX WATCHER. Reporting no longer acknowledges. Withdrawals are now
   reported: an entry that vanishes is listed until an acknowledging run prunes
   it. Three things are load-bearing and each is pinned by an arm that was
   watched go red. The comparison is on the STABLE NAME, because with
   content-digest keys an edit and a withdrawal both move the key. The baseline
   is the union of the reported record and the seen record, because an entry
   listed once and pulled before anyone acknowledged lives only in the reported
   record. And the acknowledgement prunes BOTH records - a sibling shipped this
   same feature with an ack that pruned only one, so the line could never be
   cleared, and every one of its tests still passed.

2. THE WATCHER COVERS THE ENTIRETY OF moon_sync_inbox, on an operator ruling.
   It previously skipped any top-level file whose suffix was not .md, so a
   .txt, .json or extensionless file was invisible as either a note or a drop.
   Those are now keyed and named; their content is still never read into the
   report, and drop containment is unchanged.

3. THE ACCOUNT NAME IS OUT OF PUBLISHED HISTORY. Measured with an armed
   pickaxe: seven commits carried the backslash form, seven the forward-slash
   form, four the 8.3 short form, and five tracked files still carried it live.
   OPS-38 had scoped itself to CODE surfaces, which is why prose survived. The
   rewrite was rehearsed on a throwaway clone first - exactly five blobs
   differed and 156 tracked paths were unchanged - then run for real and
   force-pushed. Re-measured against published origin/main, all forms are 0
   with the control unchanged. The remote carries ZERO refs/pull refs, checked
   with an armed control, so the permanent-ref hole a sibling reported does not
   apply here. RESIDUAL, STATED RATHER THAN HIDDEN: a force-push does not purge
   the host's unreachable objects, which stay fetchable by direct object id
   until the host garbage-collects them. The rewrite reduces exposure; it does
   not end it.

4. THE LANE SLOT. ops/lane_slot.py, 45 tests, ADR-007. The protocol was
   reconstructed from sibling notes and re-implemented: no file copied, no
   sibling module imported, no port allocated, nothing acquired at import time.

5. THE INVENTORY EXCHANGE, BOTH WAYS. docs/INVENTORY.md is generated from
   measurement, carries no stale suite count by design, and is guarded by a
   test asserting every path it names exists and that no account-shaped string
   appears. Inbound practices were checked against this tree before adoption;
   three measured INAPPLICABLE because the tools they depend on do not exist
   here at all.

6. THE VERBATIM DROP IS REMOVED, on an operator ruling. Seven files, 121852
   bytes, confirmed never tracked before deletion. The removal was the first
   live firing of the new withdrawal reporting: the watcher printed the drop as
   WITHDRAWN on real mail instead of going silent.

7. THE OUTSIDE POLLER IS REAL, measured with controls armed in both directions.
   Two corrections to the sender's own account of it: the registered trigger
   carries NO repetition element, so the Windows scheduler enforces no interval
   at all and the idle-derived ladder lives entirely inside a process that
   cannot be observed from outside it; and there is NO trustworthy evidence it
   reads this repository's directory, because NTFS last-access updates are
   disabled machine-wide, which makes access timestamps worthless as evidence
   in either direction. What exists is a live process whose command line
   matches - intent, not an observed read.

THE WRAP'S REFUTATION PASS REFUSED THE MERGE - read this before trusting the
list above, because two of the three blockers were inside guards this same
session had already declared done.

  BLOCKER 1. The account-name history rewrite purged blob CONTENT and left
    COMMIT MESSAGES untouched, and the artifact claimed the job was finished.
    A pickaxe searches diffs and never reads a message, so the clean result was
    a claim about the TOOL, not about the repository. One published commit
    message still carried the spelling. A second rewrite, using a message
    replacement, was run at the wrap. NOW measured on both axes against
    published origin/main with controls armed in both directions: 0 commit
    messages and 0 content hits, control 14 and 10 respectively, and 0
    refs/pull refs on the remote. Anyone re-deriving this must search MESSAGES
    and CONTENT separately - one query does not cover both.

  BLOCKER 2. The arm named for proving that an acknowledgement prunes BOTH
    inbox records did not test that. Deleting the acknowledge branch's write of
    the reported record left all seven inbox modules green. Root cause is the
    part worth keeping: the arm opened by ACKNOWLEDGING, so the mutation removed
    the only write of the record the arm claimed to check, the baseline
    collapsed to the seen record alone, and the assertion passed for an
    unrelated reason. The replacement drives the case from a REPORT-ONLY run.

  BLOCKER 3, on THE HARD BOUNDARY. ops/lane_slot.py calls OpenProcess and was
    missing from the hand-typed roster in tests/test_process_capability.py, so
    widening its mask to PROCESS_ALL_ACCESS left every test green. That file's
    own line 131 had predicted the omission and said nothing would detect it;
    the prediction came true the same day. The roster is now DERIVED from
    parsed source. Closed as OPS-46, with its blindness stated: the API list is
    a denylist and the test tree is excluded by design.

  A MERGER ERROR, recorded because it is this repository's trap twice over. The
    first independent re-mutation of blocker 3 came back GREEN and looked like a
    refutation of the fix. It had replaced the first of two occurrences of the
    literal, which was a COMMENT rather than the constant. The second attempt
    hit the constant but ran only the capability module, while the mask check
    lives in tests/test_loop_watch.py. Both greens were claims about the
    command, not about the guard. Assert the anchor AND check where the
    assertion actually lives.

NEXT ITEM - OPS-43, and why it matters more than it looks
An outgoing note is written straight into a sibling's directory and NO copy is
kept here, and moon_sync_inbox is gitignored, so nothing in this repository
records that any reply was ever sent. A session reading only its own disk
observes nothing and concludes this project has been silent - correctly from
its evidence and wrongly in fact. Every cold session reads only its own disk,
by design. That is not hypothetical: on 2026-09-07 a subagent reported that
Lanternlight had never replied to any of its 71 notes, the merger relayed it
without probing, and it reached WAKEUP_NOTES.md and ledger LL-0161 before the
delivery step listed the destination directories and found six earlier replies
sitting in them. One delivered note carried "first reply on this channel" in
its own title and needed a correction sent after it.

ACCEPTANCE - all four criteria are written out in ROADMAP.md OPS-43. Two
warnings that are easy to miss. Criterion 2 interacts with ruling 2 above: the
watcher now covers the ENTIRETY of the folder, so an outbox placed inside it is
watched and must be CLASSIFIED rather than skipped, or every note we send comes
back as unread mail. Criterion 3 must be proven non-vacuous by removing the
outbox copy, watching the test go red, restoring, and confirming green.

ALSO OPEN
  OPS-41 - a report-only mid-session acknowledge trigger. No UserPromptSubmit
    hook exists in this tree, measured absent rather than empty. Must land
    AFTER OPS-43, and a sibling is adopting the same shape for the same reason.
  OPS-44 - the source-register guard's denylist is absorbing this project's own
    filenames: four entries in one wave, thirteen in the next, and it tripped a
    third time on a ledger entry listing the notes we had just sent. The guard
    reads every dotted token under docs/ and a note filename ends in what
    parses as a two-letter TLD. Both options are stated in the item and NEITHER
    is chosen, because that guard's own docstring reserves logic changes for a
    deliberate decision - an auto-exemption once hid a real TLD.
  OPS-45 - a Stop-hook auditor for transcript claims, aimed squarely at the
    LL-0161 relay failure above.
  OPS-35 criterion 5 - interoperating with a real sibling holder. NOT met, and
    it cannot be met while the lock root stays ours. See the decision below.

PENDING OPERATOR DECISION - DO NOT ANSWER IT YOURSELF
ROADMAP.md OPS-42 question 1. The lane slot is implemented but its lock root is
OURS, at ops/runtime/lane_slots/, overridable by LL_LANE_SLOT_ROOT. It is NOT
the shared machine-wide bucket. The reason: the wider cross-project design has
not landed, so a reserved-style lock would go unrecognised in the shared bucket
today and a surplus lock would consume a slot the other trees are rationing,
which is a change to another tree made unilaterally. The consequence is that we
implement the protocol and currently coordinate with NOBODY. The two options
are written out in the item. Flipping the root is one line. A sibling has taken
the opposite branch and is reversing its own decision record to join. Record
the operator's ruling in the item; do not decide it in a session.

TRAPS THIS SESSION PAID FOR - do not re-learn them
  A grep that ABORTS prints nothing, and a `|| echo "clean"` after it prints a
    clean bill over the crash. That happened here on a PII sweep of outgoing
    notes. Sweep with Python, where a crash cannot look like a negative.
  A per-file test-count baseline goes STALE the moment the wave it is guarding
    adds a module. The baseline dispatched to this session's workers covered 40
    of 47 modules and was blind to 92 tests. Re-derive it from the committed
    module set, not from a measurement taken before the work started.
  Re-run a mutation AFTER any refactor of the code under the guard. A lint
    refactor touched the same function this session; the mutant was re-applied
    and still killed 7 tests, but a refactor is exactly when a guard quietly
    stops guarding.
  Two siblings relaying the same observation is ONE stale observation relayed
    twice, not corroboration. The claim that this repo's git hooks were mode
    100644 reached four inboxes that way and was retracted in writing this
    session. Both are 100755 and have been throughout.
  A subagent's premise is a hypothesis. The "never replied" claim above reached
    two tracked files before anything probed it.

CROSS-PROJECT POSITION, so it is not re-derived
  We answer notes; we never vendor. Five notes were delivered on 2026-09-07 and
  are listed by name in ledger LL-0162, which until OPS-43 lands is the only
  evidence inside this repository that they exist.
  A sibling offered to send source or prose. We asked for PROSE ONLY. That
  follows directly from the ruling to delete the payload already in our inbox
  plus the standing never-vendor rule; it was not a new decision.
  Sibling inbox directories, re-derived by listing C:\*\moon_sync_inbox this
  session rather than read from anywhere: Riot Commander, Legion Wallpaper,
  Clockspeed, Resin Compute. Record this map somewhere durable as part of
  OPS-43 criterion 4.
